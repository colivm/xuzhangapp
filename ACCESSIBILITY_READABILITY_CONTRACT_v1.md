# 叙账可访问性与阅读性契约 v1

> 生效日期：2026-07-15
> 范围：iOS 主产品的五个 Tab、核心 Sheet、会员页、AI 指令台、痕迹与记录入口

## 1. 字号与排版

- 关键标题、正文、说明和状态优先使用 SwiftUI 语义字体（`title3`、`headline`、`body`、`subheadline`、`footnote`、`caption`），随 Dynamic Type 缩放。
- 仅图表内的辅助数字允许使用 `caption2`；图表必须另有完整 VoiceOver 摘要。
- 关键说明不得依靠低于 `0.72` 的文字透明度表达；更低透明度只允许用于纯装饰图层。
- 并排主操作必须使用可自适应布局；空间不足或进入无障碍字号时改为纵向，不截断主要文案。

## 2. 触控与状态

- 所有关键按钮、图标关闭按钮、分段切换和主操作的最小触控尺寸为 `44 × 44 pt`。
- 底部 Tab、记录模式、关闭、保存、导入、继续问和会员购买必须提供明确 VoiceOver 名称；选中状态通过可访问性值表达。
- 加载、识别、保存和导入结果必须能被读成状态，不只依赖颜色或动画。

## 3. VoiceOver

- 自绘图标和视觉图表不得把无意义的内部元素逐个暴露给 VoiceOver。
- 周/月切换、柱状图、节奏图、会员免费/付费对比应合并成一句可理解的摘要。
- 图标按钮必须有 `accessibilityLabel`；目的不明显时补 `accessibilityHint`。

## 4. Reduce Motion

- 开启“减弱动态效果”后，禁止非必要的缩放、位移、弹簧和自动滚动动画。
- 内容切换可保留无位移的即时更新或轻量淡入；业务状态、保存结果和路由结论不得改变。

## 5. 冻结边界

- 本契约不改变账单字段、分类语义、OCR/AI 导入规则、额度、会员价格、购买恢复、周记/月章生成和页面职责。
- 无障碍调整只改变字体、布局承接、触控尺寸、辅助朗读和装饰动画。

## 6. 发版检查

- iPhone 小屏与常规屏：默认字号、特大字号、至少一个无障碍字号。
- VoiceOver：五个 Tab、复盘首卡、AI 指令台、痕迹周/月卡、会员对比、记录模式、OCR 确认和回放完成页。
- Reduce Motion：AI 结果预览、痕迹切换、OCR 待整理层、会员购买状态、回放章节和完成页。
- 自动门禁：`python scripts/accessibility_lint.py` 与 `AccessibilityLayoutPolicyTests`。
