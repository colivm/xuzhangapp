你在 xuzhangapp 实现 Task UI-P2.2 — 复盘 Tab「阅读先行」去报告生成器感（小 PR · 仅 InsightWebView）。

================================================================================
边界
================================================================================

只改 UI 层级、按钮气质、文案壳；不动 AI 生成逻辑、试用计数 enforce、HomeViewModel insight API。

可改：NativeDemoApp/Views/InsightWebView.swift
禁止：StatsWebView、会员价目、PlaybackService、git commit

================================================================================
产品目标
================================================================================

复盘 = 已经发生的温柔总结（先叙），不是「点按钮等报告」的 AI 工具（议退后）。
对齐北极星：回望本身有价值。

================================================================================
任务
================================================================================

1. weeklyInsightSection 置顶且默认展开正文（localWeeklyInsightBlocks 三行），按钮区最多保留 1 个主动作「梳理本周节奏」，「生成周度分享卡」改 quiet secondary

2. monthlyInsightSection：
   - 试用次数「剩余 x/5」移到生成按钮下方 12pt 小字，不置顶
   - 「生成月度复盘」渐变满宽按钮 → 中等 accent 按钮或 quiet「生成这一月的回顾」
   - 「🔒 开通会员解锁无限复盘」橙色渐变 → 一行 link「想多聊几句？了解会员」

3. todayInsightSection 移到最底或加折叠「今日小记（可选）」，避免与「今天」Tab 抢主路径

4. advancedInsightToggle 保持；locked 季度/年度按钮去 🔒 emoji，用 muted outline

5. 面板标题 22pt bold 可保留，但 panel 间距加大、减少「三块报表」堆叠感

================================================================================
验收
================================================================================

[ ] 打开复盘 Tab：先读到周节奏文案，不是先看到「生成」大按钮
[ ] 无橙色渐变锁按钮
[ ] 月度生成 / 试用 / 会员跳转功能仍可用
[ ] 今日小记仍可生成

@WALKTHROUGH_ISSUES_QUEUE.md
@NativeDemoApp/Views/InsightWebView.swift
@PRODUCT_NORTH_STAR.md
