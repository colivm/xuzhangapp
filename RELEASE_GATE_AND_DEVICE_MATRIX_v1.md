# 叙账 RELEASE-01 发版门禁与统一真机矩阵 v1

> 日期：2026-07-15
> 适用：`NativeDemoApp` iOS 主产品
> 当前状态：2026-07-16 Windows/代码门禁通过；当前为 Windows 环境，Xcode、StoreKit 沙盒和 iPhone 真机矩阵因工具与设备不可用而阻塞
> 结论规则：自动门禁、Xcode 门禁和本文件全部必测项都通过后，才允许把相关任务标为 `VERIFIED`。

## 1. 冻结边界

本门禁只提供确定性夹具、测试接线、Debug 隔离装载和验收记录，不改变：

- 账单字段、分类、OCR/AI 结论、额度、会员价格和购买恢复逻辑。
- 今日回放、周记、月章、生活线索和月度整理的生成规则。
- 云端 DTO、同步冲突规则和“照片仅本机”的产品承诺。
- `site/` 正式官网的行为与视觉；历史 Web Demo 已退役，不再作为发版基准。

任何真机失败只允许定向修复对应任务，不得借发版检查重写其他已完成项。

## 2. 确定性夹具基线

夹具目录：`qa/release_fixtures/`。日期采用 UTC 的 Apple 参考时间秒数；三个规模共享相同的前缀 ID 和生成公式。

| 记录数 | 金额分总和 | 含图记录 | 图片数 | OCR pending/resolved | 年份 | 记录摘要前 12 位 |
|---:|---:|---:|---:|---:|---|---|
| 100 | 2,462,750 | 8 | 15 | 5 / 5 | 2024/2025/2026 | `f3a282ed166f` |
| 1,000 | 25,077,500 | 77 | 153 | 46 / 45 | 2024/2025/2026 | `0117d6c97b78` |
| 5,000 | 125,487,500 | 385 | 769 | 228 / 227 | 2024/2025/2026 | `6c597531a746` |

每档均覆盖全部十个分类、旧单图、旧多图、非零封面、多图顺序、有效 1×1 PNG、OCR 待整理/已整理、记忆上下文和跨年日期。完整总摘要固定为：

```text
df670606b42414bdf43f34d66d2b5977f897eaf5dc0fe3e5cc428f18977e7129
```

重新生成与验证：

```powershell
python scripts/generate_release_fixtures.py
python scripts/validate_release_gate.py --phase fixtures
```

生成后若摘要变化，必须先解释公式或数据边界为什么变化；不得直接更新基线掩盖回归。

PERF-04 另提供 3 张确定性真实尺寸 JPEG，位于 `NativeDemoApp/Resources/QARealPhotos/`：均不少于 12 MP、单张不少于 2 MB。它们只用于真机冷启动、滚动、缩略图和原图内存压力，不替代上表迁移夹具。

## 3. 自动门禁顺序

### 3.1 Windows/跨平台代码门禁

```powershell
python scripts/validate_release_gate.py --phase windows
```

该命令按失败即停顺序执行：夹具结构与摘要、`git diff --check`、生活语义回归、体验静态检查、文案体验检查、文案 lint、迁移样本和 SQLite schema 实际执行。体验静态检查内部继续执行术语、会员价值、AI 能力、无障碍和可观测性专项 lint。

### 3.2 macOS/Xcode 门禁

```bash
python3 scripts/validate_release_gate.py --phase all \
  --simulator-destination 'platform=iOS Simulator,name=iPhone 15'
```

必须保留以下三段独立结果，任一失败均不能发版：

1. Debug build。
2. Release build。
3. `NativeDemoAppTests` 全部 XCTest。

同时检查 Xcode 日志中无新增 Swift concurrency、actor isolation、Sendable、SQLite 链接、SwiftUI 泛型推断或可访问性 API 警告/错误。

2026-07-16 环境审计：当前系统为 Windows 10（10.0.19045），`xcodebuild`、`swift`、`simctl`、`instruments` 均不可用。因此本节没有执行，状态为 `BLOCKED`；解除条件是在 macOS/Xcode 环境运行上述三段命令并保存独立日志。

## 4. Debug 真机夹具装载

在 Xcode Scheme 的 Run Arguments 添加：

```text
-QAReleaseFixtureCount 1000
-QAReleaseFixtureReset
```

记录数只接受 `100`、`1000`、`5000`。也可使用环境变量 `QA_RELEASE_FIXTURE_COUNT` 和 `QA_RELEASE_FIXTURE_RESET=1`。

- 仅 Debug 生效；Release 构建不会装载。
- 账本写入 `Documents/QAReleaseFixtures/ledger_<count>_<photo-profile>/`，不覆盖普通账本。
- `-QAReleaseFixtureReset` 每次新进程先重建对应隔离账本；同一进程不会反复清空。
- 首次启动先写旧格式 JSON，再走真实图片文件化和 SQLite 激活路径。
- `tiny` 默认配置按上一条执行；`realistic` 配置直接建立隔离 SQLite/图片目录，避免把数百 MB Base64 JSON 当作性能目标本身。
- 夹具模式自动停止 `HomeViewModel` 云端上传/删除/合并，防止假数据进入真实账号。
- R-11 同步必须关闭夹具参数，并使用专用测试账号与可清理的云端环境。

完成每档性能测试后，从 Xcode Devices and Simulators 下载 App Container，再执行精确迁移审计：

```powershell
python scripts/validate_release_gate.py --phase device-audit `
  --device-container "D:\evidence\NativeDemoApp.xcappdata" `
  --expected-count 5000 `
  --photo-profile tiny
```

审计会读取隔离目录中的 SQLite 和图片文件，核对 quick check、schema、记录数、金额分、分类、OCR 状态、图片 SHA/顺序/封面、文件 byte count 和迁移 manifest。

### 4.1 PERF-04 真实照片性能装载

首次建立隔离数据：

```text
-QAReleaseFixtureCount 1000
-QAReleasePhotoProfile realistic
-QAReleaseFixtureReset
```

建立完成后移除 `-QAReleaseFixtureReset`，完全杀进程并连续冷启动 5 次。控制台必须出现 `PERF-04 ledger metadata cold start`，同时使用 Instruments 的 App Launch、Core Animation 和 Allocations/Memory Graph 记录：

| ID | 操作 | 通过标准 | 状态 |
|---|---|---|---|
| REAL-01 | 1,000 条、153 张 12 MP/约 3 MB 照片，连续冷启动 5 次 | 启动只读元数据；首屏可交互 p50 ≤ 1.8s、p95 ≤ 2.5s；无逐张原图读取峰值 | `NOT_RUN` |
| REAL-02 | 首页连续上下滚动 30 秒，再进入痕迹周/月卡并横滑多图 | 列表只加载缩略图；>100ms hitch 不超过 3 次，无单次 >400ms；图片出现时不跳错记录；横滑图片不误切周/月 | `NOT_RUN` |
| REAL-03 | 快速打开/关闭 10 条带图详情，并切换多图 | 详情才读取原图；关闭后内存可回落；无图片串位、黑屏或持续增长 | `NOT_RUN` |
| REAL-04 | 编辑一条无图记录、编辑一条带图记录、删中间图、换封面 | 只变更对应 SQLite 行和记录图片目录；其他图片修改时间不变 | `NOT_RUN` |
| REAL-05 | 记录稳定内存 | 首页稳定内存 ≤220 MB、滚动峰值 ≤320 MB、原图详情峰值 ≤420 MB；退出详情后 30 秒内明显回落 | `NOT_RUN` |
| REAL-06 | 在痕迹页用显式“本周/本月”连续切换 20 次，每次穿插多图横滑和快速回滑 | 周/月切换不依赖整卡横扫；图片手势只归图片容器；>100ms hitch 不超过 3 次，无单次 >400ms；无黑屏、旧范围反写或内存持续增长 | `NOT_RUN` |

若设备低于 iPhone 13 或系统调试开销明显，允许同时记录设备基线对照，但不得只用模拟器替代以上真机结论。

## 5. 统一真机环境

至少覆盖：

- 一台小屏 iPhone 与一台常规/大屏 iPhone。
- 当前最低支持 iOS 与当前稳定 iOS；若只能使用一台设备，另一个系统版本用模拟器补布局，不替代 StoreKit、相册、权限和性能真机证据。
- 免费用户、月/年订阅沙盒用户、永久会员沙盒用户、未登录和专用同步账号。
- Wi-Fi 正常、离线、弱网/请求失败三种联网状态。

证据统一填写：设备/系统、构建号、夹具规模、前置状态、操作、实际结果、截图或录屏、日志、结论（`PASS`/`FAIL`/`BLOCKED`）和缺陷编号。

## 6. FIX-001 / FIX-002 必测

| ID | 前置与操作 | 通过标准 | 状态 |
|---|---|---|---|
| FIX-001-A | AI 指令台生成多条草稿并“确认导入” | 保存后回到指令台顶部，有成功承接，无白屏、重复 Sheet 或重复保存 | `NOT_RUN` |
| FIX-001-B | 只剩最后一条草稿时单笔保存 | 结果清空后页面仍有标题/输入区，不停在空白滚动区域 | `NOT_RUN` |
| FIX-001-C | 有结果时点清空，再立即输入新指令 | 回顶、输入可用、旧任务不回写 | `NOT_RUN` |
| FIX-002-A | 工作日 08:30 前补记今天通勤 | 今天不生成未来通勤 | `NOT_RUN` |
| FIX-002-B | 工作日 08:30～18:29 补记今天通勤 | 只生成早高峰，不生成晚间 | `NOT_RUN` |
| FIX-002-C | 工作日 18:30 后补记今天通勤 | 可生成早晚两条，重复检测仍有效 | `NOT_RUN` |
| FIX-002-D | 补记昨天/上周，另测周末/节假日 | 历史早晚规则和原工作日判断不变 | `NOT_RUN` |

## 7. 固定 R-01～R-12 回归

| ID | 核心操作 | 必查边界 | 建议数据 | 状态 |
|---|---|---|---|---|
| R-01 | 手动只输金额保存 | 只保存一次；标题/分类预填正确；成功承接不叠层 | 空账本、100 | `NOT_RUN` |
| R-02 | 手动改分类、备注、日期后切 Tab 返回并保存 | 用户锁定不被推荐覆盖；草稿日期不漂移；保存后才重置 | 空账本、100 | `NOT_RUN` |
| R-03 | 第一笔分别选“继续记”“听今日回放” | 未点击播放不扣额度；点击只扣一次；照片/奖励/宠物按队列出现 | 空账本 | `NOT_RUN` |
| R-04 | OCR 识别、取消、确认、快速重复点、待整理编辑/删除/全部确认 | 取消不写入；一次提交；pending/resolved 正确；关闭不偷偷导入 | 空账本、100 | `NOT_RUN` |
| R-05 | AI 通勤补记 | 执行 FIX-002 全部时段；历史和重复检测不变 | 空账本、100 | `NOT_RUN` |
| R-06 | AI 查询/补记后批量、单笔保存和清空 | 执行 FIX-001；快速重跑只落最后结果；切 Tab/关闭后旧任务不反写 | 100/1,000/5,000 | `NOT_RUN` |
| R-07 | 今日回放开始、暂停、继续、重播、关闭、额度用尽 | 明确开始才扣；退出完成度正确；关闭后队列继续且不叠层 | 100 | `NOT_RUN` |
| R-08 | 周记/月章切换、播放、空数据、弱数据、额度用尽 | 痕迹为完整章节主入口；周/月不串；旧内容承接；额度常量不变 | 空账本、100/1,000/5,000 | `NOT_RUN` |
| R-09 | 痕迹生活/线索、周/月/自定义/分类快速切换并切 Tab 返回 | 筛选/滚动保留；旧计算不反写；1,000/5,000 条可持续滚动 | 1,000/5,000 | `NOT_RUN` |
| R-10 | 多图新增、删中间图、换封面、缺图、相册取消/失败 | 顺序/封面稳定；缺图只影响该图；失败不破坏账本；孤儿清理 | 100/1,000 | `NOT_RUN` |
| R-11 | 专用账号上传、另一设备拉取、冲突合并、退出登录、删除云端字段 | 本地不被意外清空；冲突保留最新；照片不上传且文案明确 | 关闭 QA 参数 | `NOT_RUN` |
| R-12 | 月/年/永久购买成功、取消、失败、重复点、恢复成功/无权益 | 遮罩复位；权益和高亮正确；无重复购买；恢复结论准确 | StoreKit 沙盒 | `NOT_RUN` |

## 8. 前序优化集中签收

| 范围 | 必测项 | 通过标准 | 状态 |
|---|---|---|---|
| INT-01 | 连续保存两笔，分别触发照片与奖励；首笔选两种承接 | 同一时刻一个提示；FIFO 不丢失；回放只在点击后扣额度 | `NOT_RUN` |
| INT-02 | 首笔后连续保存、20 分钟内/后、跨自然日分别触发照片与奖励 | 单次保存最多一个强提示；每天最多两个；20 分钟冷却；奖励资格和照片入口仍存在 | `NOT_RUN` |
| NAV-01 | AI/痕迹/回放/设置会员入口快速连点和下滑关闭 | 不出现 `Attempt to present...`、空白 Sheet、重复页或目标丢失 | `NOT_RUN` |
| NAV-02 | 痕迹、复盘、记录页分别建立上下文后跨 Tab 往返 | 模式、筛选、滚动和未提交草稿保留；只构建当前 Tab | `NOT_RUN` |
| DATA-02/03 | 三档夹具首次启动迁移、单笔增删改、重启 | device-audit 全通过；已有图片/旧 JSON 不被无关重写；失败可恢复 | `NOT_RUN` |
| DATA-04 | 本地备份导出、缺图导出、清空本机危险提示 | 包含可用照片并报告缺图；不声称云端含照片 | `NOT_RUN` |
| DATA-06 | 导出后重新导入、预览取消、较新/较旧冲突、缺图包、篡改包与恢复失败 | 确认前零写入；只合并新增或较新记录；本机相同/较新版本保留；缺图位置可见；篡改/失败不替换原账本 | `NOT_RUN` |
| PERF-01/02 | 1,000/5,000 条滚动、输入、切范围、快速重跑/取消 | 主线程无明显长卡顿；加载态可取消；旧结果不回写 | `NOT_RUN` |
| PERF-03/DATA-05 | 真实照片启动、缩略图/原图加载、单笔增删改 | 启动不 hydrate 原图；只处理变化记录和对应图片目录；失败可回退 | `NOT_RUN` |
| PERF-04/05 | 执行 REAL-01～REAL-06 | 真实照片尺寸/字节验证通过；冷启动、滚动、痕迹周/月切换、图片横滑和内存达到阈值 | `NOT_RUN` |
| PROD-01/02 | 从痕迹看周/月章，从复盘继续问；全入口术语巡检 | 职责可理解；不出现并行旧称或第二份完整周/月章 | `NOT_RUN` |
| MEMBER-01/AI | 免费、会员、联网开关、远程成功/失败/缺 Key/额度耗尽 | 两层价值清晰；次数不变；本机/远程/回退来源真实；不编造事实 | `NOT_RUN` |
| MEMBER-02 | 未登录购买/恢复、登录取消/失败/成功、登录后再次确认、快速重复点 | 会员页直达登录；套餐/恢复意图只续接一次；登录后不自动扣款；Product ID、价格、验证和账号绑定不变 | `NOT_RUN` |
| OBS-01 | 首记、首播、周/月、AI、会员漏斗与耗时 | 事件顺序/桶正确；本机最多 1,000 条/30 天；无金额、标题、备注等敏感字段 | `NOT_RUN` |

### 8.1 DATA-06 本地备份恢复专项

| ID | 操作 | 通过标准 | 状态 |
|---|---|---|---|
| BACKUP-01 | 导出含多图/非首图封面的备份，再从“备份与联网”选择该包 | 校验在后台完成；预览显示时间、记录数和照片数；尚未确认时本机账本与图片目录不变 | `NOT_RUN` |
| BACKUP-02 | 在预览页取消、点遮罩取消，再重新选择并确认 | 两种取消都零写入；确认后新增记录进入正确日期顺序，照片顺序和封面不变 | `NOT_RUN` |
| BACKUP-03 | 同 ID 分别准备“本机较新、时间相同、备份较新”三组冲突 | 本机较新和相同版本保留；只有备份较新版本更新；每个恢复意图只执行一次 | `NOT_RUN` |
| BACKUP-04 | 导入官方导出但报告缺图的包，再导入照片内容被篡改或清单数量不符的包 | 官方缺图包可恢复并保留缺图位置；篡改/清单异常包拒绝，原账本和原照片保持不变 | `NOT_RUN` |
| BACKUP-05 | 1,000 条真实照片夹具导出并恢复，恢复时尝试下滑关闭 | 有明确加载态；恢复期间不可误关或重复提交；完成后仅变化记录/图片落盘，无空账本或持续卡死 | `NOT_RUN` |

### 8.2 MEMBER-02 登录续购专项

| ID | 操作 | 通过标准 | 状态 |
|---|---|---|---|
| MEMBER-LOGIN-01 | 未登录时分别点年度、月度、永久会员 | 直接打开账号登录 Sheet；返回后仍保留原套餐和实时价格；未再次点击前不出现 StoreKit 购买页 | `NOT_RUN` |
| MEMBER-LOGIN-02 | 登录 Sheet 发送验证码失败、验证码错误、取消和下滑关闭 | 可安全重试或取消；不调用购买、不出现重复 Sheet；回到会员页后可重新选择 | `NOT_RUN` |
| MEMBER-LOGIN-03 | 免费账号登录成功，再点续接卡片一次并快速重复点 | 只在明确点击后发起一次原套餐购买；购买遮罩阻止重复提交；Product ID 与 appAccountToken 绑定不变 | `NOT_RUN` |
| MEMBER-LOGIN-04 | 未登录点恢复购买，登录成功后暂不继续，再重新操作并确认 | 登录后不自动查询 StoreKit；恢复意图只消费一次；取消后可重新发起，恢复结果仍绑定当前账号 | `NOT_RUN` |
| MEMBER-LOGIN-05 | 登录到已有有效会员或永久会员的账号 | 直接同步现有权益并停止续购意图，不重复购买；会员页显示正确层级与到期状态 | `NOT_RUN` |

### 8.3 PERF-05 / LOGIC-01～07 产品状态矩阵

以下矩阵按状态验证唯一主动作、保留入口和取消/失败边界。代码与 Windows 门禁通过不替代本表真机结论。

| ID | 前置状态 | 操作 | 通过标准 | 状态 |
|---|---|---|---|---|
| FLOW-01 | 空账本、免费、无草稿 | 冷启动并分别进入首页、复盘、痕迹 | 首页主动作只引导记第一笔；复盘显示空态并直达手动记账；不执行空聚合、不展示自动会员推销；痕迹不伪造章节 | `NOT_RUN` |
| FLOW-02 | 只有 1 笔今日记录、尚未回放 | 返回首页并选择继续记或今日回放 | 今日回放是可选下一步；继续记录仍可达；未明确播放不扣额度 | `NOT_RUN` |
| FLOW-03 | 1～2 笔有效记录、今日无新回放内容 | 首页、复盘和痕迹之间往返 | 主动作回到继续记录；复盘可查已有记录但不声称周/月成熟；不自动打开会员页 | `NOT_RUN` |
| FLOW-04 | 本周至少 3 笔、当前周记未完成且有额度 | 首页进入本周痕迹并完成/取消回放 | 首页推荐本周；取消不误记完成；完成后当前周不重复抢主动作，复盘成为可选下一步 | `NOT_RUN` |
| FLOW-05 | 本月至少 3 笔、日期 24 日及以前 | 首页和痕迹周/月入口巡检 | 不提前推荐月章；用户主动查看本月仍可进入，不改额度或历史数据 | `NOT_RUN` |
| FLOW-06 | 本月至少 3 笔、日期 25 日及以后、月章未完成且有额度 | 首页进入月章并完成/取消 | 月章只在成熟后推荐；取消不误记完成；完成后可选复盘 | `NOT_RUN` |
| FLOW-07 | 有 OCR 待整理，同时存在手动草稿或成熟回放 | 返回首页并切换记录入口 | OCR 待整理优先；其后是手动草稿；取消 OCR 或保存失败时手动金额、分类、备注和日期不丢失 | `NOT_RUN` |
| FLOW-08 | 已开始手动金额输入 | 展开补充细节、切 Tab、触发键盘保存、尝试 OCR | 保存保持主动作；OCR 侧门不抢流程；分类/备注/日期为可选；失败可重试且草稿不重置 | `NOT_RUN` |
| FLOW-09 | 免费用户完成今日/周/月回放 | 连续完成、关闭会员提示、切换到其他页面再触发自动场景 | 自动提示每日最多一次；“稍后再说”后场景内和跨自动场景均进入冷却；关闭不跳会员页 | `NOT_RUN` |
| FLOW-10 | 免费用户主动点额度用尽或受限能力 | 在自动提示冷却期间主动点击 | 用户主动入口立即打开对应会员上下文，不被自动提示预算阻断；取消返回原页面 | `NOT_RUN` |
| FLOW-11 | 未登录免费用户，分别选择套餐和恢复 | 登录失败/取消/成功后继续或返回 | 登录失败/取消可重试且不购买；成功后只保留一次原意图；再次明确点击才调用 StoreKit；Product ID、实时价格和账号绑定不变 | `NOT_RUN` |
| FLOW-12 | 周/月卡包含 1～9 张真实尺寸照片 | 横滑图片、关键词，再用显式按钮切本周/本月 | 内部横滑不切时间范围；显式按钮切换不丢筛选；无双图实时模糊、串图、黑屏和明显掉帧 | `NOT_RUN` |
| FLOW-13 | 痕迹主卡为本周/本月，细查分别选择周/月/年、自定义日期和分类 | 打开细查、切换条件、关闭并返回主卡 | 周/月细查与主卡范围同步；年/自定义/分类只影响线索和明细，不污染周/月生活章节缓存 | `NOT_RUN` |
| FLOW-14 | 备份导入预览、图片选择、AI/OCR 和保存均准备失败样本 | 逐项取消、失败、重试并快速重复点击 | 确认前零写入；失败不清空原账本/草稿/图片；旧异步结果不回写；重试只提交一次 | `NOT_RUN` |
| FLOW-15 | 本周与上周同期准备不同金额、笔数和分类，并补测其中一段为空 | 在复盘选择“做对比”，查看双周期总览、分类变化和对比依据 | 两段金额/笔数并列且使用同尺度；本周按上周相同已过天数比较；分类差额可见；相关记录分别归入两段，任一段为空仍明确显示；全程只读 | `NOT_RUN` |
| FLOW-16 | 准备近 7 天与前 7 天金额/笔数不同、多个分类、20 条通勤候选，并分别使用默认/特大/无障碍字号 | 进入复盘首屏，依次切换查记录、做对比、补遗漏，点击分类快捷入口，展开长候选列表后返回首屏 | 首屏金额、差额、笔数、活跃天数、最高分类和每日分布准确；任务卡上下文与数据一致；三种任务标题/输入/推荐/结果明确区分；分类快捷入口使用明确时间和分类；候选默认收敛且可完整展开；小屏和大字不截断关键动作；查/比零写入、补记确认前零写入 | `NOT_RUN` |
| FLOW-17 | 使用默认、纸境·湿墨、霓虹深渊、气象·雷阵雨、工业·安全橙点、永享·夜读，分别覆盖 light/dark；准备 30 条查询证据、双周期分类对比、20 条补记候选和雨天记忆卡 | 每套模式进入复盘首屏和 AI 指令台，连续上下滚动 30 秒，切换查/比/补、横滑推荐指令、展开记录/候选并返回顶部 | 页面/导航/普通卡片/输入/任务切换/指标/记录列表使用主题 Token；当前周期用主题强调色、上一周期保持中性色；支出增加、疑似重复、确认前写入和雨天装饰保留稳定语义；无固定白卡或滚动材质穿透；>100ms hitch 不超过 3 次、无单次 >400ms，长列表无整批瞬时构建和持续内存增长 | `NOT_RUN` |
| FLOW-18 | 准备含餐饮、交通、咖啡、重复记录和通勤习惯的账本；输入简体/繁体/口语同义句、隐式对比、否定补记、普通“生成通勤”、主观问题和账本外主体 | 依次执行“这礼拜吃饭用了多少”“最近坐车花销”“前仨月咖啡花费”“這週吃飯花了多少錢”“本月比上月多多少”“不要补记今天通勤”“生成今天通勤”“生成一份今天通勤统计”“交通不错吗”“老板今天怎么样”“上次买可乐是哪天”“这周打车是哪天” | 同义表达落到一致查账范围与分类；隐式双周期进入只读对比；否定补记和普通生成均零候选零写入；带“统计”的生成按只读查询；主观/账本外问题明确不支持；最近一笔与范围查询不串路；所有结果可核对原始记录，重复执行确定一致 | `NOT_RUN` |
| FLOW-19 | 准备本周/上周同期总额和笔数不同的数据，至少覆盖一个新增分类、一个消失分类、一个增减分类、一个金额持平分类和每段超过 5/30 笔记录；覆盖默认、特大、无障碍字号及两套深浅主题 | 生成对比后核对顶部两段总览；在默认“差异来源”查看变化类型、分类金额条和变化占比，展开全部分类；切换“原始记录”并展开两段记录；再运行新对比、清空和切换任务 | 顶部金额/笔数/同期口径与改版前一致且不重复显示“主要分类变化”；默认进入差异来源，新增/消失/增加/减少/持续只由分类金额和笔数决定，不做标题模糊配对；变化占比按分类金额差绝对值计算且不解释原因；原始记录仍分两段、时间倒序、默认各 5 笔、每段最多 30 笔，汇总按全部记录；新结果/清空/任务切换重置默认态；切换零重算、零写入，主题、大字、VoiceOver 和滚动无回退 | `NOT_RUN` |
| FLOW-20 | 分别生成查询、对比、记忆、重复核对、缺金额、不支持和含补记候选的结果，并把普通结果滚动到底部 | 普通结果点击“继续问”；随后修改保留的指令并运行；再核对缺金额、不支持和补记候选的底部动作 | 普通结果不再出现“重新整理一次”；点击“继续问”不重算、不写入、不清空原指令，结果收起后快速回到顶部并自动聚焦输入框；可直接修改后生成新结果；补记保存、缺金额和不支持动作保持原样；Reduce Motion 下无非必要动画，VoiceOver 能读出返回输入的意图 | `NOT_RUN` |
| FLOW-21 | 准备至少覆盖新增、消失、增加、减少、持续五种状态的双周期分类数据；分别使用默认/深色主题、默认/特大/无障碍字号 | 打开“差异来源”，核对状态数量概览、每张分类卡的图标/颜色/主金额/说明和两段条形；滚到底部查看“继续问” | 状态概览数量与既有分类结果一致；五种状态通过图标、局部语义色和左侧状态线可区分，卡片背景/正文仍跟随主题；消失显示本期零点和虚线轨迹，减少显示浅色差异尾段，持续显示等号金额与“两段一致”，上一周期保持中性色且共用原最大金额尺度；金额/笔数/排序/占比与改版前一致；“继续问”为明确向上箭头，不再像字母或光标；大字、VoiceOver、Reduce Motion 和连续滚动无回退 | `NOT_RUN` |
| FLOW-22 | 准备至少 14 笔、覆盖三个以上分类且近 7 天与前 7 天金额不同的账本；分别使用默认/深色主题、默认/特大/无障碍字号，并在应用重启和新增一笔后重复 | 进入复盘首屏，观察标题、近 7 天看板、三任务和分类快捷入口；依次从首屏打开查记录/做对比，清空后关闭再重新打开；在指令台内连续切换查/比/补 10 次 | 首屏保持现有主题与三任务顺序，标题/看板/任务卡高度收敛且分类快捷入口更早露出；三指标共用一个轻量承载层，无卡中三张独立白卡；默认指令每次可见，用户编辑/清空/重新打开不出现父子文本不同步；任务切换不触发主线程账本扫描，1,000 条记录下无明显停顿、无单次 >400ms hitch；推荐内容与改版前一致；大字、VoiceOver、Reduce Motion 和 44pt 触控无回退 | `NOT_RUN` |
| FLOW-23 | 使用 1,000 条记录，其中 180 天内包含同一时段/日期类型的重复金额和稳定分类；准备会员/非会员两种状态 | 进入手动记录空态观察常用金额；连续输入金额、编辑备注、展开/收起分类与日期、在金额和备注焦点间切换、停留滚动 30 秒；修改日期、保存一笔后再重复，并在输入变化后立即尝试保存 | 空态常用金额只在账本修订或有效日期上下文变化时重建；金额/备注变化只生成对应草稿快照，普通重绘、焦点切换和折叠展开不扫描完整账本；分类推荐、预填标题、生活线索和最终保存字段与改版前一致，旧草稿结果不短暂用于新金额；生活线索后台准备且旧请求不覆盖新草稿；1,000 条下输入、键盘、滚动无明显停顿或单次 >400ms hitch，会员边界、Dynamic Type、VoiceOver 和 Reduce Motion 无回退 | `NOT_RUN` |
| FLOW-24 | 使用 1,000 条记录，今天至少 20 笔并覆盖可识别生活线索；过去 120 天准备四个以上工作日同方向、同金额通勤记录，同时覆盖早间、午间补记、晚间和已存在今日通勤四种时间状态 | 冷启动进入首页，连续滚动、展开今天全部记录、编辑/删除/新增一笔、切换会员状态并等待两个 60 秒计时周期；分别在通勤时间边界前后重进首页 | 首页主动作只读取账本派生事实，不在普通重绘重复统计完整账本；可见记录生活线索按账本修订/日期/会员一次后台准备，20 行不产生 20 次主线程完整历史签名；通勤建议只在账本修订或分钟时间桶变化时后台生成，标题、金额、早晚/补记、重复阻止和一键保存规则与改版前一致；提示预算与主动作优先级不变；1,000 条下滚动、Sheet 开关和计时器触发无明显停顿或单次 >400ms hitch，旧任务不覆盖新账本/新时间桶 | `NOT_RUN` |
| FLOW-25 | 使用 1,000 条记录，其中今天分别准备 0、3、9、20 笔并覆盖上午/下午/晚上、生活线索和场景；会员/非会员各验证一次 | 从首页明确开始今日回放；在播放中连续暂停/播放、点选胶片、重播并等待完成；播放期间另触发主题/宠物状态变化，关闭后新增一笔再重新打开 | 每次打开前只按当前账本修订生成一份今天记录、章节和总时长快照；播放期间只更新索引、进度、暂停和完成状态，不重新筛选/排序完整账本、不重建生活线索章节；0/3/9/20 笔的章节、顺序、文案、时长和改版前一致；关闭时完成度仍按本次冻结记录结算，新增一笔后下一次打开才进入新快照；额度只在明确开始时扣除，会员提示和完成动作不变；1,000 条下播放动画、胶片滑动与重播无明显停顿或单次 >400ms hitch | `NOT_RUN` |
| FLOW-26 | 使用 1,000 条跨至少 12 个月的记录，包含连续天、草稿、生活线索和照片引用；准备未登录、已登录非会员、订阅会员和永久会员四种状态 | 打开会员页，连续切换套餐、展开更多方案、滚动长期档案、触发登录续购卡和购买遮罩；保持页面打开时新增/编辑一笔后继续操作 | 首次进入及账本修订时以不可变输入后台生成会员证明与长期档案；套餐切换不触发账本扫描或完整签名，购买遮罩、登录续购和普通滚动只读已发布快照；草稿不计入记录数，连续天、月份跨度、生活线索、标题与文案和改版前一致；账本变化后只发布最新修订，旧请求不覆盖新结果；Product ID、价格、套餐高亮、登录续购一次性意图、购买/恢复和 StoreKit 验证不变；1,000 条下滚动与套餐切换无明显停顿或单次 >400ms hitch | `NOT_RUN` |
| FLOW-27 | 使用 1,000 条跨周、跨月并包含连续/间断日期和零金额记录的账本；准备未登录、免费、订阅和永久会员状态 | 进入“我的”，连续切换同步/联网/宠物等设置、编辑昵称并开关账号 Sheet；在账号详情中滚动会员记忆卡；新增、编辑、删除一笔后重复 | 账号摘要与账号详情共用同一份账本修订统计快照；记录数、最长连续天、周记素材数和月份数与改版前一致，零金额仍不计入；普通重绘不统计完整账本，登录状态、输入焦点、设置开关和 Sheet 开关只读快照；账本变化时后台生成新结果且旧请求不反写；设置、同步、会员展示、登录、清空/删除等危险操作语义不变；1,000 条下“我的”滚动、输入和 Sheet 开关无明显停顿或单次 >400ms hitch | `NOT_RUN` |
| FLOW-28 | 使用 1,000 条跨年记录，准备周/月/年、自定义日期、全部分类/单分类、零金额和同日多笔数据 | 在痕迹打开“细查这一段”，反复切换时间与分类、展开自定义日期、滚动长列表；进入行内编辑，修改日期/分类/金额并保存，再删除一笔和关闭/重开 Sheet | 每个账本修订与筛选组合只生成一次细查快照，匹配记录、ID、总额和按日分组共用同一结果；列表数量、金额、日期倒序、同日时间倒序和空态与改版前一致；编辑删除语义不变，筛选外记录会关闭编辑态；普通滚动、滑动和 Sheet 状态不重复筛选完整账本；痕迹周/月主章节、缓存、额度、手势和路由不变；1,000 条下筛选与滚动无明显停顿或单次 >400ms hitch | `NOT_RUN` |
| FLOW-29 | 在周记/月章分享编辑器选择一张接近 1600px 和一张大于 12MP 的本地照片；准备多种分享样式与隐私确认路径 | 选择自定义背景后连续切换全部样式、展开/收起选择器、滚动和预览 30 秒；保存分享图、移除背景、重新选择另一张并再次保存 | 每次照片数据变化只规范化并解码一次，选择器预览和最终分享卡共用已解码图片；样式切换不重复解码，移除后图片与数据缓存同时清空，重新选择只使用新图；1600px 上限、JPEG 质量、相册权限、隐私确认、保存提示和导出结果不变；大图下连续切换与滚动无明显内存尖峰、卡顿或单次 >400ms hitch | `NOT_RUN` |
| FLOW-30 | 开启首页宠物，准备 0/1/多笔、通勤＋咖啡、记录时高温＋当前下雨、敏感记录和用户明确文字；覆盖默认/深色/高对比主题、待机、连续点击、保存后消息、强提示遮挡、切 Tab、锁屏/后台、低电量、Reduce Motion 和 VoiceOver | 首页停留 60 秒观察待机；连续开关气泡 20 次并在消息返回前后切 Tab；等待触摸/保存后气泡自动关闭；触发第一笔/照片/奖励/会员提示；切后台再返回；分别开启低电量、Reduce Motion 和 VoiceOver | 首页不出现系统 emoji；待机、点击和说话三组像素帧清晰且不染主题色，承载底板/气泡继续适配主题；触摸与保存后消息共用单一气泡，普通模式约 4 秒自动关闭，VoiceOver 保留足够朗读时间，再次点击立即收起；迟到请求、切 Tab、后台和强提示不能重新打开气泡；通勤＋咖啡优先描述真实记录，当前下雨只说“现在”，不得生成“家里的花费”；记录时高温与当前天气分开，系统暖标签不代言用户感受，敏感记录不复述标题；最近消息避免连续重复；强提示仍按原优先级让位；后台、低电量和 Reduce Motion 显示稳定帧且无持续任务；60 秒无持续首页重算、明显 hitch、任务堆叠或内存增长 | `NOT_RUN` |
| FLOW-31 | 准备最近 7 天 0/1/多笔餐饮记录、餐饮加交通的跨分类记录，以及未指定分类但结果碰巧只有餐饮一类的账本；覆盖默认/特大/无障碍字号和 VoiceOver | 分别执行“最近 7 天餐饮记录”“最近 7 天记录”“最近 7 天哪类最多”，核对总额、笔数、三项指标和原始记录 | 显式单分类查询显示“有记录 / 平均每笔 / 最高单笔”，不再出现“金额最高：餐饮 / 该类金额”；0 笔时后两项为明确空态，1 笔时平均每笔与最高单笔允许相同；未指定分类、跨分类或明确询问分类构成时显示“有记录 / 最高分类 / 该类金额”，即使结果只有一类也不按结果猜成单分类；总额、记录集合、排序、正文最高单笔、识别、主题和只读边界不变；大字与 VoiceOver 标题和值语义清楚 | `NOT_RUN` |
| FLOW-32 | 覆盖记账主页、普通编辑、聚焦编辑、痕迹内联编辑、记忆详情和 OCR 确认六个记录时间入口；准备今天、历史日期、00:00、23:59、月末、闰日和 DST 日期；覆盖小屏、特大/无障碍字号、VoiceOver 与 Reduce Motion | 从 22:55 打开时间入口，快速滚动到 08:05 后完成；重复测试 00:00、23:59、“现在”、取消、下滑关闭、切记录/OCR 行和页面离开；痕迹自定义范围只调整日期 | 六个入口均使用小时/分钟滚轮，滑动期间不写回、不触发预填/保存/OCR 提交，完成时只提交一次且秒归零；取消、下滑、切记录/行与离开零写入；“现在”只在所选日期为今天时显示且不改日期；跨 23/0、月末、闰日和 DST 保持原日期并得到有效时间；痕迹统计日期范围不出现时间入口；44pt 触控、VoiceOver 可调语义、大字布局和主题可读性通过 | `NOT_RUN` |
| FLOW-33 | 准备未登录非会员、已登录非会员、月度/年度会员和永久会员；分别从设置、OCR、回放、场景包、AI 指令台、痕迹和永久主题入口打开；覆盖小屏、特大/无障碍字号、VoiceOver 与 Reduce Motion | 核对各身份首屏与向下滚动层级；执行未登录购买→登录后续购、购买取消/失败、恢复无权益/成功；订阅会员打开 App Store 管理入口并恢复；永久会员查看长期档案与保存边界 | 非会员保留场景 Hero、原套餐顺序/价格和唯一一张“免费与会员”双价值对比，同一价值不再重复；订阅会员显示状态、紧凑已解锁、管理/恢复、长期档案和保存边界，不出现销售对比；永久会员从生效状态直接进入真实档案，不显示对比、套餐或重复价值卡；账单字段可同步与照片仅本机说明准确；Product ID、StoreKit 验证、finish、登录续购一次性意图、失败/取消/恢复语义和全部入口上下文不变 | `NOT_RUN` |
| FLOW-34 | 准备结构化 hot/cold/rain/snow 通勤、普通天气通勤、高温餐饮、高温普通打车、渔具/露营/骑行/摄影/模型/乐器、普通购物、仅有暖标签的旧记录、外地上下文和无匹配账本；覆盖会员/非会员、查记录/做对比/补遗漏任务、默认/深色主题、Dynamic Type 与 VoiceOver | 在查记录输入“高温通勤/热天通勤/酷热天上班/冷天通勤/雨天通勤/雪天通勤/爱好类消费/兴趣消费/外地消费”；在补遗漏输入相同名词短语和“补记高温通勤”；再输入“辛苦了/热天辛苦/今天很热吗/小投入/爱好值得吗/买这个划算吗” | 查记录的高置信名词短语进入只读查询；天气＋通勤必须同时成立，高温餐饮/普通通勤/普通打车不误入；兴趣查询只命中具体兴趣物件或活动，不读取“爱好里的小投入”等暖标签；当前天气不补写历史，旧数据只用受控事实片段；无记录显示“已识别但无匹配”，不显示未识别；补遗漏中的名词短语零候选零写入，明确补记仍走原通勤预览且不伪造天气；主观/价值词保持阻止；非会员看到原会员能力边界；结果保留原始记录、单次快照、主题和无障碍语义 | `NOT_RUN` |
| FLOW-35 | 准备会员/非会员、周记 3 笔同日与跨 2 日、月章 5 笔跨 2/3 日、24/25 日、普通剩余额度与仅剩 1 次额度；覆盖默认/深色主题、特大字号和 VoiceOver | 分别冷启动首页，手动进入未被主动推荐的周/月痕迹；完成周记和月章，依次点击“完成”“继续问”“先看本周”和低优先级会员入口 | 周记只有达到 3 笔且覆盖 2 个记录日才主动推荐；月章只有达到 5 笔、覆盖 3 个记录日且到 25 日后才主动推荐；未达到时首页自然说明内容会在更完整时出现，痕迹手动入口始终可用。会员与非会员完成区主按钮均为“完成”并正常关闭；周/月均可选进入复盘且不自动跳会员页；普通剩余额度不显示会员承接，仅剩 1 次时会员入口位于所有用户任务之后；完成记录、额度扣次、回放内容、首页主动作优先顺序、价格和 StoreKit 不变 | `NOT_RUN` |
| FLOW-36 | 使用专用同步账号准备本机与云端各有记录；分别准备有效 90 天新令牌、过期令牌、服务端轮换 `JWT_SECRET` 后的旧令牌、离线、400 校验错误和 500 服务错误 | 冷启动刷新账号，手动同步，新增一笔触发自动上传，删除一笔触发云端删除，并切换同步开关；随后重新登录并确认合并 | 新签发令牌 `exp - iat` 为 90 天；有效令牌正常同步。过期或密钥轮换令牌收到 401 后立即清除旧会话、关闭当前本机同步态并明确提示重新登录，设置页不再显示假登录；本机账本、照片、备份和云端记录均不删除，原账号同步偏好保留，重新登录仍走显式合并确认。离线、超时、400 和 500 不误退出；同一轮重复 401 只失效一次；账单 DTO、`updatedAt` 冲突规则和照片仅本机边界不变 | `NOT_RUN` |
| FLOW-37 | 准备 0/1/2/3 张照片、缺失引用、损坏图片和三张真实 12MP 照片；覆盖冷启动、未命中缓存、相册拒绝、默认/深色主题、特大字号、VoiceOver 与 Reduce Motion | 完成周记后打开保存故事图；准备期间连续切换样式、关闭重开、快速连续点击保存 20 次；分别保存正常、缺图、损坏图和 12MP 组合，保存中尝试换样式与关闭页面 | 图片准备反馈只出现在操作层，保存按钮在准备完成前不可用；最终相册图片永远不出现转圈、“正在加载图片”或空白图片区。预览与保存使用同一模板、照片和文案；同一照片只读取解码一次，前景与模糊背景复用；缺失/损坏图片按真实可用数降级且不修改原账本引用；连续点击只执行一次保存，保存中模板不能改变，关闭页面取消未完成任务；12MP 图片后台降采样，主线程无明显长卡顿或持续内存增长；相册拒绝保留预览并可重试 | `NOT_RUN` |
| FLOW-38 | 分别准备 0/1/2/3 张真实可用照片、照片全部损坏后降为 0 张，以及自定义背景；覆盖普通/敏感标题、默认/深色主题、特大字号与 VoiceOver | 打开周记保存页，核对自动模板、手动样式数量和中文名称；逐个保存记录摘要、单图记忆、周记拼页和自定义背景 | 0 图自动使用记录摘要且不出现空图片区；1 图自动使用单图记忆，主图约占卡片一半并显示“日期 · 安全记录说明”；2/3 图自动使用周记拼页。手动列表只提供当前照片能力允许的记录摘要、单图记忆、周记拼页，自定义背景只能由用户明确选择；不再暴露全屏照片、胶片、杂志等未做质量门槛的强模板。标题不再是“这一周，一张照片”，副文不重复标题；指标统一为记录数、记录日和真实照片数；品牌只出现一次，成品无仿二维码或不可扫码图块；敏感记录不扩写标题，主题、大字和 VoiceOver 无截断或错误语义 | `NOT_RUN` |
| FLOW-39 | 分别准备周记有照片/无照片、月章有照片/无照片、空账本、同分类并列、1/多记录日和一张缺失引用照片；覆盖默认/深色/高对比主题、特大/无障碍字号、VoiceOver、Reduce Motion 与真实 12MP 照片 | 进入痕迹，在本周/本月按钮间切换并横滑周记照片与本月日记；核对周记标题、月章封面、无图热力和下方日记首屏 | 周记以代表记录的真实日期和安全标题回答“这周发生了哪一幕”，辅助句明确笔数、记录日和最高频分类，不出现“被留下/有轮廓”等模板句。月章以最高频分类、笔数占比、记录日和最长连续记录回答“这个月形成了什么”；有图时照片仅作为右侧小封面，同一封面记录不在本月日记首屏重复；无图时显示记录日热力且没有空图片区或抽象房间插画。所有事实随章节快照一次准备，周/月切换、横滑和主题变化不重复扫描完整账本；空数据、缺图和并列分类结果稳定；多主题、大字、VoiceOver、Reduce Motion 与 12MP 滚动无布局截断、串图或明显 hitch | `NOT_RUN` |
| FLOW-40 | 准备首次无缓存、已有本周快照但本月未完成、已有旧线索但本月线索需刷新、缓存命中、真实空数据和 1,000 条账本；覆盖默认/深色/高对比主题、特大/无障碍字号、VoiceOver 与 Reduce Motion | 冷启动进入痕迹；快速切换本周/本月与生活/线索各 10 次；在本月痕迹和本月线索整理中尝试滚动、点击旧卡；等待完成后重复缓存命中路径，并在切换中离开 Tab 再返回 | 全页只出现一个加载反馈，挂在 ScrollView viewport 中央并带主题适配轻遮罩；本月分别准确显示“正在整理本月痕迹…”和“正在整理本月线索…”，不再同时出现顶部 Pill、滚动内容加载卡或卡片右上角进度。首次无快照立即显示；有旧快照的刷新不足 150ms 不闪遮罩，超过 150ms 才显示且旧内容不可误触；完成时内容无整页位移/高度动画，只淡出遮罩。真实空数据由有效快照展示空态；快速切换、取消和离开页面后旧请求不重开遮罩、不覆盖新内容；多主题、大字、VoiceOver、Reduce Motion 与 1,000 条数据无明显跳动或 hitch | `NOT_RUN` |
| FLOW-41 | 准备工作日早通勤历史：120 天内至少 4 笔、3 个工作日、稳定金额，并另备不满足样本、已有今日早通勤、仅有早餐、OCR 待整理和手动草稿；覆盖冷启动、首页后台过夜、同一分钟反复前后台、跨分钟/跨日、周末及 1,000 条账本 | 前晚停留首页并锁屏，次日于个人通勤窗口内直接恢复；随后连续打开控制中心/来电式 inactive→active 20 次，等待两个分钟桶；分别在个人窗口前、窗口内、窗口后以及已有通勤/高优先级主动作状态重试 | 后台过夜恢复后立即用当前分钟准备候选，符合规则时出现“一键记通勤”，不再等待最长 60 秒 Timer；同一分钟/同账本重复 active 零重算、无卡片闪烁和任务堆叠，跨分钟/跨日只发布最新请求。工作日、个人窗口、历史样本、稳定金额、今日重复阻止和首页动态主动作优先级与改前一致；周末、不满足样本、已有早通勤及 OCR/草稿/今日回放等高优先级状态不误弹；不自动保存、不生成通知。1,000 条账本下前台恢复、滚动和卡片出现无主线程长卡顿或明显 hitch | `NOT_RUN` |
| FLOW-42 | 准备周记 0/1/2/3+ 笔、同日多笔、每天各一笔、全部通勤、全部同类、自动情绪标签、用户标题、无安全标题和有/无照片；准备月章月初空、后段空、前后段均有、上月同期 0/1/3+ 笔、明显增减、持平、新增/消失分类及本月 1～10 日场景；覆盖默认/深色主题、宠物开关、特大字号与 VoiceOver | 分别播放周记和月章，逐章核对标题、主文、辅助证据和收尾；来回切换宠物开关，关闭重开并检查同一账本确定性；对本月 1 日至今天与上月相同日序手工复算金额、笔数和分类变化 | 周记保持弱数据 3 章、成熟数据 5 章，月章保持 6 章及原时长；周记明确概况、分布、具体一笔、可靠重复和收尾，月章明确月初、后来、上月同期与月内重复。正文不出现胶片/气味/画面等抽象模板，不直出自动情绪、内部场景或“第 N 次”标签，不由宠物代言；无证据不推断感受。主文与辅助层不复述同一事实，月初/后来不复用同一记录；本月只和上月同期比较，上月不足 3 笔时明确不做环比。0/弱/敏感/缺图路径事实准确；Dynamic Type、VoiceOver 和多主题下不截断，播放控制、照片、进度、额度、会员、分享和完成动作与改前一致 | `NOT_RUN` |
| FLOW-43 | 分别准备本周 3 笔同日、3 笔跨 2 日、成熟周记但今日仍有未回放记录、额度为 0、已完成当前周记和下一 ISO 周的新数据；保留痕迹上次停留在本月、线索、自定义范围与本周生活四种状态 | 冷启动观察首页主动作和痕迹 Tab 热点；依次进入本月、线索、加载中立即返回、本周有效章节并返回首页；再播放周记至 80%，同周重开和跨周重试 | 3 笔仅 1 个记录日不亮热点且首页不主动推荐周记；3 笔跨 2 日且可播放、未完成、未查看时热点出现，今日回放仍保持更高主动作。进入本月、线索、未完成加载或自定义范围不消费；只有生活＋本周的当前有效快照显示后热点消失，且不写周记播放完成。同周不重复提示，跨周重新按新成熟度判断；额度不足和已完成播放不亮热点，80% 完成状态不回退；手动痕迹入口、周/月快照、加载遮罩、首页顺序和滚动性能不变 | `NOT_RUN` |
| FLOW-44 | 准备冷缓存、热缓存、0/1/多笔今天记录、带/不带生活线索、手工保存、快捷通勤、行内编辑及 1,000 条账本；覆盖 Reduce Motion 与低电量 | 从记录页保存后立即返回首页，连续重复 20 次；观察宠物首帧、最新账单和既有账单，等待首页生活线索后台发布；再执行快捷通勤和行内编辑 | 宠物底板出现时同步显示稳定像素首帧，不出现透明空框；今日小记不在视图渲染中扫描完整账本，宠物任务不被同步生活线索聚合推迟。手工保存和快捷通勤返回首页不播放自动账单发光；既有账单线索在新快照完成前保持，发布后不经历清空再出现；行内编辑仍保留一次明确反馈。0/1/多笔结果、生活线索事实、首页主动作、提示预算、低电量和 Reduce Motion 行为不变，1,000 条下无明显首帧 hitch | `NOT_RUN` |
| FLOW-45 | 在小屏/常规屏、默认/深色主题、横竖尺寸变化、特大字号、VoiceOver、Reduce Motion 和低电量下开启宠物；准备气泡显示与隐藏两种状态 | 从右下拖到左侧、上方和边界外，松手并切 Tab/杀进程重开；连续执行点击、短拖、长按各 20 次；使用 VoiceOver 自定义动作移到左右侧和隐藏，再从“我的”重新开启 | 拖动期间只有宠物浮层移动，首页账本/主动作不重算；松手吸附最近左右边缘，垂直位置被顶部和底部安全区夹取并在重启后恢复，尺寸变化后不出屏。短拖不触发说话，点击不改变位置，0.6 秒长按只隐藏一次且不再触发点击；提示明确可从“我的”恢复，重新开启仍使用保存位置。气泡朝屏幕内侧展开，不遮出屏；强提示继续覆盖宠物，像素帧、主题、低电量、Reduce Motion 和 VoiceOver 语义保持 | `NOT_RUN` |
| FLOW-46 | 开启天气与宠物，准备当前高温/雨/雪/低温、普通咖啡、明确冷饮、热饮、通勤、购物类“冰美式随行杯”、当天保存与历史补记；另准备首次和后续点击宠物 | 首次点击宠物后再次点击；分别在四种天气下点击并保存当天记录；补记昨天咖啡/通勤；让天气缓存超过 1 小时后再次点击 | 首次点击只出现一次可执行的“拖动换位、长按休息”说明，后续恢复正常陪伴；当前天气只在 1 小时内作为“现在”的关怀，历史补记和过期天气不附会当前天气。普通咖啡可与高温并列并提醒防晒补水，但不得称为冷饮；只有餐饮记录标题明确含冰饮/冷饮/雪糕等证据才可称冷饮，购物类同词不误判。雨雪通勤只陈述真实通勤并给当前出行提醒，不推断居家、感受、消费原因或已经饮用；系统暖标签继续不作为事实。消息仍服从单一气泡生命周期、近期去重、强提示让位与首页零重算边界 | `NOT_RUN` |
| FLOW-47 | 准备今天 0/1/8 笔记录，覆盖长标题、情绪标签、生活线索、无图/单图/多图；使用小屏/常规屏、默认/深色主题、特大/无障碍字号、VoiceOver 与 Reduce Motion | 从首页打开“今天留下的痕迹”，连续滚动并依次点按有图/无图记录，左右滑动每行并删除一条，再编辑保存；对比改前可见行数与底部汇总 | 顶部只保留“点任一条可调整”操作提示，笔数和合计仅在底部汇总一次；默认字号下外层、行间和行内留白明显收敛，同屏可见记录增加。情绪与生活线索空间足够时同排、空间不足时自然纵排，不截断关键事实；照片继续保持 82pt 可辨识高度。有图仍进入详情，无图仍进入编辑，左滑删除、取消、保存、排序、金额、分类、时刻、主题与 VoiceOver 语义不变；长列表滚动无新增重算或明显 hitch | `NOT_RUN` |
| FLOW-48 | 准备今天 0 笔且无历史、存在稳定常用金额、存在本周主场景三种首页状态；天气开关分别开/关，宠物开启；覆盖早/午/晚、默认/深色主题、特大字号和 VoiceOver | 冷启动并反复进入首页，读取首页今日小记、宠物首次交互提示及后续空态气泡；再新增第一笔确认非空文案 | 首页今日小记统一平静说明“今天还没有记录”；有稳定历史时只补“往常这个时间常记什么”或“本周哪类场景较多”的一层事实，无历史时只说明当前页为空，不出现“从这里开始、只输金额、先放进账本”等催促。宠物首次仍只说明真实拖动/长按操作，后续 0 笔气泡只表达陪伴，不说“硬凑、先记、想起一笔再写”；天气可用时仍可优先给当前关怀。新增第一笔后原 1 笔以上今日小记、宠物真实账单候选、首页动态主动作、按钮目的地和成熟门槛不变；多主题、大字和 VoiceOver 无截断或重复朗读 | `NOT_RUN` |
| FLOW-49 | 依次准备空账本、无通勤用户、仅一笔停车费、最近与前 7 天都有重复餐饮或兴趣记录、跨 2 个日期的明确通勤、当前下雨但历史无雨天通勤、以及真实历史雨天通勤；另用 1,000 条账本覆盖缓存与修订更新 | 进入复盘后先停留首屏等待推荐后台准备，再分别打开查记录、做对比、补遗漏并来回切换 20 次；新增/编辑一笔后观察推荐刷新；分别执行“对比最近 7 天和前 7 天的消费”与“对比本周和上周的消费” | 空账本和非通勤用户只显示不假设分类/职业的通用查账与时段对比，补遗漏允许没有推荐；单笔停车费、低金额交通和当前下雨均不能制造交通/通勤推荐，当前下雨只有存在真实历史雨天通勤时才可推荐回查。餐饮/兴趣等具体推荐只在真实跨日或跨周期证据成立时出现；明确通勤需跨至少 2 个日期才可推荐补记。复盘首页“做对比”承接最近 7 天/前 7 天，用户主动输入本周仍按上周同期日序。推荐在复盘首屏按账本修订、会员和有效天气后台预生成；点进任务和任务切换只读缓存，不重新扫描全局账本，1,000 条下连续切换无明显 hitch、旧结果反写或滚动掉帧 | `NOT_RUN` |
| FLOW-50 | 分别装载 100/1,000/5,000 条账本，准备手动保存、普通编辑、OCR 单条/批量、AI 批量补记、照片新增/删除/换封面、删除记录、备份恢复和云端合并；首页另覆盖同日、跨日和会员身份切换 | 对每种逻辑提交用 Instruments 记录提交后 2 秒的 Main Thread Hitches、SwiftUI 更新、任务数和内存；提交后立即滚动首页，再切痕迹本周/本月与复盘查/比/补；连续快速编辑同一记录 20 次并等待稳定 | 完整账本排序、今天/周/月/年筛选和旅程事实不在主线程 getter 中执行；一个逻辑编辑只发布一个记录值，快速 20 次修改只接受最后修订且无任务持续累积。首页同日同会员保留旧主文直到新快照一次替换，跨日/会员变化立即清除不安全内容，旧通勤候选不可继续写入；复盘替换无整页布局动画，痕迹先稳定可见范围再可取消预热另一范围。三个页面均无白屏、卡片消失后重现、旧结果反写或明显滚动掉帧；5,000 条下无单次 >400ms 主线程 hitch，提交后 2 秒内任务与内存回落 | `NOT_RUN` |
| FLOW-51 | 清除宠物位置偏好后冷启动，并另准备左/右及高/低四种已保存位置；覆盖气泡隐藏/显示、首页可滚动长内容、小屏/常规屏、VoiceOver 与 Reduce Motion | 默认状态连续点击宠物 20 次开关气泡；从宠物本体向上、下、左、右和斜向拖动，分别穿过页面纵向滚动手势；做 1～7pt 短抖、0.6 秒长按，切 Tab/重启后复查 | 首次默认精确位于旧版右下角（右 16、底 102），气泡出现/消失时宠物中心坐标不变，气泡只向屏幕内侧展开。超过 8pt 的拖动由宠物优先接管、实时跟手且不带动页面滚动，松手吸附边缘并保存；小于阈值的抖动只按点击处理且不写位置。长按只隐藏不点击，既有有效位置继续恢复；连续操作不触发首页账本重算、位置跳变或手势任务累积 | `NOT_RUN` |
| FLOW-52 | 准备本周、上周同期、上周完整周、前一周、本月、上月同期、上月完整月和前一个月金额/笔数明显不同的数据；覆盖空账本和繁体输入 | 在做对比中依次输入“对比上周”“和上周比”“比比上周”“这周对比上周”“上周对比前一周”“比较上周和前一周”“对比上月”“本月对比上月”“上月对比前一个月”；再到查记录输入“查上周记录”“查上月记录” | 省略主体的上周/上月参照分别补全为“本周 对比 上周同期”“本月 对比 上月同期”；显式当前周期结果一致。只有明确写出前一周/前一个月等历史参照时才显示“上周 对比 前一周”“上个月 对比 前一个月”。普通查询仍只查字面指定的上周/上月，不被补成本周/本月；周按相同星期日序、月按相同月日序，金额、笔数、分类、证据和只读边界不变 | `NOT_RUN` |
| FLOW-53 | 开启宠物并分别放在左/右、顶部/底部四个已保存位置；气泡显隐各一轮，首页准备可滚动长内容；覆盖慢拖、快拖、停顿后继续、横/竖/斜向、1～7pt 抖动、Reduce Motion 开关 | 用手指连续拖动宠物 20 次，并录屏逐帧对照手指与宠物中心；拖动超过 450ms、跨过屏幕中线和抵达四边界；同时观察首页是否滚动，再点按和长按 | 拖动采样使用不随宠物移动的 viewport 坐标，宠物位移连续跟随手指，不出现前后帧往返振荡、归零或动画追赶。活跃拖动期间不写普通状态、不启动 450ms 延迟任务，首页不滚动；松手只提交一次、无过冲后回弹，随后吸附最近左右边缘并保存。1～7pt 仍按点击且不保存，0.6 秒长按仍隐藏；气泡、默认右下角、安全边界、VoiceOver、像素帧和首页数据零重算保持 | `NOT_RUN` |
| FLOW-54 | 准备周记 0/1/2/3+ 笔和月章数据；覆盖两笔咖啡、结构化高温通勤、雨雪/冷天、周末早餐、默认日常标签、停车费暖标签、跨记录旧标签、医疗/债务等敏感记录，以及主文或辅助依据已包含同一词的去重样本；使用默认/深色/高对比主题、特大/无障碍字号、VoiceOver 与 Reduce Motion，并装载 1,000 条账本 | 分别播放周记与月章，逐章检查主文、数字依据和底部辅助标签；开关宠物、暂停/继续、前后切章并连续滚动 30 秒；修改一笔账后重新生成并对照新快照 | 主文继续保持 `COPY-02 v2` 的自然事实表达，不拼入自动标签；只有首个总览章按资格最多显示一个“生活线索”和一个“情绪标签”，例如真实咖啡重复与记录自身高温通勤。默认/弱暖/跨记录/敏感标签不展示，里程碑和内部词条不回流；主文、supportLine 与两个标签之间以及跨章节均不重复。无合格信号时不留空位；两个长标签在窄屏/大字下纵向承接，VoiceOver 分别准确朗读。辅助信号只在周/月快照构建时准备一次，播放索引、动画、滚动、主题和宠物开关不重新扫描账本；章节数量/顺序/时长、照片、进度、额度、会员、分享、控制与完成动作均不变 | `NOT_RUN` |
| FLOW-55 | 准备连续三周均有咖啡、其中一周咖啡明显增多、另一周另有用户自写标题或真实照片；再准备普通餐饮与医疗/债务词条混在同一场景、只有敏感记录、空账本和 1,000 条账本 | 依次生成日/周/月叙事计划并检查主线、辅助观察、生活线索和事实依据；相同修订重复生成 20 次，再改变修订和近期主线集合 | 咖啡可持续作为高置信生活线索，但无新信息时不得连续成为主线；明显增减、隔期回归、用户自写标题或真实照片可让其重新进入主线。所有输出只引用输入快照中的证据 ID，普通与敏感记录混组时敏感 ID、标题、照片和标签不进入任何角色；只有敏感记录时使用不展开的私密保底，不谎称没有记录。相同输入结果确定，角色规划不修改账本、UI、播放结构、额度、会员、主题或宠物；1,000 条下模板切换、播放索引和滚动只读既有计划，不触发全账本重算 | `NOT_RUN` |
| FLOW-56 | 准备 0/1/2/3 张真实可用照片、三张均损坏后降为 0 张、自定义背景、连续两周相同咖啡、咖啡明显增多、另有用户自写标题/照片、普通与敏感记录混组；使用默认/深色/高对比主题、特大字号、VoiceOver 和三张真实 12MP 照片 | 完成周记后打开保存故事图，逐一切换当前照片能力下的三个安全模板并保存；准备中、模板切换中和连续点击保存 20 次观察照片读取、文案与内存 | 0/1/2/3 张照片各有且只有三个安全内置模板，自定义背景仍独立显式选择；全部缺损按 0 图矩阵降级。模板只改变构图，不改变事实：主线、轻总结、辅助观察和最多两个生活线索不在左右/上下重复，页面不再解释“系统如何排版”；稳定咖啡可在生活线索出现但无变化时不反复占主标题，有真实变化/原话/照片时可重新成为主线。敏感标题、照片细节和证据 ID 不泄漏。模板切换只读同一 `narrativePlan` 与不可变 prepared photos，不重扫账本、不重新读取/解码照片；预览与保存同源，照片顺序、缺图降级、权限、保存去重、额度、播放结构和主题边界不变 | `NOT_RUN` |
| FLOW-57 | 准备今日/本周/本月 0/1/2/3/9 笔，连续两日/两周/两月相同咖啡、咖啡明显增减、用户自写标题、真实照片、普通通勤/餐食/爱好、医疗敏感混组；覆盖宠物开关、Reduce Motion、VoiceOver、1,000 条账本 | 分别播放今日回放、弱/成熟周记和月章；逐卡逐章记录标题、正文、辅助标签、章节数/时长与额度，暂停/重播/前后切换各 20 次并连续滚动 | 今日回放开场和收尾不再同时重复稳定咖啡，咖啡仍可在对应单笔卡真实出现；周记开场保持笔数/记录日概况，代表一笔优先承接用户原话或照片，稳定咖啡只在“反复出现”或生活线索层出现；月章月初/后来优先承接所在时段的高信息记录。弱周记仍 3 章、成熟周记仍 5 章、月章仍 6 章且顺序/时长不变；`COPY-FIX-02` 首章辅助标签不被主文吞并。播放索引、滚动、宠物、主题切换只读已准备快照，不重扫账本或再跑生活线索聚合；敏感记录不进入角色计划。照片、进度、暂停、免费次数、会员、分享和完成动作保持不变 | `NOT_RUN` |
| FLOW-58 | 分别准备三周同星期通勤、连续每周相同咖啡、咖啡较上周同期 ±1/±2/50% 边界、空一周后咖啡回归、月中查看但上月月末有额外记录、同时满足两个候选、混入医疗/债务敏感记录、旧账本修订和 1,000 条历史 | 生成日/周/月“过去的回声”，重复同一输入 20 次；将同一 echo ID 放入近期冷却后重试；修改一笔并以新旧修订交叉返回；在今日收尾、周/月末章和分享图观察呈现 | 基础场景回声仍限定 `repeat/return/change` 三类；新增的受控上下文回归和新组合只按 FLOW-63 验证。repeat 至少三个有记录周期命中同一星期节奏且普通咖啡不靠连续出现触发；return 必须前一周期缺席、当前与历史各至少两个日期且至少隔一个完整周期；change 必须可比同期绝对差 ≥2 且相对差 ≥50%，周/月只比相同已过日序。每个输出最多一条，排序确定；日期、次数、差值和全部 evidence ID 可复算，敏感 ID/标题不进入；证据不足、阈值未到、稳定咖啡无变化、近期同 echo ID 和旧修订都不显示。今日/周/月/分享只读生成时准备的结果，不在播放、滚动、模板切换时扫描历史；不新增章节，不改账本、筛选、额度、会员、主题或同步 | `NOT_RUN` |
| FLOW-59 | 开关联网整理，分别准备未登录生产端点、有效代理会话、离线、超时、400 内容保护、500、合法返回、未知/重复 feature、未知 evidence IDs、新数字、推断词、旧修订和连续快速编辑 20 次；切换温和/中性、关闭联网、退出/重新登录；账本含用户原句、照片引用、医疗/债务敏感记录及 1,000 条历史 | 保存/编辑记录后立即滚动首页并打开今日回放、周记、月章和分享模板；抓包核对顶层 factPacks 与服务端重建 prompt，等待后台返回后重新打开；快速编辑中让旧请求晚返回，播放/保存时断网；依次改变联网、语气和登录状态 | 正式 App 无接口/上游 Key 配置入口，固定走生产 backend；联网关闭、未登录、额度不足和 release fixture 零请求。请求只在真实账本修订稳定 1.5 秒后合并预生成日/周/月，连续编辑只接受最后请求；代理路径不接收客户端 messages，服务端仅从已校验 factPacks 重建 prompt 并固定温度。factPacks 每批最多 day/week/month 三组、每组最多 F1～F6，只含受控角色/场景、次数和日期范围，不含照片/引用、账本 UUID、用户原句、医疗债务标题；未知 feature 在模型调用前拒绝。返回保持 `{rewrites}`，必须引用 lead 且 evidence IDs 属于同 scope/period 允许集合，新数字、未知证据、重复 scope、推断/理财词、超长、旧修订一律拒绝；旧 daily/monthly 仍返回 `{summary, action, encourage}`。切换语气、开关或账号状态立即清缓存并取消旧请求，开启且已登录时按最新设置重新预生成；关闭/退出后播放只用本地文案。今日/周/月/分享只同步读缓存，播放、滚动、模板切换和保存不发请求、不等待、不显示加载态；任何失败完整保留本地结果、章节、照片、额度、会员和流畅度，有效批次只消耗一次既有远程调用计数 | `NOT_RUN` |
| FLOW-60 | 从曾保存客户端模型 Key/自定义 AI 地址与模型的旧版本覆盖安装；另准备全新安装、未登录/已登录、联网开关开/关、Debug 发布夹具、本地/staging 与生产两套服务环境 | 启动后检查 Keychain 与设置迁移；走正式 OCR、今日小记、月度整理和日/周/月轻润色；抓包检查请求；分别请求 ai-proxy `/v1/auth/dev-token`、`/v1/category/recommend` 与 backend `/v1/member/nudge/policy`，再验证会员引导 evaluate/dismiss | 旧客户端模型凭据只删除不读取，旧 JSON 多余字段被安全忽略，账本/登录/同步/语气/联网偏好不变；iOS 只访问固定生产 backend、只带登录 Bearer JWT，不发送上游 Key、可变地址或 model，未登录安静使用本地规则。记录页没有演示 OCR，正式相册识别/确认/导入和额度不变；`#if DEBUG` 的 100/1,000/5,000 条发布夹具、隔离目录、性能日志和 XCTest 仍可用且正式包不可达。生产 ai-proxy 的 dev-token、已删除的演示分类路由与 backend 的 nudge 策略读写均为 404，本地/staging 只保留 dev-token 和策略联调；生产 nudge evaluate 固定每天最多一次、关闭同场景冷却 7 天，不能切回 90 秒 debug，正常会员入口、权益和文案不变 | `NOT_RUN` |
| FLOW-61 | 五个 Tab 均准备有内容与空态；覆盖灵动岛/刘海/普通屏、默认/深色/高对比主题、默认/特大字号与 VoiceOver。清除 `pet_interaction_hint_seen_v2`，另准备已提示、宠物关闭、首页弹层阻塞、保存消息阻塞和今日 0/1/多笔数据 | 冷启动依次切换五个 Tab，比较复盘与痕迹左右边距；首页分别停留 5 秒、25 秒与 150 秒，期间打开/关闭今日列表、编辑、回放和首笔提示，切 Tab、锁屏/回前台；再保存一笔并立即触发弹层，最后拖动和长按宠物 | 系统状态栏/灵动岛安全区完整，fixed title slab 不再占用首屏，五个 Tab 内容只保留轻量页内上距且无误触；复盘主态/空态左右边距一致，接近痕迹但 iPad 内容不无限拉宽。首次约 5 秒主动出现拖动/长按提示，只有真正显示后才记已读；被弹层阻塞时关闭后重试。已提示用户约 25 秒出现一条只基于已准备今日账单和缓存天气的自然消息，后续约 150 秒且单次首页会话最多 3 条；VoiceOver 更晚、更低频。任何弹层、编辑、后台、离开首页和关闭宠物都会取消任务，保存消息阻塞后优先续接，不出现旧消息反写、全账本扫描、网络索权、滚动掉帧或任务累积；点击、拖动、吸附、长按隐藏和位置持久化保持原行为 | `NOT_RUN` |
| FLOW-62 | 准备连续周/月稳定咖啡、咖啡真实增减与隔期回归、通勤和咖啡并列、普通/明显变化的话费水电充值订阅停车费、票据照片、明确 `moment/place/object/careRecord` 生活照片、无角色旧照片、普通改标题、AI/OCR 导入标题、确有信息增量的用户原话、密集日混入低价值账单与医疗/债务敏感记录；覆盖联网关/开、未登录、合法/失败/旧修订润色、100/1,000/5,000 条、默认/深色/高对比主题、特大字号、VoiceOver 与 Reduce Motion | 在同一账本修订下依次查看痕迹本周/本月生活卡、线索首页/生活线索/深层线索、周记/月章播放和分享预览；记录各表面主线证据 ID、主视觉记录 ID 与生活线索。等待预生成润色后重新打开并连续滚动/切章 30 秒；重复发布相同润色，再新增/编辑/删除一笔触发新修订 | 同一周期的生活卡、线索和播放选择同一 `leadSignalID`，生活卡讲概览、线索讲依据、播放负责展开，不得逐字复制同一 headline/summary。用户可见文案不得把 `userEditedTitle` 或选中的一条候选写成“主动记录/用户主动写下的记录”数量；普通改标题与 AI/OCR 导入标题不能凭来源标记领衔，有信息增量的安全原话只能在本机展示。周/月都声称唯一证据时 ID 必须一致。稳定咖啡保留为生活线索而不反复领衔，真实增减/回归可进入主线；分类构成只作数据层，不能自动成为叙事主线；话费、水电、充值、订阅、停车等只作数字依据或中性变化观察。票据和无合格角色的旧照片不称为现场；合格生活照片领衔时主视觉必须是同一 evidence item，其他照片明确标为独立本周/本月画面。敏感记录零泄漏。AI 只润色已选事实，不新增数字、人物、地点、原因或情绪；进入页面、滚动、切章和模板切换不发请求、不扫完整账本，相同润色不重复失效快照，旧修订不反写。周记 3/5 章、月章 6 章及顺序/时长、额度、会员、主题、筛选和分享照片边界不变；5,000 条下无明显 hitch 或持续内存增长 | `NOT_RUN` |
| FLOW-63 | 准备以下独立夹具：本周两个不同日期的 21:00 后晚间通勤、上周缺席且三周前两个日期存在；当前只有一天、历史只有一天；普通交通、停车费和旅行；咖啡与晚间通勤当前两个日期同现、历史 4～8 个有记录周无同类组合；仅一天同现、历史曾同现、先后顺序一致/不一致；稳定咖啡、分类构成、行政账单、弱数据、敏感混组。另覆盖合法关系润色、编造首次/连续/周期/数字/证据、旧修订及 100/1,000/5,000 条 | 同一修订依次查看本周生活卡、线索依据、周记末章、分享预览和复盘查证入口；记录 relation/lead ID、当前与历史 evidence IDs。开关联网并让合法、越界和旧修订返回；连续滚动、切章、切模板 30 秒，修改一笔后重复 | 晚间通勤回归只有当前与历史都至少两个独立日期、上一周真实缺席时成立；“三周前、连续、重新出现”均可由记录复算。新组合只有当前至少两个日期、历史至少 4 个有记录周且窗口内从未出现时成立；顺序不一致只能说“一起出现”，不得说“之后”，首次必须保留“近 N 个有记录周”边界。普通交通、停车、旅行、稳定咖啡、分类构成、弱数据和行政账单不得冒充主叙事；证据不足时 `hasNarrativeLead=false`，只显示真实笔数/记录日/照片数和独立生活印记。生活卡、线索、播放、分享共享同一关系 ID；线索同时给当前日期与历史依据，复盘只读查证。AI 仅润色已认证关系/事实，编造首次、连续、周期、顺序、新数字、未知证据和旧修订全部拒绝并回退同一事实。页面进入、滚动、播放、模板切换零远程请求、零渲染期全账本扫描；5,000 条无单次 >400ms hitch 或持续内存增长。UI、周记 3/5 章、月章 6 章、额度、会员、StoreKit、照片准备、主题、宠物、筛选和存储同步均不变 | `NOT_RUN` |
| FLOW-64 | 分别装载 0/1/8 条今日记录及 100/1,000/5,000 条历史；覆盖同日连续保存/编辑/删除 20 次、跨日、会员切换、生活印记有/无结果、宠物自动气泡、通勤卡和今日列表；开启 Main Thread Hitches 与 SwiftUI 更新观察 | 每次保存后立即返回首页并连续上下拖动 20 次，不等待生活印记；计算未完成时打开今日列表、点击记录、拖动宠物，再让旧修订任务晚于新修订返回 | 首页 `ScrollView` 始终响应，不因生活印记准备出现不可拖动、遮罩或主线程停顿；一次账本修订只建立一次历史匹配上下文，今日整体、周主题、主动作副文案和逐行标签从同一快照派生，SwiftUI 绘制零完整账本签名和零逐行历史扫描。已有记录的旧标签在同日同会员时稳定承接，新记录先无标签后局部补齐；删除不残留、跨日/会员切换不穿透旧文本，快速 20 次变更只发布最新修订。相同输入的标签、主题、主动作副文案和周分类结论与改前一致；5,000 条保存后无单次 >100ms 生活印记主线程聚合、无任务持续堆积或整页跳动，首页 UI、动态主动作、账单、宠物、通勤、回放、会员、存储与同步均不变 | `NOT_RUN` |
| FLOW-65 | 分别准备：OCR 导入同一工作日晚间路线的至少两个历史日与一笔新记录，新记录改变金额并将 1 号进站口换为 2 号进站口；只有一笔普通地铁、周末单笔公共交通、用户标题“下班路上”；系统展示文案含“超市买菜和家用”或“朋友小聚聚餐”但标题/品牌/结构字段不支持的记录；真实盒马品牌和真实随礼/红包标题；另装载 100/1,000/5,000 条历史 | 完成 OCR 确认导入后依次查看记录、首页生活印记、线索页本周/本月与周记/月章；分别新增、编辑、删除相关记录并重进页面，记录通勤、买菜和人情线索的证据 ID；三档数据量重复同一操作 | 可靠历史下同一路线不因金额/进站口变化而漏掉通勤，导入记录复用现有 `scenePackId = commute`；明确“下班路上”无需历史即可命中。单次普通地铁、周末单笔公共交通不得自动升级通勤。系统展示文案不得制造买菜或人情事实，真实盒马和真实随礼/红包仍命中且证据 ID 可回到对应记录；删除事实记录后线索同步消失。100/1,000/5,000 条结论一致且页面进入、滚动不新增同步全账本扫描；不改金额、标题、日期、分类、OCR 确认 UI、线索排序/会员边界、照片、复盘、额度、存储或同步 | `NOT_RUN` |
| FLOW-66 | 准备：周期首笔为交通、主图锚点为餐饮的两笔记录；标题为“可乐喝咖啡”的普通餐饮照片；普通地铁/公交照片；“停车费”车辆照片与“停车费小票”明确票据照片；电影/展览照片；两张同日照片；一笔带旧版自动“票据/现场”元数据的记录和一笔带自定义照片说明的记录；覆盖本周/本月、特大字号、VoiceOver 及 100/1,000/5,000 条 | 打开周记/月章和线索页，核对主图右下角图标；查看“这张照片对应哪一笔”卡片；依次把旧自动角色记录的标题编辑为普通咖啡、电影/展览，再编辑自定义说明记录；切换 Tab、重启 App 后重复，并在三档账本连续滚动 | 餐饮主图始终显示餐饮图标，不受周期首笔交通影响；可乐/咖啡和普通公共交通照片只称“照片”，不得称“现场/票据”或猜测图中内容。“停车费”可称车辆记录但不得称票据，只有标题/结构明确小票、发票、收据或截图时才称票据；电影/展览等明确体验可称体验画面。线索卡直接显示同一 item ID 的照片缩略图、标题、金额和时间，同日多图不得串笔。编辑标题后旧自动角色立即清除或按新证据重判，自定义说明与明确角色不被覆盖；重启后结果一致。100/1,000/5,000 条不因绑定查找增加渲染期全账本扫描；图片顺序/封面、账单字段、OCR、章节、筛选、主题、额度、会员、StoreKit、存储同步均不变 | `NOT_RUN` |
| FLOW-67 | 同一天准备：20:43 手工备注“下班路上拍了张照片”且该笔有真实照片；1～3 笔稳定咖啡/饮品；21:00 后晚归对照；只有相同文字但无照片、OCR/AI 导入相同标题、系统默认标题、敏感/隐私文字、普通路上照片与非通勤拍照；覆盖记录卡、首页今日故事、线索页、周记/月章、分享预览、特大字号、VoiceOver 及 100/1,000/5,000 条 | 保存并附图后立即回首页，随后进入记录详情、线索、周记/月章和分享；编辑标题离开/恢复“下班路上拍了张照片”，移除/重新添加照片；让稳定咖啡先后于该笔出现并重启 App，三档账本重复 | 20:43 不需要冒充 21 点后晚归，也能基于用户原话和真实照片显示“下班路上，也把这一刻留了下来。”，记录短标签为“下班路上，留住这一刻”；首页优先该条当日新鲜表达，不被稳定咖啡聚合压过，周记/月章代表一笔、照片说明和线索标题保持同一事实。无真实照片、非手工用户原话、导入/系统标题或敏感文字不得触发；移图或改掉依据后不保留旧情绪，重新满足后恢复。不得从金额、分类或图片内容推断开心、疲惫、人物、地点、原因；既有 21 点后晚归规则与生活印记全局优先级不变。100/1,000/5,000 条页面进入/播放不新增远端请求或渲染期全账本扫描；账单字段、照片角色、item ID 绑定、章节、额度、会员、StoreKit、同步、主题均不变 | `NOT_RUN` |
| FLOW-68 | 准备最近 3/7/30 天与各自紧邻的前 3/7/30 天餐饮、交通和混合分类记录；另准备“最近七天/前七天”中文数字、不同 N、只有单段时间、无对比词、空结果与 100/1,000/5,000 条；覆盖先选“查记录”再输入对比、先选“做对比”再输入查询、快速连续执行和 VoiceOver | 点击复盘首页橙色差额整区；核对弹层任务和预填文字；关闭后先点“查记录”，输入“最近 7 天餐饮和前 7 天比呢”并执行；再从“做对比”输入普通查询；对 3/7/30 天、中文数字、空结果和快速连续请求重复 | 橙色差额整区可点击，直接进入“做对比”并预填“对比最近 7 天和前 7 天的消费”，VoiceOver 朗读动作。最近 N 天与前 N 天在 N 相同时稳定识别 compare，当前窗口和紧邻上一窗口边界、金额、笔数、分类证据可复算；中文七天等价。先前“查记录”不得污染本次对比，本次最终识别为 compare 时任务、加载态和结果始终留在“做对比”；反向输入真实查询时可按本次意图回到“查记录”；不支持文本不改写任务。快速请求只接受最新结果。100/1,000/5,000 条结论确定；不修改年份时间槽、查询/对比数据口径、账单、远端 AI、痕迹、额度、会员、主题或缓存生命周期 | `NOT_RUN` |
| FLOW-69 | 在 2026-07-23 准备 2025-07-23、2025-07-24、2025-12-31、2026-01-01、当天及未来日期的餐饮/交通记录；另准备空结果、闰年 2 月边界、100/1,000/5,000 条账本和 VoiceOver | 分别在“查记录”输入“过去一年餐饮记录”“近一年交通花了多少”“最近一年餐饮花了多少”“今年餐饮记录”“去年餐饮记录”，逐项核对标题、合计、笔数、证据记录与日期；切换一次其他任务再返回重复 | 过去一年/近一年/最近一年均使用截至今天的滚动 12 个月，不回退成默认过去 3 天；今年从当年 1 月 1 日到今天，去年覆盖完整上一自然年。起止边界按 `[start, end)` 精确包含/排除，闰年和时区不串日；空结果仍显示所问范围。切换任务不改写本次查询时间槽，100/1,000/5,000 条金额、笔数和证据一致；不修改对比状态机、查询金额口径、账单、远端 AI、痕迹、额度、会员、主题或缓存生命周期 | `NOT_RUN` |
| FLOW-70 | 分别准备无记录、有本周/本月线索及照片的账本；覆盖无账单变更、增/改/删、分类/时间筛选、周/月、会员切换、跨日、缓存文件缺失/损坏，以及 100/1,000/5,000 条；开启 Instruments、Main Thread Hitches 和性能埋点 | 首次进入痕迹和线索后，在首页/记录/复盘等 Tab 间各来回 10 次；杀进程重开且不改账本，再进入痕迹与线索；随后逐项制造真实变化、切换筛选/会员、跨日并破坏缓存后重复 | 同账本修订下 Tab 返回零重复准备、零加载遮罩，已发布周/月/线索快照由 `StatsTabState` 和确定性 key 直接复用。冷启动展示缓存在账本指纹、日期、会员和范围一致时先承接真实旧标题/笔数/金额，后台原子替换且不阻断滚动；缓存缺失、损坏或身份不匹配安全丢弃，不显示假数据。账单修订、日历周期、筛选、自定义日期、会员、解锁和叙事修订均触发真实失效，旧任务不得反写；会员降级不短暂泄露会员内容。100/1,000/5,000 条结论一致，Tab 返回不新增全账本签名或快照任务，冷启动指纹仅每个账本修订计算一次；不混入 `PERF-AUDIT-04`，不改线索/叙事算法、页面结构、额度、StoreKit、账单或同步 | `NOT_RUN` |
| FLOW-71 | 分别准备真实空账本、1 条、跨周/月的多条生活记录及 100/1,000/5,000 条账本；覆盖首次安装、已有冷缓存、账单增/改/删、跨日、快速开关会员页 10 次、杀进程重开、缓存缺失/损坏，以及免费/订阅/永久三种状态 | App 启动后立即从首页、痕迹、回放、AI 指令和“我的”打开会员方案；关闭再开并快速重复，观察长期档案区和准备任务；不改账本杀进程重开，再制造真实修订、跨日、损坏缓存后重复；核对价格、购买、登录与恢复 | 长期档案在 `ContentView` 共享生命周期后台预热，Sheet 打开/关闭不取消、不清修订；已有内存或指纹/日期匹配的冷缓存时立即显示真实旧档案并后台原子更新，页面可滚动，不出现等待遮罩。无有效快照时只显示明确首次准备态，不得先显示假 0；真实空账本计算完成后才可显示 0 条/0 天，并带当前真实修订。账单变化时旧内容承接但旧任务不反写；跨日、指纹变化、缓存缺失/损坏安全重算。100/1,000/5,000 条每个修订只准备一次，重复开关零重复扫描；价格、权益、登录、购买、恢复、StoreKit、会员状态、账单和同步完全不变 | `NOT_RUN` |
| FLOW-72 | 准备“仅本周快照、本月未准备”“仅本月快照、本周未准备”、同周期旧 key、匹配/不匹配冷启动展示及周/月均已准备五组数据；在线索页另准备自定义日期与本年两种状态；覆盖灵动岛、刘海和普通屏，默认/深色/高对比主题、特大/无障碍字号、VoiceOver 与 Reduce Motion | 在痕迹生活页连续切换本周/本月 20 次并在准备中滚动；快速切换生活/线索，在自定义日期和本年状态分别返回生活；让另一周期完成后台预热后直接切入；分别从首页周记、本月入口和复盘周/月入口深链进入，手动滚离目标后再次触发同一外部深链；在定位让帧期间改选范围；制造账本修订与跨日后重复 | 选中本周时只能显示 key 与范围均匹配的本周正文，选中本月时只能显示匹配的本月正文；目标不存在或 key 失效时绝不使用跨周期正文兜底，只显示目标周期的准备反馈。由自定义日期/本年返回生活时必须恢复此前生活周/月范围并关闭线索专属筛选，无长期骨架；单纯进入线索不得先改写当前范围。另一周期的预热身份只由请求范围与真实修订组成，切入后预热快照直接复用，不重复整理、不闪回 Loading。生活/线索与周/月控制始终固定在 Safe Area 下方，Loading、滚动和内容替换都不得把它们顶进状态栏或灵动岛；普通模式/范围切换不命令滚动，不存在透明占位滚动目标。同一外部深链重复触发时先无动画重置再让出一帧并重新校验意图，只执行一次有效定位；用户在让帧期间改选范围必须取消旧意图，不得被旧任务拉回。连续切换 20 次最终选择、标题、正文和 VoiceOver 值一致，Reduce Motion 下无额外位移动画，无旧范围反写、明显跳动或 >100ms 主线程 hitch。公共交通识别/变化阈值、线索证据文案、缓存容器、账单、会员、额度、分享与同步不变 | `NOT_RUN` |
| FLOW-73 | 准备同一周的 0/1/3 张安全照片、含收据/截图候选、缺图、长标题与账本修订变化；覆盖回放完成页预览和复盘“保存本周摘页”两个入口，以及默认/会员主题、特大字号、VoiceOver、Reduce Motion | 回放入口连续切换样式 20 次并逐次核对预览后保存；复盘入口连续保存 20 次；准备中修改账本修订、删除一张照片、加入收据候选后重复；下载相册原图核对像素与内容 | 两个入口均先经过同一 `CoverShareFlow`，预览和导出引用同一个锁定 `PreparedCoverRenderInput`；保存时不联网、不重新生成文案、不重新读取或异步加载图片。旧修订不得导出，缺图和收据/截图安全降级且不冒充 Hero。整张图只有根节点一个 Footer，品牌恰好一次，记录数/记录日/照片数只在 Footer 且以 `·` 分隔；主体没有指标卡、App 卡片或加载态。导出为 1080×1920，连续操作无重复相册写入、旧结果反写、明显主线程卡顿或持续内存增长；首发 6 模板、图片评分/取色和 AI 导演仍未在本项启用 | `NOT_RUN` |
| FLOW-74 | 为同一周期准备 0/1/2/3/4+ 张安全照片，并分别加入无 Hero、仅 secondary、收据、截图、缺图、长文和至少/不足 3 个记录日的数据；覆盖主角故事、杂志版面、生活手札、一句话、时间线、留白六模板，以及默认/深色/高对比主题、特大字号、VoiceOver 与 Reduce Motion | 在回放分享中逐套选择六模板，按各图片档位核对可选资格、Hero/辅助图主次、文案布局和降级模板；删除已准备图片、加入收据/截图并连续切换预览/保存 20 次；下载相册原图核对 1080×1920 像素、Footer 和实际图片数量 | 六模板只按硬资格出现：主角故事/杂志版面必须有事实绑定 Hero，时间线至少 3 个记录日，一句话/留白长文回退生活手札；0/1/2/3/4+ 图均不出现空占位，杂志最多 1 大 2 小，其余模板不超过各自上限，收据/截图与 secondary-only 图片绝不成为 Hero，缺图安全降级。六套在层级、排版和装饰上可辨识，Hero 与辅助图主次明确；正文事实只消费一次，主体不重复 Footer 指标。整张画布品牌只出现一次，记录数/记录日/照片数只在唯一 Footer 且以 `·` 分隔；预览与保存同源、导出固定 1080×1920，连续操作无旧结果反写、重复相册写入、明显主线程卡顿或持续内存增长；不得启用图片评分/取色、AI 导演、20 套扩展或旧渲染器退役 | `NOT_RUN` |
| FLOW-75 | 准备三张真实 12MP 照片及清晰/轻微模糊/严重模糊、正常/严重过曝/欠曝、短边 900px、主体居中/贴边、单人脸/多人脸贴边、横竖构图、近似重复、收据/支付截图和无图夹具；覆盖默认/深色/高对比主题、VoiceOver、Reduce Motion、低电量与离线 | 从播放倒数第一章观察预热并立即打开分享；逐张核对 Vision 裁切焦点和 Core Image 动态色板，连续换模板 20 次、关闭重开 10 次、连续保存 20 次；准备中切后台、关闭 Sheet、增删照片并推进账本 revision，让旧任务晚于新任务返回；用 Instruments 记录主线程 hitch、峰值内存和分析次数 | 图片读取、降采样、Vision/Core Image 分析均在后台且最多并发 2 张，同一稳定图片身份和规则版本只分析一次；播放最后一章提前预热，未完成时只在操作层显示非阻断准备状态，封面无加载态。严重模糊、严重曝光、短边不足、收据/截图和隐私拒绝媒体不得成为 Hero；合格 Hero 先满足故事证据再按质量稳定排序，辅助图保持不同方向/焦点的互补主次。人脸只提供裁切矩形，预览与 1080×1920 导出均不切掉受保护区域，且不识别身份、不推断年龄、性别或情绪。动态色板只来自锁定 Hero，正文对比度至少 4.5:1、次要文字至少 3:1；无图/失败/自定义背景回退受控色板。换模板和保存零重新分析、零图片 IO、零网络，预览/导出消费同一 `PreparedCoverRenderInput`；旧 context/revision 不反写，取消后不自己弹回，连续操作无重复相册写入、明显主线程卡顿或持续内存增长；AI 导演、20 套扩展和旧渲染器退役仍未启用 | `NOT_RUN` |
| FLOW-76 | 开启/关闭“联网整理”，分别准备未登录、有效登录、额度充足/耗尽、离线、代理超时、400 Schema 拒绝、500、合法响应、未知模板/色板/背景/媒体别名、越权 Hero、额外字段和旧 revision；准备 0/1/2/3/4+ 张安全/低质/收据图片并开启抓包、VoiceOver、Reduce Motion 与低电量 | 播放到最后一章前后观察 AI 导演预热；预热中立即打开分享层、手动选模板、关闭重开、切后台并修改账本；连续快速开关分享 20 次、切模板 20 次、连续保存 20 次。抓包核对请求体，故意让旧请求晚返回并分别制造超时、断网和非法 JSON；下载相册原图比对预览 | AI 导演只在最后一章且本机图片分析完成后后台发起，打开分享层、切模板、SwiftUI 渲染和保存均零新请求；同一 request key 合并且缓存最多 24 个轻量 Decision，请求 8 秒硬超时并可取消。抓包中只能出现长度/数量/方向/质量档位、M1～M7 匿名别名和最多 5 个合法模板/色板/背景/动画 token；不得出现照片/字节/引用、脸框/色样、账本或 evidence UUID、用户原文、故事正文、金额、商户、地点、自由 messages 或客户端 model。合法结果仍经过 source revision、fingerprint、候选、Hero 事实绑定、隐私、Footer、重复、对比度和布局校验；未知/额外/越权/旧结果、未登录、关闭联网、额度不足、离线、超时及服务失败全部静默保留完整本地 Recipe。分享层打开前可采用已缓存结果；打开后 Session 锁定不跳版，手动样式始终优先，保存与预览同源且不等待 AI。连续操作无旧结果反写、重复相册写入、明显 hitch 或持续内存增长；不得加入多样性冷却、其余 14 套模板、完整动效或旧渲染器退役 | `NOT_RUN` |
| FLOW-77 | 准备 20 套封面均可命中的独立数据，覆盖 0/1/2/3/4/7 张安全图片、纵向/横向/方图、长文、低质、收据/截图、无事实绑定 Hero、`memoryWall` 4～6 图、`bookCover` 仅纵图及 `quietEditorial` 装饰图；覆盖回放与复盘两个旧分享入口、窄屏/刘海屏、默认及无障碍 Dynamic Type、VoiceOver、Reduce Motion、离线与低电量 | 在同一已准备 revision 下逐一手动选择 20 套并来回快速切换 20 次，展开/收起选择器、横向滑动且中途切换自动/手动/自定义背景；让旧 AI 请求迟到并增删图片推进 revision；连续保存 20 次，下载原图逐张比对预览，并用 Instruments 核对图片读取、分析、网络、主线程 hitch 与内存 | 20 套只按硬资格出现且名称、顺序、选中状态和 VoiceOver 值准确；窄屏与 Dynamic Type 下标题/说明不截断、操作自动换行，Reduce Motion 下无非必要位移。0/1/2/3/4/7 图均无空占位或重复 frame，4 张以上不用等大宫格，Hero 面积严格大于每张辅助图；`bookCover` 横图/方图不得成为 Hero，`quietEditorial` 只用 decoration。所有 body/media frame 均在 540×960 内且不进入 `y >= 872` Footer 保留区；20 套布局签名可区分。全画布只有根节点一个 Footer，品牌恰好一次，记录数/记录日/照片数只在 Footer 且以 `·` 分隔。切换与保存零图片读取、零分析、零 AI，预览/导出使用同一锁定 `PreparedCoverRenderInput`，旧 context/revision 和迟到 AI 不反写，手动选择不被覆盖，连续保存无重复写入、明显 hitch 或持续内存增长，输出固定 1080×1920；两个旧入口继续可用且旧分享渲染器不在本项删除 | `NOT_RUN` |
| FLOW-78 | 周记与月章分别准备有/无照片、AI 导演开/关、播放中/用户手动暂停/播放完成三种状态；打开 Instruments 的 Main Thread Hitches、Allocations、Memory Graph，并准备可读取的 crash/Jetsam 日志 | 播放中切后台 5 秒后返回 20 次；手动暂停后往返 5 次；停留播放完成页往返 20 次，其中分别在图片解码、Vision/Core Image 分析和 AI 导演请求进行中切后台；再打开分享预览并保存一次 | inactive/background 时播放计时和 8fps 动态背景立即暂停，图片解码/分析、封面 Session、保存任务与 AI 导演取消且已准备大图可释放；后台无迟到结果反写。active 后仅原本播放中的会话续播，手动暂停不自动播放，完成页不重播；封面只按当前 context 幂等准备一轮，预览和保存仍同源且照片/模板/Footer 不变。前后台往返 20 次无崩溃、Jetsam、持续内存爬升或明显叠加任务，单次主线程 hitch 不超过既有门槛；若仍崩溃必须附 crash/Jetsam 堆栈再定向定位，不把本项策略门禁冒充根因完全关闭 | `NOT_RUN` |
| FLOW-79 | 准备支付成功截图：`LAWSON + ¥4.20` 且无日期；另备金额行无货币符号但含“支付金额 4.20”、`4月20日`、`2026-04-21 08:35`、日期标签下一行 `4.22`、金额与明确日期并存六组样本；设备日期固定在可核对当天 | 逐张走正式相册 OCR、确认页和导入，核对金额、商户、日期、分类；取消后重试并快速重复选择，导入后进入记录编辑核对持久化结果 | 无明确日期的 `¥4.20` 和“支付金额 4.20”都使用导入当天，不得变成 4 月 20 日；金额仍为 4.20，LAWSON/罗森品牌和既有分类不变。`4月20日`、完整年月日及日期/时间标签附近的裸月日继续按证据解析，金额行本身不提供日期；明确日期优先于当天回退。取消零写入、一次提交、重试不复用旧截图结果；不改变 OCR 入口、额度、确认 UI、首页动线或账本同步 | `NOT_RUN` |
| FLOW-80 | 本周和本月各准备 `00:08 + 加班打车 + 交通 + ¥50.90 + 真实照片`；另备同周期更高价值的明确用户原话/合格生活照片主线，以及 `00:08 + 普通地铁/公交`、白天打车、无工作词夜间交通对照 | 先看首页对应事实，再播放周记/月章，逐章核对标题、正文、辅助标签、金额和照片；分别移除“加班/下班”词、改到白天、移除/恢复照片并重建同周期播放，重复打开 10 次 | 强下班证据在无更高主线时成为对应“这一笔”或月初/后来章节，使用“凌晨零点多还在下班路上…”专用正文，金额仍为 50.90，照片锚点绑定同一 item ID；同一事实不在正文和辅助情绪重复。已有更高价值主线时不抢 Lead，只在开场辅助层出现一次且仍使用专用文案。普通夜间地铁/公交、白天打车和无工作词交通不得获得强下班兜底；周/月章节数、顺序、时长、首页动线、额度、会员和分享规则不变，重复打开结果稳定 | `NOT_RUN` |
| FLOW-81 | 准备同一天和跨天的至少 8 笔同名“罗森”记录，均只有商户名、不同金额/时间；另准备旧版本已存“一口热食很及时/饭团和饮料在手边”的罗森记录，以及标题明确含关东煮、便当、饭团、咖啡的四笔对照；覆盖保存后首页、记录详情、编辑预览、杀进程重启、免费/会员与 VoiceOver | 连续保存 8 笔品牌名-only 记录，逐笔查看标签并杀进程重开两次；打开两笔旧记录；再新增和编辑四笔明确食品记录，分别删除/恢复食品词；快速进入首页和详情 20 次并核对同一 item ID 的标签 | 只有商户名时只显示“罗森这笔记下/路过罗森记一笔/罗森这一单记下/在罗森留下一笔/便利店这一笔”等中性事实，零“热食/热乎/一口热的”，也不得凭品牌制造饭团、便当、关东煮、咖啡、饮料或小食；多笔记录不再永久固定同一句，同一 item ID 在页面切换和重启后保持确定。旧错误标签按当前标题证据安全纠正。明确食品词存在时标签保留对应食品名但不强加温度，删除证据后立即回到中性商户事实，恢复后可恢复具体标签；免费/会员结果一致。不得修改商户匹配、分类、金额、OCR、首页主动作、播放次数/会员规则、生活印记排序、章节、分享、存储或同步 | `NOT_RUN` |

## 9. 无障碍与权限矩阵

| ID | 操作 | 通过标准 | 状态 |
|---|---|---|---|
| A11Y-01 | 默认、特大和至少一个无障碍字号走五个 Tab、OCR、会员、回放 | 不截断关键文案；按钮可触达；需要时自动纵排 | `NOT_RUN` |
| A11Y-02 | VoiceOver 走五个 Tab、AI 指令、图表、OCR、会员、回放完成页 | 名称/值/状态/提示完整；选中状态明确；图表有摘要 | `NOT_RUN` |
| A11Y-03 | 开启 Reduce Motion 后重复 AI、痕迹、记录/OCR、会员、回放 | 非必要位移/缩放/弹簧停用；功能反馈仍存在 | `NOT_RUN` |
| PERM-01 | 首次位置请求选择不允许，并在系统设置保持拒绝 | 记账和回放可用；天气上下文安全降级；无循环索权 | `NOT_RUN` |
| PERM-02 | 相册选择取消、有限照片、拒绝访问 | 不新增空图；队列释放；账本和已有图片不受损 | `NOT_RUN` |
| PERM-03 | 保存分享图时拒绝“添加照片”权限 | 明确失败提示；页面可继续操作；无假成功 | `NOT_RUN` |
| NET-01 | 离线执行 AI 指令台、今日小记、月度整理、同步 | 本机指令仍可用；远程功能明确回退/失败；本地数据保留 | `NOT_RUN` |

## 10. 发版签收记录

| 门禁 | 结果 | 证据位置/日志 | 签收人 | 日期 |
|---|---|---|---|---|
| Windows repository gate | `PASS` | `python scripts/validate_release_gate.py --phase windows`；夹具、差异、语义、交互、文案、迁移和 SQLite schema 全通过；仅有既有 7 条文案软提示 | Codex | 2026-07-16 |
| Xcode Debug build | `BLOCKED` | Windows 环境无 `xcodebuild`/Swift 工具链 | Codex | 2026-07-16 |
| Xcode Release build | `BLOCKED` | Windows 环境无 `xcodebuild`/Swift 工具链 | Codex | 2026-07-16 |
| 全部 XCTest | `BLOCKED` | 测试已接线；当前环境无法运行 iOS XCTest | Codex | 2026-07-16 |
| 100 条真机 | `BLOCKED` | 无 iPhone/Xcode 设备部署环境 | Codex | 2026-07-16 |
| 1,000 条真机 | `BLOCKED` | 无 iPhone/Xcode 设备部署环境 | Codex | 2026-07-16 |
| 5,000 条真机 | `BLOCKED` | 无 iPhone/Xcode 设备部署环境 | Codex | 2026-07-16 |
| 真实照片 REAL-01～06 | `BLOCKED` | 真实照片资产与夹具通过；无 iPhone/Instruments | Codex | 2026-07-16 |
| PERF-05/06 / LOGIC-01～11 / AI-02 FLOW-01～19 | `BLOCKED` | 代码策略、主题目录、识别与对比层级边界、静态护栏和 Windows 门禁通过；无 Xcode/iPhone 完成交互、掉帧、主题、识别、对比切换与手势签收 | Codex | 2026-07-16 |
| FIX-001/002 | `BLOCKED` | 无 iPhone，详细场景保持 `NOT_RUN` | Codex | 2026-07-16 |
| R-01～R-12 | `BLOCKED` | 无 iPhone；StoreKit/同步场景还需要沙盒账号与专用环境 | Codex | 2026-07-16 |
| DATA-06 BACKUP-01～05 | `BLOCKED` | 无 iOS 文件 App、iPhone 与 1,000 条恢复性能环境 | Codex | 2026-07-16 |
| MEMBER-02 MEMBER-LOGIN-01～05 | `BLOCKED` | 无短信测试账号、iPhone 与 StoreKit 沙盒 | Codex | 2026-07-16 |
| 无障碍/权限 | `BLOCKED` | 无 iPhone VoiceOver、Dynamic Type、Reduce Motion 与权限环境 | Codex | 2026-07-16 |
| StoreKit/同步 | `BLOCKED` | 无 StoreKit 沙盒、短信/同步测试账号与第二设备 | Codex | 2026-07-16 |

最终结论只能填写：

- `PASS`：所有必测项通过，且 device-audit 与自动门禁一致。
- `FAIL`：存在可复现缺陷，记录所属任务和定向修复范围。
- `BLOCKED`：缺设备、账号、Xcode 或外部服务；不得写成已验证。
