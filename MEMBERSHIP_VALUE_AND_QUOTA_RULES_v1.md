# 叙账会员价值与免费额度规则 v1

> 日期：2026-07-15
> 状态：展示层方案已确定；额度常量与历史计数保持冻结
> 前置要求：任何共享额度池、新常量或历史计数迁移，必须获得明确产品确认后单独实施。

## 1. 用户只需要理解两件事

| 核心价值 | 免费版 | 会员 |
|---|---|---|
| 省力记 | 手动记账、基础统计、本地保存始终免费；OCR 有每日体验次数 | OCR 连续导入、批量补记、完整生活场景，不被次数打断 |
| 长期回望 | 今日回放、周记、月章、生活线索和月度整理在各入口提供体验次数 | 上述回望能力持续可用，并适合长期整理多年记录 |

规则：会员页只讲这两层价值；精确剩余次数只在用户即将使用对应能力时显示，不在会员页同时堆六套计数。

## 2. 当前生产常量（本任务不修改）

| 能力 | 当前免费规则 | 存储/刷新边界 | 代码来源 |
|---|---:|---|---|
| 今日回放 | 3 次/日 | 本地日键，次日刷新 | `DailyFeatureQuotaStore.todayPlaybackFreeLimit = 3` |
| OCR 导入 | 3 次/日 | 本地日键，次日刷新 | `DailyFeatureQuotaStore.ocrDailyFreeLimit = 3` |
| 周记 | 3 次/自然周 | 周键刷新；登录后同步本地使用量 | `SummaryPlaybackQuotaStore.weeklyFreeLimit = 3` |
| 月章 | 10 次长期体验 | 非每月刷新；登录后同步本地使用量 | `SummaryPlaybackQuotaStore.lifetimeMonthFreeLimit = 10` |
| 生活线索 | 5 次/月 | 月键刷新 | `LifeInsightService.freeMonthlyLimit = 5` |
| 月度整理 | 5 次体验 | 当前 `monthly_trial_used_v1` 累计，不自动月刷新 | `InsightWebView.trialTotal = 5` |

远程 AI 每月调用上限是开发者/用户设置边界，不等同于免费会员额度，不在会员页作为权益计数展示。

## 3. 本轮立即实施：展示层简化

1. 会员页始终展示“省力记”“长期回望”两项主价值，不再默认展开七条权益。
2. 免费与会员对比只保留两行；手动记账、基础统计和本地保存明确始终免费。
3. 设置账号页用同一两层模型，不重复罗列周记、月章、今日回放、OCR、AI 等多张权益卡。
4. 今日回放、OCR、周记、月章、生活线索、月度整理继续在各自使用入口显示准确剩余次数和刷新方式。
5. 不改变购买方案、Product ID、价格、恢复购买、会员判定和历史计数。

## 4. 后续候选：共享“长期回望体验”额度池（未实施）

推荐候选仅用于下一次产品确认：

- 今日回放与 OCR 仍保留每日规则，因为它们分别对应当天回看与输入效率。
- 周记、月章、生活线索、月度整理可考虑合并为一个按月刷新的“长期回望体验”池。
- 会员继续不限；手动记账与基础统计继续免费。

该候选会改变现有常量和用户预期，本轮不得落地。

## 5. 若未来获批，迁移必须满足

1. 新字段与旧字段并存至少一个版本；旧计数只读保留，不立即删除。
2. 首次迁移不得减少用户当前可用体验：新池初始剩余数取“新规则应有值”和“旧规则可用权益折算值”中的较大值，并设置上限防止异常膨胀。
3. 已购买会员不迁移免费次数，只迁移展示版本；恢复购买结果不受影响。
4. 未登录用户以本机迁移；登录用户先合并本机/账号使用量，再生成新池，避免换机重复获得或丢失。
5. 写入 `quota_model_version` 与迁移审计摘要；迁移失败继续读旧规则，不把次数清零。
6. 回滚时切回旧规则读取，保留新池数据但停止扣减，确保可再次迁移。
7. 必须新增旧用户 0 次、部分剩余、全部未用、跨月、登录合并和会员恢复测试。

## 6. 验收边界

- 免费用户不会误以为手动记账需要会员。
- 用户在会员页能用一句话复述：会员主要是“省力记 + 长期回望”。
- 精确次数与代码常量一致；本轮不存在任何历史额度迁移。
- 所有购买、取消、失败、恢复和账号切换路径保持原结论。
