你在 xuzhangapp 实现 Fix A6 — 记账页「写点细节」键盘不挡输入（仅 RecordView.swift）。

================================================================================
现象
================================================================================

noteSection 在 recordDetailsFold 底部；聚焦时键盘盖住 TextField，ScrollView 未随焦点滚动。

================================================================================
任务
================================================================================

1. manualForm 外包 ScrollViewReader，note 区加 .id("recordNoteField")

2. onChange(focusedField)：当 newValue == .note 时 withAnimation scrollTo("recordNoteField", anchor: .center)

3. 不改 life-slip 结构、不改折叠逻辑

================================================================================
验收
================================================================================

[ ] 展开「写点细节」点输入框，字段滚到键盘上方可见
[ ] 金额键盘 dock 与 OCR 路径零回归

@NativeDemoApp/Views/RecordView.swift
