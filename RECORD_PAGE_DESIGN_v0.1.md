# 叙账 · 记账页设计原则 v0.1

> 更新时间：2026-06-08  
> 状态：定稿（战略层；现行实现见 `NativeDemoApp/Views/RecordView.swift`）
> 读者：产品、设计、开发

---

## 0. 北极星句

**叙账的记账页应该像「把一段生活放进账本」，不是「填写金额表单」。**

---

## 1. 这句话在说什么

| 填写金额表单 | 把一段生活放进账本 |
|--------------|-------------------|
| 字段标签：金额、分类、备注 | **一段即将被记住的生活** |
| 主视觉是 ¥ 和输入框 | 主视觉是 **叙事预览**（备注/情绪/品牌句） |
| 用户完成「提交数据」 | 用户完成 **「确认：今天是这样过的」** |
| 保存 = 入库 | 保存 = **送进可叙之材** |
| 表单越全越专业 | **越省越像叙账**（F1.3/B2.13 预填托举 UI） |

与 [`RECORDING_CHAIN_VISION_v0.1.md`](RECORDING_CHAIN_VISION_v0.1.md) 一致：**缩短记账链路、叙事自动长出来**——UI 必须把预填结果 **展示成生活句**，而不是藏在表单字段里。

---

## 2. 与 playback / 切片的关系

```text
记账页（放进账本）  →  列表/今日  →  回放/切片（讲出来）
     输入层 · 叙式            素材            叙 · 已 polish
```

记账页不必做成播放 Sheet，但应 **同一套叙事语法**：

- headline / subtitle 层级（非 KPI 标签）
- 情绪胶囊是 **主角**，不是列表 debug 信息
- 金额 **重要但退后**——是生活的刻度，不是审计主角
- 暖色、呼吸、极淡 rhythm（参考 D1.1 `WeeklyShareCardView`）

---

## 3. 目标体验（手动记账）

### 3.1 默认态（无金额）

- 安静、留白；一句轻引导，如「记下一笔今天的生活」
- 金额区是 **舞台中心**，无「金额」字段标签
- 无分类 chip 海、无备注框——**还没开始叙**

### 3.2 输入金额后（预填渐显）

预填来源不同，**预览卡与一键备注的关系也不同**（见 §3.5 三态）。

- **生活预览卡**（核心新增）：
  - **主行**：`inputTitle`（备注/生活句）优先；空则 brand displayName；再空则 category soft fallback
  - **胶囊**：`emotionTag` 预览（Resolver；与保存同源）
  - **次行**：分类 · 时间（小字）
  - 金额在 preview 卡 corner，字号 **小于** 叙事主行
- 分类：默认 **只显示当前选中 +「改」**；展开才见 chip 网格
- 备注 TextField：**默认折叠**；点预览卡或「补充细节」才展开
- 主按钮：**「放进账本」** / **「记下这一笔」**

**主行 ≠ 胶囊**：一键备注写 **主行生活句**（`inputTitle`）；胶囊是 **情绪标签**。瑞幸可胶囊用品牌句，主行仍可用场景包换说法。

### 3.3 会员场景包 · 一键备注

**一键备注不会废掉**，按三态调整 prominence（§3.5）：

- **A 品牌明确**：预览可自动齐；一键备注 **次要**——「换一句说法」，点完刷新预览主行
- **B 习惯命中**：预览有底稿；一键备注 **并列**——「换一句」
- **C 冷启动/低置信**：预览主行空；**「✨ 帮我写一句」贴在预览卡上** = 原一键备注，最显眼

展开更多场景包：二级「换一种说法」；每次选包 → **预览主行即时更新**。禁止删除或藏死 `ScenePackSectionView`。

### 3.4 OCR 模式

- 语义：**「从账单里捞生活片段」**，不是「上传票据填表」
- 本 PR 可 **轻改 copy + 间距**；不大改 `OCRConfirmSheet` 结构（另 task）

### 3.5 三态分工（必守）

```text
品牌（F1.3）  →  认得出是谁：主行+胶囊可自动齐；一键备注=润色/换角度
习惯（B2.13） →  认得出模式：预览有底稿；一键备注=换说法
都没有        →  预览是壳；一键备注=叙事发动机（主角）
```

瑞幸类：可几乎直接「放进账本」；会员仍可用场景包换 **不同于品牌池** 的主行备注。  
地铁 4 元、无品牌、新用户：**一键备注不能收进折叠深处**。

---

## 4. 禁止（记账页）

- 首屏堆满：金额标签 + 分类 grid + 备注框 + 保存 + 场景包
- 主按钮「保存」+ 巨大渐变（表单提交感）
- 用「必填项」「请填写」类审计语气
- 为好看删掉预填/情绪/品牌能力
- 改动 `PlaybackService` / `SummaryPlaybackSheet` / `WeeklyShareCardView` 播放与导出逻辑

---

## 5. 验收问句

改完任一区块，问：

> **这是在帮用户把一段生活放进账本，还是在让他填一张表？**

---

## 6. 相关文档

| 文档 | 用途 |
|------|------|
| [`RecordView.swift`](NativeDemoApp/Views/RecordView.swift) | iOS 现行实现 |
| [`RECORDING_CHAIN_VISION_v0.1.md`](RECORDING_CHAIN_VISION_v0.1.md) | 双引擎预填 |
| [`PRODUCT_NORTH_STAR.md`](PRODUCT_NORTH_STAR.md) | 先叙后议 |

---

## 修订记录

| 日期 | 说明 |
|------|------|
| 2026-06-08 | 首版：记账页北极星句 + 目标体验 |
| 2026-06-08 | §3.5 三态：品牌/习惯/冷启动下一键备注 prominence；主行≠胶囊 |
