# 痕迹页状态、证据与性能恢复方案 v1

> 日期：2026-07-24
> 范围：痕迹 Tab 的生活页、线索页及其周/月/自定义周期切换
> 当前阶段：`TRACE-RECOVERY-01` 已达到 `CODE_DONE`；`TRACE-RECOVERY-02/03` 仍为 `NOT_STARTED`

## 1. 问题归纳

### 1.1 本周 / 本月切换看起来没有反应

- 顶部选中状态已经切换，但正文可能继续显示另一个周期的旧内容。
- 本月快照尚未准备时，当前实现会拿本周快照兜底；用户无法分辨切换未生效，还是数据相同。
- 这是正确性问题，不只是动画或点击反馈问题。

### 1.2 线索页和痕迹页反复 Loading，切换明显卡顿

- 周、月、分类或自定义范围变化时，当前可见快照会先被清空，再异步查询缓存或重建。
- 即使目标缓存已经存在，也会经历异步让步，至少闪一次整页 Loading。
- 从其他 Tab 返回、账单没有变化时，也可能再次出现加载感。

### 1.3 Loading 时顶部“生活 / 线索”被顶进状态栏或灵动岛

- 模式切换时，目标内容还没发布就先修改了滚动锚点。
- ScrollView 用透明大占位参与布局，导致顶部选择器随错误锚点向上滚动。
- 半透明全屏 Loading 又把错位控件透出来，形成“卡住并顶到最上面”的视觉结果。

### 1.4 “公共交通的变化”无法理解和核对

- 算法实际比较的是当前周期和上一可比周期，并且已经计算出当前数量、基线数量、差值和两组证据 ID。
- 展示层只保留“当前页有 3 笔记录”，丢掉“本周 3 笔、上周 1 笔、增加 2 笔”等关键事实。
- 页面下方图表统计整页 14 笔，而不是这条公共交通变化的证据，餐饮等无关记录也会混入视觉解释。
- “通勤出行 4 次”与“公共交通 3 笔”来自不同聚合口径，界面没有说明，容易被理解为统计互相矛盾。

## 2. 是否由同一个原因造成

不是单一代码错误，但可以归为两个共同根因。

### 根因 A：筛选状态、展示快照、加载状态和滚动状态没有共用同一个目标 Key

它同时造成：

- 周/月选中状态与正文周期不一致；
- 切换时先清空、后取缓存，缓存命中也闪 Loading；
- 内容未就绪就滚动到目标锚点，顶部控件越过 Safe Area；
- 返回 Tab 或无数据变化时出现不必要的重新准备感。

本质是：界面知道“用户选了什么”，却没有严格约束“当前正文属于什么”。

### 根因 B：变化计算结果在投影到 UI 时丢失了证据上下文

它造成：

- 有“变化”结论，但没有当前值、基线值和差值；
- 标题问“来自哪些记录”，界面却没有逐笔列出记录；
- 图表使用整页数据，而不是本条结论的证据数据；
- 单场景周期变化被套用“两条生活线”等不准确的展示标签。

两类根因在“屏幕展示的内容必须与当前状态和事实一致”这一原则上相通，但实施时必须分开修，避免性能生命周期修改和统计语义修改互相污染。

## 3. 目标架构

### 3.1 单一快照 Key

生活页和线索页的可见内容都必须由完整 Key 唯一确定：

```text
TraceSnapshotKey
├── ledgerRevision       账本修订号
├── viewMode             生活 / 线索
├── rangeKind            周 / 月 / 年 / 自定义
├── rangeStart           自定义开始日期
├── rangeEnd             自定义结束日期
├── categoryFilter       分类筛选
├── entitlementRevision  会员或解锁资格版本
└── contentRuleVersion   内容规则版本
```

界面只允许展示 `publishedSnapshot.key == selectedKey` 的内容。不同周期之间禁止互相兜底。

### 3.2 每个 Key 独立的准备状态

```text
absent → preparing → ready
             ↓
           failed
```

- `ready`：同步发布缓存，不出现 Loading。
- `absent`：只在目标内容区显示局部准备态，不继续显示另一周期正文。
- `preparing`：同 Key 请求合并，不重复构建。
- `failed`：保留顶部筛选能力，内容区给出重试；不得整页锁死。
- 账本修订变化时，仅使旧修订 Key 失效；快速连续变化采用 latest-wins，旧任务不得反写。

### 3.3 固定顶部控制区

- “生活 / 线索”和周/月范围控制固定在 Safe Area 下方，不作为目标正文的滚动锚点。
- 普通模式或周期切换保持当前合理滚动位置，不主动跳到页面顶部或目标卡片。
- 只有外部深链明确要求定位某张卡片时，才记录一次性滚动意图；必须等目标快照真实发布、锚点真实存在后执行。
- 删除为等待内容而设置的透明大占位，不让不可见内容影响滚动几何。

### 3.4 变化证据契约

每条周期变化必须携带完整、不可歧义的展示事实：

```text
TraceChangeEvidence
├── sceneID / sceneTitle
├── currentRangeLabel
├── baselineRangeLabel
├── currentCount
├── baselineCount
├── absoluteDelta
├── relativeDelta
├── direction
├── currentEvidence[]
└── baselineEvidence[]
```

每条证据至少包含：精确记录 ID、日期、标题、金额和分类。UI 不得根据日期或标题再次猜测记录归属。

## 4. 页面呈现方案

### 4.1 周 / 月切换

- 目标缓存存在：点击后选中状态与正文同步切换，无整页 Loading、无跳动。
- 目标缓存不存在：选中状态立即切换，正文区域显示该周期的轻量准备态。
- 准备态必须明确写当前目标，例如“正在整理本月痕迹”，不能继续展示本周正文。
- 顶部控件保持可见、可操作，并始终位于 Safe Area 下方。

### 4.2 公共交通变化卡

建议首屏直接显示：

```text
本周变化 · 公共交通

本周 3 笔    上周同期 1 笔
比上周增加 2 笔

本周记录
07/21  地铁出行  ¥2.00
07/22  地铁出行  ¥2.00
07/24  地铁出行  ¥3.00

上周同期记录
07/15  地铁出行  ¥2.00
```

- 单场景变化不再显示“两条生活线”。
- “来自哪些记录”必须当场给出两组记录，不让用户再猜。
- 若保留图表，只允许使用这条变化的当前与基线证据；优先采用简单的双柱对比。
- 若准确图表暂时无法实现，先移除图表，保留可核对的数字和逐笔证据。
- 当“通勤出行”与“公共交通”同时出现时，分别标注“生活线索聚合”和“本条变化证据”，并解释口径不同。

## 5. 分阶段实施

### TRACE-RECOVERY-01：周期真值、滚动与 Safe Area

状态：`NOT_STARTED` → `IN_PROGRESS` → `CODE_DONE`；Windows 门禁已通过，缺 Xcode/iPhone `FLOW-72`，不标记 `VERIFIED`

范围：

- 禁止生活页周/月跨周期快照兜底；
- `selectedKey` 与 `publishedSnapshot.key` 强一致；
- 普通切换不再修改滚动锚点；
- 外部深链改为内容发布后的单次滚动意图；
- 顶部控制区固定在 Safe Area 下方；
- 删除透明大占位对滚动几何的影响。

完成标准：周期选择、正文标题和正文事实始终属于同一 Key，任何加载阶段顶部控件都不进入状态栏。

### TRACE-RECOVERY-02：缓存优先与非阻断加载

状态：`NOT_STARTED`

范围：

- 用按完整 Key 保存的可见快照替代单个 `preparedClueSnapshot`；
- 切换时先同步查找内存快照和持久快照；
- 缓存命中不创建无意义的让步任务，不先清空内容；
- 只有真正未命中才后台构建，同 Key 请求合并；
- Loading 改为内容区局部准备态，不全屏禁用滚动；
- 账单、筛选和资格均无变化时，从其他 Tab 返回不得重建。

完成标准：缓存命中时零整页 Loading、零重复 `buildClue`、零滚动跳动。

### TRACE-RECOVERY-03：变化事实与证据展示

状态：`NOT_STARTED`

范围：

- 把当前值、基线值、差值、方向和两组证据 ID 从计算层完整传到快照与 UI；
- 当前与基线证据按精确 ID 投影为日期、标题、金额、分类；
- 单场景变化使用“本周变化”等准确主题；
- 图表只消费本条证据，禁止使用整页记录；
- 明确“通勤出行”与“公共交通”的统计口径。

完成标准：用户无需离开卡片就能回答“与谁比、差多少、由哪几笔记录得出”。

## 6. 固定执行顺序

```text
TRACE-RECOVERY-01
        ↓
TRACE-RECOVERY-02
        ↓
TRACE-RECOVERY-03
```

先恢复周期和界面的真实性，再优化生命周期性能，最后升级变化证据。任何时刻只允许一个任务为 `IN_PROGRESS`。

## 7. 验收矩阵

| 场景 | 验收标准 |
| --- | --- |
| 周/月连续切换 20 次 | 选中状态、标题、正文周期始终一致 |
| 目标周期无快照 | 不显示另一周期正文，只显示目标周期局部准备态 |
| 缓存命中 | 零整页 Loading、零重建、零滚动跳动 |
| 从其他 Tab 返回 | 无账单/筛选/资格变化时不重新加载 |
| 冷启动进入痕迹 | 顶部控件不进入状态栏；准备态只覆盖内容区 |
| 快速生活/线索切换 | 旧任务不得反写，最终内容与最后一次选择一致 |
| 自定义日期范围 | Key 包含精确起止日期，筛选真实生效 |
| 公共交通变化 | 同时显示当前数、基线数、差值和两组逐笔证据 |
| 变化图表 | 不混入餐饮等非本条结论记录 |
| 空证据或证据不足 | 主动弃权，不生成无法核对的变化卡 |
| 100 / 1,000 / 5,000 条 | 缓存命中不重建；主线程无大于 100ms hitch |
| VoiceOver / Dynamic Type | 控件顺序正确，放大字体不遮挡且可读 |
| Reduce Motion | 无依赖位移动画的状态表达，内容切换仍清楚 |

真机签收必须覆盖：冷启动、Tab 返回、跨日、账单新增/编辑/删除、会员变化、快速切换、自定义范围和网络不可用。

## 8. 冻结边界

本方案不修改：

- 公共交通或通勤的识别规则；
- 周期变化阈值（当前差值至少 2、相对变化至少 50%）；
- 金额、日期、标题、分类或账单存储字段；
- OCR、照片角色、叙事生成、分享系统、宠物、同步；
- 会员权益、额度、StoreKit 或 AI 指令；
- `PERF-AUDIT-04`、`ARCH-03` 或其他相邻路线任务。

不得借本项顺手重构整个 `StatsWebView`。每个阶段只修改其明确的状态、缓存或证据边界，并单独补测试、门禁和真机用例。

## 9. 预计修改范围

- `NativeDemoApp/Views/StatsTraceModels.swift`
- `NativeDemoApp/Views/StatsTraceSnapshotStore.swift`
- `NativeDemoApp/Views/StatsWebView.swift`
- `NativeDemoApp/Services/LifeNarrativeEchoService.swift`（仅在第三阶段补充既有事实输出契约；不改判断算法）
- `NativeDemoAppTests/StateRegressionTests.swift`
- `scripts/experience_static_check.ps1`
- `RELEASE_GATE_AND_DEVICE_MATRIX_v1.md`
- `GLOBAL_PRODUCT_INTERACTION_OPTIMIZATION_LEDGER_2026-07-15.md`

## 10. 当前结论

三个交互与卡顿现象主要由同一个快照生命周期错误造成。第一阶段已经完成“目标周期不兜底、顶部控制区固定、滚动意图延后”的代码与 Windows 门禁；按完整 Key 的缓存复用仍属于第二阶段。公共交通卡片不是算法无依据，而是展示投影丢失了基线和证据，仍按独立第三阶段修复。
