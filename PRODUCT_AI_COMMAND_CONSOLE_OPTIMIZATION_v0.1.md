# 叙账 · AI 指令台优化方案 v0.1

> 日期：2026-06-18  
> 状态：真机初评后方案沉淀，今天只记录，不开发  
> 范围：复盘页 AI 指令台；不改记账页分类、备注、情绪标签、预览卡逻辑

## 1. 产品判断

AI 指令台适合作为会员强付费点。

它不是传统报表，也不是在固定卡片旁边补一句 AI 解释，而是让用户用自己的话指挥账本：

- 想查什么，就让 AI 整理什么。
- 想补什么，就让 AI 生成待确认草稿。
- 想核对什么，就让 AI 找证据，而不是丢一堆指标给用户。

这符合叙账的北极星：看见生活，而不是制造财务焦虑。

## 2. 当前能力定位

### 已有方向

- 自然语言查询：如「过去三天餐饮花了多少」「这周交通」。
- 简洁结论：先给一句人话总结。
- 记录列表：把支撑结论的账单列出来。
- 轻量图表：只辅助理解，不做传统 KPI 看板。
- 批量补记草稿：如过去一周工作日通勤早晚各一次。
- 确认后保存：补记类必须用户确认，不静默写账本。

### 产品价值

| 价值 | 说明 |
|---|---|
| 主动意图强 | 用户自己提出问题，比被动看报表更有付费感 |
| 去焦虑 | 不强调超支、预算、排名，而是整理生活证据 |
| 账本越久越有用 | 历史记录越多，查询和补记越聪明 |
| 与记账页解耦 | 不触碰记账页高风险文案/分类/备注逻辑 |
| 可持续扩展 | 查询、找重复、补记、导出、复盘都能接入 |

## 3. 优先优化方向

### P0 · 算法可靠性

1. 时间范围解析
   - 支持：今天、昨天、过去三天、本周、上周、本月、上个月、最近 7 天。
   - 模糊表达兜底：如「这阵子」「最近」默认最近 7 天，并在结果中说明。

2. 分类与场景识别
   - 餐饮：吃饭、外卖、早餐、午餐、晚餐、咖啡、奶茶。
   - 交通：通勤、地铁、公交、打车、停车、加油。
   - 日用：超市、纸巾、清洁、生活用品。
   - 健康：药、医院、挂号、运动补给。

3. 结果排序
   - 查询类：结论优先，其次关键记录，再给列表。
   - 补记类：日期和时间段稳定排序，方便确认。
   - 找重复：按疑似程度排序，说明判断依据。

4. 安全边界
   - 补记、合并、删除、批量保存都必须确认。
   - AI 只能生成草稿，不直接修改已有账单。
   - 不回写记账页的分类/备注/情绪标签推荐规则。

### P1 · UI 结构

结果页建议固定为三层：

```text
一句结论
├─ 证据：列表 / 轻图 / 重复项理由
└─ 操作：继续追问 / 生成草稿 / 确认保存
```

不同指令使用不同结果壳：

| 类型 | UI |
|---|---|
| 查询消费 | 结论 + 简洁柱图 + 账单列表 |
| 找重复 | 疑似重复组 + 金额/时间/备注相似理由 |
| 批量补记 | 待确认草稿列表 + 金额来源说明 |
| 缺信息 | 温和追问输入，不显示报错态 |
| 不支持 | 给可改写示例，不直接失败 |

### P2 · 会员边界

建议边界：

| 能力 | 免费 | 会员 |
|---|---|---|
| 简单查询 | 每日少量体验，如 3 次 | 不限 |
| 跨月查询 | 不开放或每周 1 次体验 | 不限 |
| 找重复账单 | 体验 1 次 | 不限 |
| 批量补记 | 可预览少量，不可批量保存 | 可确认后一键保存 |
| 导出结果 | 不开放 | 可导出或生成分享图 |
| 连续追问 | 限次数 | 不限 |

边界原则：

- 已经生成的免费结果不要突然遮住。
- 在执行前或继续深入时提示会员。
- 会员提示要说明「这类整理会更省事」，不要写成硬性拦截。

## 4. 推荐文案

### 入口文案

- 问问账本
- 用一句话整理账本
- 想怎么看，就怎么问

### 输入占位

- 试试：过去三天餐饮花了多少？
- 试试：帮我找找最近有没有重复账单
- 试试：补记上周工作日通勤，早晚各一次

### 免费触达会员

- 这类连续整理会员可继续用。
- 批量草稿已生成，会员可确认后一键保存。
- 更长时间范围的整理，会员可以继续追问。

### 不确定反馈

- 我还没看清这个范围，要不要换个说法？
- 这条指令需要一个金额，填一下就能生成草稿。
- 我找到了几条可能相关的记录，先给你核对。

## 5. 近期迭代建议

### 第一版小修

- 优化时间范围解析。
- 增加分类同义词词典。
- 查询结果按「结论 / 证据 / 操作」重排。
- 批量补记金额来源显示清楚：历史金额 / 用户输入 / 待确认。
- 免费用户增加轻量次数边界。

### 第二版增强

- 支持连续追问上下文。
- 找重复账单增加相似度解释。
- 支持“导出这次结果”。
- 支持把 AI 查询结果转成周分享图素材。

### 第三版会员强化

- 跨月、季度、年度查询。
- 复杂组合条件：时间 + 分类 + 金额区间 + 备注关键词。
- 自动生成「这一段生活证据」卡片。
- 与 OCR 复核联动：AI 帮用户整理疑似重复和不确定项。

## 6. 不能碰的边界

- 不改记账页最后一次用户意图规则。
- 不改 RecordView 的备注、分类、情绪标签推荐链路。
- 不静默保存 AI 生成的补记记录。
- 不自动删除重复账单。
- 不把复盘页做成传统财务 KPI 报表。

## 7. 真机验收重点

- 输入常见自然语言是否能得到稳定结果。
- 结果是否先说人话，再给证据。
- 批量补记是否必须确认。
- 免费用户是否知道哪里是会员能力。
- 会员入口是否自然，不抢当前任务。
- 复盘页整体气质是否仍然是「看见生活」，不是「财务审判」。
