# COPY-02 周记 / 月章播放文案活人感收敛方案 v2

> 日期：2026-07-17
> 状态：可评审方案，尚未修改播放代码
> 归属任务：`COPY-02`（当前唯一 `IN_PROGRESS`）

## 1. 目标与边界

目标不是把文案写得更“文艺”，而是让它像一个认真看过账本的人在回顾：先说清事实，只做一层观察，有明确证据时才多回应一句。

本方案确认前不修改：

- `PlaybackCopyPool.swift`
- `PlaybackService.swift`
- `SummaryPlaybackSheet.swift`
- 文案夹具、测试和播放 UI

实施后仍保持冻结：

- 周记成熟数据 5 章、弱数据 3 章；月章固定 6 章。
- 章节顺序、播放时长、照片选择、进度、额度、会员、分享和完成动作。
- 周记 / 月章的主记录范围、正金额过滤、主题和播放控制 UI。
- 不引入远程 AI，不生成账本外的人物、地点、原因、关系或感受。

唯一建议申请的事实口径例外：**当前月未结束时，月度变化只与上个月同期天数比较，不再用本月残缺数据对比上月完整月。**这只调整“变化章”的对照范围，不改变本月章节记录、金额、照片或其他章节事实。若不批准该例外，则降级为“本月未结束，暂不做完整月环比”。

## 2. 核心生成模型

每一章只承担一个主要角色：

1. 事实概况：多少笔、分布在几天、记录集中在哪天。
2. 具体记录：一笔可回看的记录，以及它的日期、金额或明确说明。
3. 节奏变化：同一范围内的集中、分散、重复或前后段差异。
4. 同期变化：有足够对照数据时，陈述增减和原始数字。
5. 克制收尾：说完即止，不升华、不催促用户继续记。

每章按固定顺序生成：

> 事实锚点 → 一层观察 → 有充分证据时才回应

示例：

- 事实锚点：`周三记了 3 笔。`
- 一层观察：`是这周记录最多的一天。`
- 可选回应：只在用户明确备注或照片说明提供了语义时增加；没有时不补。

禁止把分类、场景、情绪、次数、天气和升华同时塞进一句话。

## 3. 证据等级与可说范围

### A 级：可直接进入主文

- 日期、星期、时间、金额、笔数、活跃天数。
- 已保存分类，但只作为事实分类，不解释消费动机。
- `userEditedTitle == true` 且通过现有脏标题、隐私和默认标题检查的标题。
- 通过现有 `EchoAnchorService.isEligibleLifeTraceTitle` 检查的干净标题；若并非用户编辑，不写成“你说 / 你写”，只说“账本里记着”。
- 照片存在、照片数量、照片日期；有明确照片说明时可引用说明。
- 已保存的天气、城市、时间上下文，但只说客观事实。

### B 级：可汇总后进入主文或辅助证据

- 一周 / 一月记录数、活跃天数、唯一最集中日期、并列集中日期。
- 同一分类可靠重复次数。
- 干净标题的可靠重复次数。
- 有足够样本的同期金额、笔数和分类增减。
- 连续记录天数等可复算事实。

### C 级：只允许帮助选材，默认不得原样展示

- `displayEmotionTag`，包括自动推断和二次修正后的标签。
- `LifeMarkService` 的内部标题、场景包名称、里程碑名称。
- `scentText`、`scentWords` 中的内部组合词。
- “第 N 次”“某某一段”“这条线又回来”等自动标签。
- 由系统推断的“辛苦、放松、治愈、终于、松了一口气”等感受。

当前模型没有可靠的“用户手动确认情绪”来源字段，因此本轮把 `displayEmotionTag` 一律按 C 级处理：可用于选择记录，不作为用户感受证据。

## 4. 文案长度与层级预算

- 章节标题：2～9 个汉字，优先事实词，不用“最热、气味、胶片”等隐喻。
- 周记主文：1～2 句，建议 18～44 个汉字；最多引用 1 个记录标题。
- 月章主文：1～2 句，建议 22～56 个汉字；最多陈述 2 个变化数字。
- 辅助证据：只放日期、金额、笔数、分类、同期原始值，建议不超过 32 个汉字。
- 主文已出现记录标题时，辅助层不得再次出现同一标题，除非辅助层增加的是独立数值证据。
- 主文和辅助层没有新增信息时，辅助层隐藏，不为了填满卡片重复一句话。
- `warm` 与 `plain` 必须共享同一事实。`warm` 最多增加一层克制回应；证据不足时两者可以完全相同。
- 删除宠物代言式表达，例如“{petName} 看到……”。宠物是否开启不应改变事实结论。

## 5. 周记章节方案

### 0 笔

- 保持当前行为：不生成章节。
- 空态：`这周还没有记录，先记一笔再回来。`
- 禁止生成“生活已经有了开头”等空洞内容。

### 1～2 笔：固定 3 章

| 顺序 | 标题 | 负责内容 |
|---:|---|---|
| 1 | `这一周` | 记录数和日期分布，不总结节奏 |
| 2 | `这一笔` | 选一笔最具体记录；无干净标题时说日期、分类和金额 |
| 3 | `这周先到这里` | 一句收尾，不催用户补记，不承诺下次更完整 |

示例：

- 1 笔：`这周目前记了 1 笔，在周三。`
- 2 笔、不同天：`这周记了 2 笔，分别在周二和周五。`
- 2 笔、同一天：`这周记了 2 笔，都在周五。`
- 代表记录：`周五 19:20，账本里记着「下班地铁」。`
- 无干净标题：`周五 19:20 记了一笔交通，¥4.00。`

### 3 笔以上：固定 5 章

| 顺序 | 推荐标题 | 规则 |
|---:|---|---|
| 1 | `这一周` | 记录数 + 活跃天数，最多一层分布观察 |
| 2 | 动态：`记录最多的一天` / `记录较多的日子` / `记录怎么分布` | 唯一最大、并列最大、每天各一笔分别降级；不再使用“哪天最热” |
| 3 | `这一笔` | 留一笔具体记录；若与第 2 章来自同一记录，第 2 章只说聚合，不重复标题 |
| 4 | `这周反复出现` | 可靠重复分类 / 标题；无重复时明确说分散，不硬造“主线” |
| 5 | `这周先到这里` | 克制收尾 |

示例：

- 唯一集中日：`周三记了 3 笔，是这周记录最多的一天。`
- 并列：`周二和周五各记了 2 笔，是这周记录较集中的两天。`
- 每天各一笔：`这周的 4 笔分在 4 天里，没有特别集中的一天。`
- 全部通勤：`这周 4 笔都是交通，其中 3 笔是通勤记录。`
- 全部同分类：`这周 5 笔都归在餐饮，没有再拆出别的分类。`
- 无可靠重复：`这周几笔记录分得比较开，没有哪一类反复出现。`

## 6. 月章章节方案

月章保持固定 6 章，但标题和空数据分支必须真实：

| 顺序 | 推荐标题 | 负责内容 |
|---:|---|---|
| 1 | `7 月回看` | 记录数、活跃天数、当前是否完整月 |
| 2 | `月初留下的` | 1～10 日内的一笔代表记录；没有则明确月初无记录 |
| 3 | `后来留下的` | 11 日以后的一笔不同记录；没有则说明后段尚无新增 |
| 4 | 当前月：`和上月同期相比`；完整月：`和上个月相比` | 只讲可复算变化；样本不足时明确暂不比较 |
| 5 | `这个月反复出现` | 可靠重复类别 / 标题；无重复时说分散 |
| 6 | `这个月先到这里` | 克制收尾，不写“新的天气、新的路线” |

### 月初 / 后段空缺规则

- 1～10 日无记录：`月初十天没有记录，这个月第一笔出现在 7 月 14 日。`
- 当前日期早于 11 日：`这个月还没走到后半段，先不替后面的日子下结论。`
- 已过 11 日但后段无记录：`11 日以后还没有新的记录。`
- 月初和后段候选最终是同一笔：后段章节不得重复该标题，改用时间段空缺或整体分布事实。

### 月度比较口径

推荐口径：

- 当前未结束月份：本月 1 日至今天，对比上个月 1 日至相同日序；标题显示“和上月同期相比”。
- 完整历史月份：完整月对比前一个完整月；标题显示“和上个月相比”。
- 上月同期 / 上月完整月少于 3 笔或总额为 0：不报百分比、不判断新增 / 消失，主文写“上个月可对照的记录还不够，暂时不做环比”。
- 有明显变化：只说最强的 1～2 个变化，并给原始数字。
- 无明显变化：写“金额和笔数都接近，没有明显变化”，辅助层列两段总额与笔数。
- 新增分类：只有当前至少 2 笔，或金额达到 ¥50 且占本期至少 8%，才说“新增”。
- 消失分类：只有上期满足同等可靠阈值、本期确实为 0，才说“没有再出现”。
- 增减阈值继续沿用现有可复算标准：金额差至少 `max(¥50, 上期金额 25%)`，或笔数差至少 2，或金额占比差至少 12 个百分点。

示例：

- `和 6 月同期相比，交通从 ¥168 降到 ¥126，少了 ¥42；餐饮从 ¥84 增到 ¥102，多了 ¥18。`
- `和 6 月相比，记录都是 12 笔，总额相差 ¥9，没有明显变化。`
- `上个月同期只有 1 笔，暂时不做环比。`

## 7. 主文、辅助证据与跨章节去重

实施时每条文案事实都带一个证据键，例如：

- `record:<itemID>:title`
- `record:<itemID>:date`
- `week:busiest-day:<date>`
- `month:category-delta:<category>`
- `period:count`

去重规则：

1. 同一章主文和辅助层使用相同证据键时，辅助层必须提供不同维度的数字，否则隐藏。
2. 第 2 章已引用某条记录标题，第 3 章优先换另一笔；无法换时，第 2 章只保留聚合事实，把标题留给第 3 章。
3. 月初和后段不得引用同一条记录标题。
4. 同一标题全篇最多作为主文出现 1 次。
5. “反复出现”章只输出计数事实，不再复述前面章节的完整句子。

示例：

- 主文：`周三记了 3 笔，是这周记录最多的一天。`
- 辅助：`交通 2 笔 · 餐饮 1 笔`
- 禁止辅助：`周三是这周记录最多的一天。`

## 8. 边界场景矩阵

| 场景 | 主文策略 | 辅助证据 | 禁止表达 |
|---|---|---|---|
| 0 笔 | 不生成章节 | 无 | 空洞回顾、会员推动 |
| 周记 1 笔 | 日期 + 1 笔 | 分类、金额 | “本周节奏”“主线” |
| 周记 2 笔 | 说明同日 / 不同日分布 | 两个日期 | “反复出现”“最集中” |
| 周记 3+ 笔 | 概况、集中日、代表记录、重复各自分工 | 计数和金额 | 一句塞满全部信号 |
| 同一天多笔 | 说当天笔数和分类分布 | 当日分类计数 | “一天特别忙”这类感受推断 |
| 全部通勤 | 直接说交通 / 通勤次数 | 时间段、金额 | “辛苦了”“回家路”除非标题明确 |
| 全部同分类 | 说全部归于该分类 | 笔数、总额 | “最高分类”“生活主线” |
| 用户标题 + 默认标题 | 优先干净用户标题；默认标题降级为分类事实 | 日期、金额 | 把默认标题说成用户原话 |
| 有照片无说明 | 只说哪天有照片 | 照片数 | 猜照片内容、情绪或人物 |
| 有明确照片说明 | 可引用一次说明 | 日期、照片数 | 再用自动标签解释说明 |
| 只有自动情绪标签 | 不展示标签，不回应感受 | 客观事实 | “终于”“治愈”“松了一口气” |
| 晚间 / 凌晨通勤 | 说具体时间和记录标题 | 时间、交通金额 | “辛苦了”“终于到家”除非用户写明 |
| 医疗记录 | 说日期、健康分类和干净标题 | 金额 | 诊断病情、判断严重程度 |
| 家庭 / 人情记录 | 只复述用户明确标题 | 日期、金额 | 猜人物关系或情感动机 |
| 月初有、后段无 | 月初说具体记录；后段明确无新增 | 日期范围 | 用月初记录冒充后半月记录 |
| 前后段选中同一笔 | 只在一章引用标题；另一章讲空缺 / 分布 | 日期范围 | 同一标题重复两章 |
| 上月数据不足 | 明确暂不比较 | 两段笔数 | 百分比、趋势、新增 / 消失 |
| 同期增加 / 减少 | 陈述原始值和差额 | 本期 / 上期金额、笔数 | “重心变了”“更常回来” |
| 同期持平 | 说明接近或持平 | 两段原始数字 | 硬找变化点 |
| 新增 / 消失分类 | 达可靠阈值才说 | 分类笔数和金额 | 单笔偶然就称新增 / 消失 |
| 当前月未结束 | 同期比较，或降级不比较 | 截止日期 | 残月对完整月直接环比 |
| 内部标签 / 第 N 次 | 自动标签只选材，不展示 | 真实计数 | “咖啡饮品第 30 次”“公共交通一段” |
| 天气 / 城市 | 只说已保存事实 | 日期、天气 / 城市 | 由天气推断心情 |
| Dynamic Type | 主文最多 2 句，辅助最多 3 行 | VoiceOver 合并朗读 | 为保布局截断关键数字 |

## 9. 四组完整模拟文案

### A. 1 笔周记

1. `这一周`

   主文：`这周目前记了 1 笔，在周三。`

   辅助：`7 月 15 日`

2. `这一笔`

   主文：`周三 19:20，账本里记着「下班地铁」。`

   辅助：`交通 · ¥4.00`

3. `这周先到这里`

   主文：`这周先记到这里。`

   辅助：隐藏

### B. 5 笔成熟周记

1. `这一周`：`这周记了 5 笔，分布在 3 天里。`
2. `记录最多的一天`：`周三记了 3 笔，是这周记录最多的一天。`

   辅助：`交通 2 笔 · 餐饮 1 笔`

3. `这一笔`：`周五 21:10，账本里记着「和朋友吃火锅」。`

   辅助：`餐饮 · ¥126.00`
4. `这周反复出现`：`交通出现了 3 次，其中 2 次是通勤记录。`
5. `这周先到这里`：`这周先到这里。`

### C. 当前未结束月份

1. `7 月回看`：`7 月目前记了 12 笔，分布在 8 天里。`
2. `月初留下的`：`7 月 4 日，账本里记着「下班地铁」。`
3. `后来留下的`：`7 月 15 日，你记下了「和朋友吃火锅」。`
4. `和上月同期相比`：`和 6 月同期相比，交通少了 ¥42，餐饮多了 ¥18。`

   辅助：`统计范围：1 日—17 日`
5. `这个月反复出现`：`通勤出现了 8 次，咖啡出现了 5 次。`
6. `这个月先到这里`：`7 月先记到这里。`

### D. 上月数据不足

1. `7 月回看`：`7 月目前记了 6 笔，分布在 4 天里。`
2. `月初留下的`：按真实月初记录生成。
3. `后来留下的`：按真实后段记录或空缺生成。
4. `和上月同期相比`：`上个月同期只有 1 笔，暂时不做环比。`
5. `这个月反复出现`：按可靠重复生成；无重复则说明分散。
6. `这个月先到这里`：`7 月先记到这里。`

## 10. 实施拆分

### 第一步：证据与自然化层

在 `PlaybackService.swift` 增加只服务播放文案的证据模型和自然化函数：

- 标题来源与可引用等级。
- 时间、金额、分类和照片事实。
- 重复分类 / 标题的可靠计数。
- 当前月同期对照范围。
- 主文 / 辅助证据键与跨章节占用记录。

不修改账本模型和持久化字段。

### 第二步：模板池收敛

在 `PlaybackCopyPool.swift`：

- 周 / 月分开模板。
- 每个叙述角色只保留 2～3 条确定性模板。
- 删除“格、胶片、气味、画面、露出来、生活开头、重心换位置、证明真实发生过”等抽象升华。
- 删除 `{petName}` 代言式文案。
- 模板缺证据时使用事实降级，不再用空泛 fallback 填充。

### 第三步：章节标题与辅助层

在 `SummaryPlaybackSheet.swift` 只调整：

- 新章节标题文本。
- 辅助证据的格式与去重。
- 无新增证据时隐藏辅助卡。

不改卡片布局、动画、照片、进度和控制器。

### 第四步：回归护栏

- 增加至少 24 组确定性夹具，覆盖本文件矩阵。
- 页面 copy snapshot 覆盖周记 0 / 1 / 2 / 3+，月章空段、重复、同期变化和数据不足。
- 文案 lint 阻止抽象模板、内部标签直出、未替换占位符和宠物代言式表达。
- XCTest 验证章节数量 / 顺序 / 时长不变，主辅不重复，月初 / 后段不复用同一标题，当前月使用同期口径。
- 真机覆盖默认 / 大字 / VoiceOver、宠物开关、照片有无和 1 / 2 / 3+ 笔周记、6 章月章。

## 11. 验收标准

每个夹具逐条检查五项：

1. 事实准确：日期、金额、分类、笔数、对照范围可复算。
2. 自然程度：像口语回顾，不像模板拼接或系统标签。
3. 情绪分寸：没有证据不替用户感受。
4. 信息增量：主文、辅助层和相邻章节不重复。
5. 周月差异：周记讲最近几天和具体一笔；月章讲前后段、可靠重复和同期变化。

任一项不通过都只修对应规则或模板，不扩大到章节结构、UI、额度、照片、会员或页面拆分。
