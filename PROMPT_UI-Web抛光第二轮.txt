你在 xuzhangapp 实现 Task UI-P5b-WEB — Web 视觉抛光第二轮（仅 web-preview/；IA 与 P5 定稿勿动）。

================================================================================
边界
================================================================================

可改：
  web-preview/index.html（#statsPage #insightPage #settingsPage #monthlyInsightModal 结构/类名）
  web-preview/styles.css（chip、次章区、月记脚注、tab-icon、头像）
  web-preview/app.js（痕迹 kicker/chip 绑定、月记脚注 DOM 显隐；仅 UI 钩子）

禁止改：
  信息架构（主叙事 / 附录 Sheet / quiet 入口分层）
  buildTraceSliceChapters / generateMonthlyInsight / 试用会员 / 各 Tab 业务逻辑
  homePage / recordPage 主结构
  NativeDemoApp、backend、git commit

允许：
  · 痕迹 chip 从 pill 控件改为刊头内文字切换
  · 月记 Sheet 动作区改脚注排版（不改 generate 函数体）
  · inline SVG 替换 👤 头像（无外链图）

================================================================================
背景（用户对 P5 定稿后的抛光清单）
================================================================================

Web 四 Tab 气质已基本 freeze。本轮只修「仍像控件 / 仍像表单 / 真机偏空 / 色系统不协调」：

  ① 痕迹周/月 chip 仍像 segmented control
  ② 复盘主屏下半偏空（真机更明显）
  ③ 月记 Sheet 按钮区仍像表单，不像信纸脚注
  ④ 底栏图标背景偏淡米色，与 xz-paper token 不协调
  ⑤（可选）身份头像像系统默认，可改叙账印章
  ⑥（可选·不急）入口行右侧摘要略工具 — 本 PR 可跳过

================================================================================
任务 1 · 痕迹 chip 弱化（P0）
================================================================================

1.1 HTML — 删除独立 `.trace-range-chips` pill 容器；把切换并入刊头：

```html
<p class="trace-chapter-kicker" id="traceChapterKicker">
  <button type="button" id="traceRangeWeekBtn" class="trace-range-text">本周</button>
  <span class="trace-range-sep" aria-hidden="true">·</span>
  <button type="button" id="traceRangeMonthBtn" class="trace-range-text active">本月</button>
</p>
```

  · 删除原 `#traceChapterKicker` 纯文本节点与 `.trace-range-chips` 整块
  · 保留 button id：`traceRangeWeekBtn` / `traceRangeMonthBtn`（app.js 监听不变）

1.2 app.js renderStats / setTraceHeroRange：
  · 移除对 `refs.traceChapterKicker.textContent` 的「本周/本月」写入（kicker 由 active class 表达）
  · 继续 toggle `.active` on 两个 `.trace-range-text`

1.3 CSS — 新建 `.trace-range-text` / `.trace-range-sep`；删除或覆盖 `.trace-range-chips` / `.trace-range-chip` 的 pill/绿底/阴影：

```css
.trace-chapter-kicker {
  display: flex;
  align-items: center;
  gap: 6px;
  margin: 0 0 14px;
  font-size: 12px;
  font-weight: 700;
  letter-spacing: 0.04em;
}
.trace-range-text {
  border: 0;
  background: transparent;
  padding: 0;
  color: color-mix(in srgb, var(--subtext) 76%, var(--text));
  font: inherit;
  font-weight: 650;
  cursor: pointer;
}
.trace-range-text.active {
  color: color-mix(in srgb, var(--text) 92%, var(--subtext));
  font-weight: 700;
  text-decoration: underline;
  text-decoration-color: color-mix(in srgb, var(--accent) 35%, transparent);
  text-underline-offset: 3px;
}
.trace-range-sep {
  color: color-mix(in srgb, var(--subtext) 55%, transparent);
  font-weight: 400;
}
```

  · **禁止**：pill 外框、accent 绿底、box-shadow、≥13px 按钮 padding
  · 切换后叙事/slip/听听逻辑零回归

================================================================================
任务 2 · 复盘主屏下半编排（P0 · 真机空）
================================================================================

2.1 HTML — 在 `.insight-journal` 与 `.insight-next-chapter` 之间加章末脚注（纯文案，无交互）：

```html
<p class="insight-chapter-footnote muted">周记写到这里。想读更长的，再翻开下面。</p>
```

2.2 `.insight-next-chapter` 编排（CSS）— **禁止 flex 撑开中间**：

  · **删除/禁止**：`#insightPage { min-height + display:flex }`、`.insight-next-chapter { margin-top: auto }`
    （会把「再读一章」钉到底，中间拉出大段灰洞，用户已反馈「怪」）
  · `.insight-chapter-footnote`：13px italic；`margin: 12px 0 14px 22px`；opacity ~0.72
  · `.insight-next-chapter`：紧跟脚注；`margin: 0 0 96px 8px`（固定间距，不 auto）
  · `#insightPage` 仅 `padding-bottom: max(32px, env(safe-area-inset-bottom))` 给 tab bar 留位
  · `.insight-next-card`：可略放大（padding 14px 16px；title 16px）

2.3 目标：「周记 → 脚注 → 再读一章」**连成一段阅读流**；长屏下方可自然留白，但中间不能有无内容拉伸沟

================================================================================
任务 3 · 月记 Sheet 信纸脚注排版（P1）
================================================================================

3.1 HTML — 重构 `#monthlyInsightModal` 内 `.monthly-sheet-paper` 动作区顺序（正文体裁不变）：

```html
<div class="monthly-sheet-paper xz-chapter-card">
  <section class="insight-sheet-body">
    <article id="monthlyJournalBody" class="insight-journal-body monthly-journal-body">…</article>
    <p id="monthlyJournalClosing" class="insight-journal-closing muted"></p>

    <!-- 脚注区：取代块状 actions-top -->
    <footer class="monthly-journal-footnote">
      <p class="monthly-footnote-primary">
        <button id="generateMonthlyInsightBtn" type="button" class="monthly-footnote-link">生成这一月的回顾 →</button>
        <span id="monthlyTrialInline" class="monthly-trial-inline muted"></span>
      </p>
      <p id="monthlyAIStatus" class="muted ai-status-pill hidden"></p>
      <p class="monthly-footnote-quiet">
        <button id="monthlySaveSummaryBtn" type="button" class="monthly-footnote-link hidden">保存月度小结</button>
        <span class="monthly-footnote-dot hidden" aria-hidden="true">·</span>
        <button id="monthlyToneSwitchBtn" type="button" class="monthly-footnote-link hidden">换个叙述风格</button>
      </p>
      <button id="advancedInsightToggleBtn" type="button" class="monthly-footnote-link monthly-footnote-more">更多时间段</button>
    </footer>

    <div id="advancedInsightActions" class="advanced-insight-actions hidden">…季/年按钮保持 quiet 小钮…</div>
  </section>
</div>
```

  · 删除原 `.monthly-sheet-actions-top` 与 `.monthly-sheet-quiet-actions` 包裹（或留空 hidden）
  · `#monthlyTrialText` 可移除，试用改写入 `#monthlyTrialInline`（「剩余 x/5 次」放链接后，12px muted）

3.2 app.js renderInsight 月度段：
  · `monthlyTrialInline.textContent` ← 原 monthlyTrialText 文案；会员时 hidden
  · 有 `monthlyReport` 且非 generating：
      `generateMonthlyInsightBtn` 文案「再写一版 →」
      class：`monthly-footnote-link monthly-footnote-primary-action`
  · 无 report：
      文案「生成这一月的回顾 →」
      class：`monthly-footnote-link monthly-footnote-primary-action`
  · `monthlySaveSummaryBtn` / `monthlyToneSwitchBtn`：class 加 `monthly-footnote-secondary`；显示时同步 `·` span
  · `advancedInsightToggleBtn`：class `monthly-footnote-link monthly-footnote-appendix`
  · **禁止**改 generateMonthlyInsight 函数体

3.3 CSS — 页脚四级层次（用户验收：不能四个链接看起来像一组工具按钮）

  共用容器 `.monthly-journal-footnote`：
  · `margin-top: 18px; padding-top: 14px`
  · 顶部分隔：`border-top: 1px solid color-mix(in srgb, #f1d7bd 28%, transparent)`

  **L1 主操作** `.monthly-footnote-primary-action`（稍强，但仍非 pill 按钮）：
  · 14px；font-weight 680
  · color: color-mix(in srgb, var(--accent) 62%, var(--text))
  · 无 background / border / border-radius
  · hover 仅 color 加深，无底色块

  **L2 脚注说明** `.monthly-trial-inline`（再淡，像页边批注）：
  · 12px；font-weight 500；font-style: normal
  · color: color-mix(in srgb, var(--subtext) 72%, transparent)
  · opacity ~0.68；与 L1 同一行，gap 10px，**视觉上明显弱于** L1

  **L3 次操作** `.monthly-footnote-secondary` + `.monthly-footnote-quiet`：
  · 12px；font-weight 560
  · color: color-mix(in srgb, var(--subtext) 78%, var(--text))
  · opacity ~0.82
  · `margin-top: 10px`；inline + `·` 分隔
  · **禁止**与 L1 同色同重；降低存在感，像信纸页脚小字

  **L4 附录入口** `.monthly-footnote-appendix`：
  · `margin-top: 16px`（比 L3 再往下）
  · 12px；font-weight 520
  · color: color-mix(in srgb, var(--subtext) 62%, transparent)
  · opacity ~0.62
  · 单独一行，像附录目录，不是操作区第三排按钮

  布局：
  · `.monthly-footnote-primary`：flex wrap align baseline，gap 8–10px
  · 四个层级**不得**共用同一 `.monthly-footnote-link` 字重/颜色（必须拆 class）
  · 覆盖删除：`.monthly-generate-link` pill 描边在 footnote 内不生效
  · `.advanced-insight-actions` 距 L4 再 12px，季年钮保持 quiet 小钮

================================================================================
任务 4 · 底栏图标色对齐 xz-paper（P1）
================================================================================

4.1 只改 styles.css `.tab-icon` 相关 fill / filter，**不改** SVG 路径：

```css
.tab-icon .tab-bg {
  fill: color-mix(in srgb, var(--xz-paper-warm) 42%, var(--panel) 58%);
}
.tab.active .tab-icon .tab-bg {
  fill: color-mix(in srgb, var(--accent) 18%, var(--xz-paper-mist) 82%);
}
.tab-icon {
  color: color-mix(in srgb, var(--subtext) 62%, var(--text));
  filter: none; /* 去掉偏橙 drop-shadow */
}
.tab.active .tab-icon {
  color: color-mix(in srgb, var(--accent) 78%, var(--text));
  filter: none;
}
```

  · 删除/覆盖 `#fff1d1` `#ffe8b6` `#c58f71` `#d68f75` 等孤立米色硬编码
  · `.tab-glyph` / `.tab-glyph-soft` 可略降对比，与页面暖纸一致

================================================================================
任务 5 · 身份头像「叙账印章」（可选 · P2）
================================================================================

5.1 若做：替换 `#accountAvatar` 内 👤 为 inline SVG（44×44，圆角 12px 方印，非圆形系统头像）：
  · 视觉：小账本线框或「叙」字一枚；暖褐描边 `color-mix(#c99058, accent)`；纸底 `xz-paper-warm`
  · CSS `.settings-identity-card .account-avatar`：border-radius 12px；去掉 emoji font-size 22px 居中

5.2 若时间紧：**可跳过**，不 blocker

================================================================================
任务 6 · 入口行摘要轻量化（可选 · P3 · 默认跳过）
================================================================================

用户注明不急。本 PR **不做**，除非顺手只改 2 处 small 文案：
  · 「云端备份、联网梳理」→「可选」
  · 「本地昵称、说明与协议」→「已本地化」
不做则忽略本节。

================================================================================
禁止项（回归护栏）
================================================================================

  [ ] 不得恢复痕迹 pill chip 绿底/外框
  [ ] 不得恢复月记 Sheet 满宽/胶囊生成钮
  [ ] 不得恢复复盘主屏第二块 panel 或满宽绿 CTA
  [ ] 不得改 setTraceHeroRange / openMonthlyInsightModal 的 period/存储逻辑
  [ ] 四 Tab 主叙事 DOM 层级不大改（仅上述局部）

================================================================================
验收（Web + 真机预览）
================================================================================

痕迹：
  [ ] 周/月切换像刊头文字，不像 iOS segmented control
  [ ] 无重复「本周」刊头 + chip 双显示

复盘主屏：
  [ ] 脚注与「再读一章」间距约 14–20px，**中间无 flex 拉伸灰洞**
  [ ] 长屏下方可自然留白，但不像上下两屏撕开
  [ ] 周记 journal 零回归

月记 Sheet：
  [ ] 正文 → 收束 → 页脚线 → 四级脚注（非一组同质链接）
  [ ] L1「再写一版/生成」稍强；L2「剩余 x/5 次」更淡像批注
  [ ] L3「保存 · 换风格」存在感低于 L1；L4「更多时间段」再下再淡像附录
  [ ] 无 pill 按钮块 / 操作区表单感

底栏：
  [ ] 图标底与页面暖纸同色系，无突兀米色圈

可选：
  [ ] 头像为方印 SVG，非 👤

bump：app.js / styles.css query `?v=20260609-polish2`

================================================================================
参考
================================================================================

定稿基准：PROMPT_UI-三Tab视觉语言与动效-Web完整版.txt（勿推翻）

@web-preview/index.html
@web-preview/styles.css
@web-preview/app.js
