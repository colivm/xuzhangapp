# Test-only Apple JWS chain

These files are synthetic fixtures for `verify-iap-jws-signature.mjs` and the
mocked IAP routing/diagnostic tests. The leaf private key is intentionally
non-production test material. Do not deploy this directory, use its key in an
environment, or treat its certificate chain as an Apple trust chain.
