你在 xuzhangapp 实现 Fix B2 — OCR 账单列表补全漏行（仅 OCRService.swift）。

================================================================================
现象
================================================================================

微信/支付宝账单列表截图 6 条只识别 4 条。
根因：parseListReceipts 只收带负号支出行；交易关闭无负号、部分状态行被跳过。

================================================================================
任务
================================================================================

1. 审阅 parseListReceipts 行匹配 regex，扩展：
   - 交易关闭 + 金额（无负号）可作支出草稿或跳过并注释原因
   - 退款正数行保持忽略（产品已定）
   - 避免把时间行（周二 08:38）当标题

2. 保持 looksLikeListScreenshot 入口逻辑不变

3. 加 1～2 条单元测试或内联注释示例（若项目无 OCR 测试可只注释）

================================================================================
禁止
================================================================================

不改确认 UI、不扣次逻辑、git commit

================================================================================
验收
================================================================================

[ ] 典型 6 行支付宝列表能识别 ≥5 条有效支出（按产品规则）
[ ] 单条支付成功页仍走 parseAlipay
[ ] 误识别率不显著上升

@NativeDemoApp/Services/OCRService.swift
