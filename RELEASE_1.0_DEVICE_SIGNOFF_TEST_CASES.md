# 叙账 1.0 真机封版签收用例

版本：1.0 / Build 10  
适用包：`xuzhang1.0-release-2026` 生产 Release；TestFlight 内测另使用 `feature/xuzhangapp-staging` 包，不能混用。

## 一、签收规则

- 任一 **阻塞项** 失败，封版结论为“不通过”，不得提交 1.0。
- 每条用例记录：设备、系统版本、包版本、开始/结束时间、结果、录屏或截图、崩溃日志/控制台日志。
- 生产包必须确认请求 `https://api.xuzhangapp.com`；内测包必须确认请求 `https://staging-api.xuzhangapp.com`。不能只看分支名称。
- 性能时间从用户完成点击、详情 Sheet 完成呈现后开始计时；首次打开与杀进程重开分别记录。

## 二、构建与环境（阻塞）

| ID | 操作 | 通过标准 |
|---|---|---|
| BUILD-01 | 从 `xuzhang1.0-release-2026` 干净检出，执行 Release Archive | 编译通过，无新增 Swift/concurrency warning/error；产物为 1.0 (10) |
| BUILD-02 | 安装生产包，打开设置/登录/账本同步 | 网络请求只到生产域名；不出现 staging 域名、测试 token 或开发接口 |
| BUILD-03 | 检查 App Store Connect 包信息 | Bundle ID、仅 iPhone、图标、显示名“叙账”、权限说明正确 |
| BUILD-04 | 在 TestFlight 安装 staging 包 | 请求只到 staging 域名；生产包和 staging 包的账本、会员状态、数据库完全隔离 |

## 三、详情图片专项（本次优化，阻塞）

准备两台设备：iPhone 15 或更新机型一台；iPhone SE 第三代或同等级低内存设备一台。准备 1 张真实 12MP 以上照片、9 张不同照片、1 条无图账单。

| ID | 操作 | 通过标准 |
|---|---|---|
| PHOTO-01 | 新建带 1 张真实照片的账单，保存后立即进入“消费详情” | 主图在 1 秒内出现可辨认预览；不出现持续转圈；详情卡片可滚动 |
| PHOTO-02 | 首次打开 9 张照片的账单 | 当前主图在 2 秒内出现；底部横向列表可操作；其余图片按需加载，不阻塞首屏 |
| PHOTO-03 | 点击主图展开，再收起 | 展开图在 2 秒内出现；收起后不重新长时间生成；无白屏、跳变、崩溃 |
| PHOTO-04 | 在 9 张图片间快速左右滑动 10 次 | 当前页最终显示正确；旧图不能覆盖新图；无持续 spinner、卡死或明显掉帧 |
| PHOTO-05 | 关闭详情并立即重新打开；再杀进程重开后打开 | 第二次打开明显快于首次；杀进程后仍能在 2 秒内显示首张预览 |
| PHOTO-06 | 进入详情后立即下滑关闭，再重新打开 5 次 | 取消任务后不阻塞后续打开；不出现错误图、空白或任务堆积 |
| PHOTO-07 | 删除当前图片、设置另一张封面、退出详情、重新进入 | 删除和封面变更正确持久化；新封面首屏显示正确；缩略图不显示旧图 |
| PHOTO-08 | 使用备份恢复带图片账单，再进入详情 | 恢复后图片全部可读；首张预览在 2 秒内出现；重启后仍可读 |
| PHOTO-09 | 用测试夹具删除一张原图或缩略图后进入详情 | 缺图在 3 秒内显示“图片暂时不可用”；不能无限转圈；其他图片仍可打开 |
| PHOTO-10 | 反复进入 20 条带图账单详情，观察 Allocations/Memory Graph | RSS 不持续单调增长；无大规模原图 Data 长期驻留；无异常崩溃 |

## 四、既有关键修复回归（阻塞）

| ID | 操作 | 通过标准 |
|---|---|---|
| FIX-001 | AI 指令台批量保存、最后一条单笔保存、清空结果 | 不白屏；清空后回到顶部；保存结果和账单数量正确 |
| FIX-002 | 工作日 08:30 前、08:30–18:29、18:30 后查看通勤草稿；再测昨天/上周、周末/节假日 | 只生成不晚于当前时刻的草稿；历史范围规则不变 |
| PERSIST-01 | 连续编辑同一记录金额/标题/图片并快速保存 | 最终值持久化；旧写入不能覆盖新值；失败时可恢复且不误上传云端 |
| BACKUP-01 | 导出本地备份，修改本机账本，再恢复；重复一次并制造旧格式/缺 manifest 场景 | 恢复成功后数据和图片正确；失败不替换当前账本；不能“提示成功但仍是旧数据” |
| SYNC-01 | 双设备登录同一账号，分别新增、修改、删除、离线后恢复联网 | 新者胜、墓碑、照片保留、重试和防重复正确；不串账本 |

## 五、会员、权限和无障碍（阻塞）

| ID | 操作 | 通过标准 |
|---|---|---|
| IAP-01 | TestFlight Sandbox 购买月/年/终身、取消、待处理、恢复、过期、撤销 | 会员权益、错误提示、恢复结果正确；不会跨账号改绑 |
| IAP-02 | 生产环境真实购买/恢复；验证错环境交易 | Production 与 Sandbox 路由正确；错误码可解释；服务端不接受伪造/错环境交易 |
| PERM-01 | 相册拒绝、仅选中照片、全部允许；通知/天气权限拒绝和重新授权 | 失败路径可继续使用；不死循环、不误保存、不虚假承诺 |
| A11Y-01 | VoiceOver 浏览首页、记录、详情图片、删除/恢复按钮 | 顺序、标签、状态和操作提示完整；图片加载失败有可读说明 |
| A11Y-02 | Dynamic Type 最大字号、横竖屏、Reduce Motion、深色模式 | 无裁切、重叠、不可点击控件；动画关闭后状态仍正确 |

## 六、规模与发布门禁（阻塞）

| ID | 操作 | 通过标准 |
|---|---|---|
| SCALE-01 | 100 条账本冷启动、进入详情、搜索/滚动 | 首屏可交互；无明显卡顿；数据和图片正确 |
| SCALE-02 | 1,000 条账本冷启动、进入痕迹/复盘/详情 | 首屏和滚动可用；无持续主线程阻塞；内存稳定 |
| SCALE-03 | 5,000 条账本冷启动、进入痕迹/复盘/详情 | 不崩溃、不丢数据；性能和内存达到发布矩阵要求 |
| RELEASE-01 | Xcode Debug、Release、全部 XCTest、device-audit、Instruments | 全部通过；任何未运行项都不能标记封版通过 |

## 七、封版签收表

- 生产分支/commit：`xuzhang1.0-release-2026@77b90a33f756230df157bae8701cf153eae477b2`（2026-09-21 固定，见第 8.10 节）
- 生产包版本/Build：1.0 (10)；实际 Archive 的 commit 必须等于上面固定值，不等即为无效候选
- 测试设备与系统：
- PHOTO-01～PHOTO-10：`通过 / 失败 / 未运行`
- FIX-001/FIX-002：`通过 / 失败 / 未运行`
- 持久化/备份/同步：`通过 / 失败 / 未运行`
- StoreKit Production/Sandbox：`通过 / 失败 / 未运行`
- 权限/无障碍/Reduce Motion：`通过 / 失败 / 未运行`
- 100/1,000/5,000 条与 Instruments：`通过 / 失败 / 未运行`
- 阻塞项：
- 证据链接：
- 最终结论：`可封版 / 不可封版`

## 八、2026-09-17 封版收口补充（不替代前述必测项）

个人 AI 文案池已延期至 **2.0**，本版只验收现有功能，不接入个人池、上传备注或新增后台 AI 请求。

### 8.1 本次基线与证据差距

- 当前测试分支：`feature/xuzhangapp-staging@784c954`，产品修复提交为 `4a27e6e`，后一个提交仅为交付记录。
- 当前生产分支（本节记录时）：`xuzhang1.0-release-2026@1a3311c`，尚不包含这次首页滑动/删除与情绪候选入口修复；不能用旧生产包验收新修复。**此值已被第 8.10 节取代。**
- 两套完整 Windows 门禁及四专项已在第 167 节交付时分别通过，代码此后未改变，当前只复核原证据，不把重复运行作为新的验收进展。生产检查使用的是“真实生产配置＋五个修复文件”的隔离副本，不等于生产分支已同步。
- 日志：`C:/Users/yf/AppData/Local/Temp/xuzhang-home-delivery-8ba1543971174b31a3e210607ef7f6f3/`。staging Debug/Release 含 STAGING，production 无 STAGING，配置与原门禁规则未改。
- 最终生产候选须在获授权同步后固定 commit/Archive/Build，再单独完整检查实际测试与生产候选；不得整分支合并覆盖两套配置，不得因旧日志通过跳过最终产物检查。
- 本机为 Windows，未执行 Swift 编译、XCTest、Archive、StoreKit 真机交易和 Instruments。用户此前“基本通过”的体验反馈保留，但不能泛化为全部封版项通过。

### 8.2 最近修复的优先真机用例

每项附实际设备/iOS、分支 commit、包 Build 与录屏/结果；目前均为“待当前候选签收”。

| ID | 操作 | 通过标准 |
|---|---|---|
| RECENT-01 | 首页“查看今日全部”，首次打开、快速纵滚、斜滑、左滑后再纵滚；同时回归痕迹页 | 纵滚不被横滑锁住，一次只打开一行；不误开详情、不持续拖影；两页原路由保持 |
| RECENT-02 | 首页删除取消/确认、中间一条/最后一条/连续不同记录，编辑内删除；重启检查 | 取消不删除，确认只删目标记录，无先消失再挤动的两段动画；最终统计和持久化正确；失败不伪报成功 |
| RECENT-03 | 餐饮分类锁定/未锁定下记录“瑞幸咖啡”，切换情绪后立即保存并重开；回归罗森与早餐馄饨 | 同场景切换、保存回显一致；不变分类/备注/角度，不将馄饨写成饺子；不切换保持默认 |
| RECENT-04 | 手写“晚饭”→点快捷备注→切换情绪→保存；另测“今天晚饭”、冲突餐次、无候选备注 | 支持的场景按钮不丢；无可靠候选保持静态，不跨场景凑文案；生活印记/奖励不因换句变化 |
| RECENT-05 | 连输金额/删除/清空/12→12.00；输入后立即保存、帮写、改类、换角度；切 Tab 返回 | 文字即时响应，最后金额不漏；分类建议、用户锁、明确备注纠类仍按原规则；过期情绪选择不沿用 |
| RECENT-06 | 系统深色＋跟随系统、手动深色，进入设置/前后台；看首页两个金额按钮 | 两种深色一致，按钮可读；大字号、VoiceOver、Reduce Motion 下仍可操作 |

以上优先用例通过后继续前述图片、备份/恢复、同步、弱网、100/1,000/5,000 条与内存等完整矩阵，不用近期体验通过替代数据安全或支付验收。

### 8.3 必须单独关闭的发布阻塞

以下为 2026-09-17 的历史审计结论；支付实现和沙盒验收的最新状态以第 8.6 节及台账第 180/182/183 节为准，不重复把已修代码或已确认的沙盒购买列为未完成。

- 支付安全：台账第 144、146 节记录的 Apple 交易 JWS 签名/证书链校验缺口未有修复交付证据。必须定向处理并补相应反向测试；现有 endpoint/environment 检查通过不能证明签名校验完成。NODE_ENV、严格 URL/占位符、Sandbox 安全回退与 STAGING 配置门禁已有修复，只需回归，不重列为未修缺陷。
- StoreKit：Production/Sandbox 购买、恢复、过期、撤销、错环境和跨账号绑定矩阵仍需真实证据。测试产生实际付费或外部变更时另行确认，不自动执行。
- 构建与产物：两套配置分别编译/运行完整 XCTest，最终生产 Archive 的版本、域名与权限校验通过；未运行不能写 PASS。
- 最新首页/情绪修复生产同步及当前候选真机验收尚未完成；当前结论为 **尚不能封版**，不是新功能开发阻塞。

### 8.4 2026-09-18 连续记账意图修复签收（当前均未运行）

对应台账第 176 节。固定规则为最后一次明确用户意图；真手写可取代更早的分类选择，系统生成/候选刷新不产生新分类意图。只有手写后的润色增加“候选→用这句”，其他入口操作方式保持。Windows 专项与门禁不替代本表。

| ID | 连续操作 | 必须逐阶段核对的结果 |
|---|---|---|
| INTENT-01 | 32→购物→中文输入夜宵→立即保存→原样编辑更新两次→重启 | 首次保存前为餐饮，备注原话一致；两次原样更新不改情绪/修改时间、不产生额外写库或上传 |
| INTENT-02 | 夜宵→购物；购物→夜宵→再选购物；分别走 Sheet/Focused | 最后主动选择购物生效；原话保留，不被备注反向改回餐饮；无依据购物情绪不出现 |
| INTENT-03 | 手写夜宵→连续看候选→保留原文；再次候选→用这句→撤销 | 未采纳时输入框/大字预览/待保存备注不变；采纳后同步更新，撤销恢复原话，不推进分类意图、不增加热食/加班事实 |
| INTENT-04 | 候选出现后改金额/日期/分类/备注/实际角度，再点旧候选位置 | 旧候选失效；重新点相同分类也记录明确选择；中文组词中间态不发布分类，立即动作前提交最后文字 |
| INTENT-05 | 空备注帮写→直接换句；日用点“便利店…”快捷句；人情生成“一起吃顿饭” | 原有一步操作、来源及类别保护保持；不新增候选确认；生成文本不伪记手写或人工分类纠错 |
| INTENT-06 | 旧购物→真改备注为瑞幸→再选购物；旧瑞幸→手机充电器 | 新手写可重新裁决，后选类又胜出；旧品牌解绑，改类不替换原话；品牌/纠错来源正确 |
| INTENT-07 | 旧记录有高精度金额、秒级日期、自选情绪及照片→原样更新；仅改金额/日期 | 未操作字段精度、照片与来源不变；仅金额/日期不重分类，相关情绪沿用既有事实守卫 |
| INTENT-08 | 打开编辑后外部同步改变同笔标题/金额/分类或添加照片→无操作更新；再仅改金额 | 初始比较基线不随父视图刷新；无操作保留最新账本，单字段改动只合并实际修改，未操作字段和照片不回退 |
| INTENT-09 | 有可靠习惯推荐但无备注，不点帮写；旧推荐迟到；快速金额输入立即保存 | 已采用分类与推荐依据同时显示，旧回包不能反写明确意图；保存当前实际类别 |
| INTENT-10 | 免费/会员、深浅色、大字号、VoiceOver、离线；切 Tab 返回、连续记下一笔 | 控件可读可操作、候选区域不挤压；当前草稿保留来源，新一笔清空旧锁/锚点/候选，不扩大场景权益 |

每行填写设备/iOS、实际 commit/Archive/Build、通过/失败/未运行、录屏及写入/上传证据。同时执行 `RecordContinuousIntentTests` 和原完整 XCTest。详细 24 条序列见 `RECORD_CONTINUOUS_INTERACTION_AUDIT_2026-09-18.md` 第 6 节；D3/D4 相邻观察项继续单列，不据此声称已修。


### 8.5 2026-09-18 首次定位与联网备份（当前均未运行）

本节对应台账第 179 节。记录实际 commit/Build、iOS/设备型号、安装方式，以及系统设置中叙账的联网/定位状态。网络案例需区分真实云端记录和本机提示，不把开关开启当成功；不通过删除真实用户数据重置权限。

| 用例 | 操作与预期 | 状态 |
|---|---|---|
| P01 | 新安装、不登录，首笔记账/再次记账/点击宠物/关闭再开启基础陪伴：均无定位弹窗，保存和基础陪伴正常 | 未运行 |
| P02 | 陪伴设置查看天气用途，天气默认关闭；主动开启天气后才弹系统定位，近似位置可用，拒绝仍可记账 | 未运行 |
| P03 | 天气已开启但未授权的旧用户：保存不索权，设置提供明确授权入口；此前拒绝/受限制/关闭系统定位分别显示正确说明，不能反复索权 | 未运行 |
| P04 | 天气已授权旧用户升级：保留显式开关；关闭天气、系统撤权、允许一次过期后旧天气/城市和迟到结果不再进入新记录，历史事实保留 | 未运行 |
| P05 | 用户现场复现：仅蜂窝网络、Wi-Fi 关闭，新安装拒绝“无线局域网与蜂窝网络”；记录拒绝前后系统实际联网选项。尝试真实验证码登录/手动开同步，核对服务器响应和云端记录，不因无第二次系统弹窗就认定越权 | 未运行 |
| P06 | 已登录且本机有待传记录，系统禁止联网/飞行模式，再开启同步：不能显示已完成，已有缓存不能替代服务器确认，本机不丢记录 | 未运行 |
| P07 | 拉取成功后上传中断，或部分上传/删除失败：主入口和备份页均显示尚未完成；失败删除留待重试，不复活记录；并发另一笔成功不能盖掉失败 | 未运行 |
| P08 | 失败后点击系统设置恢复蜂窝联网，返回手动重试：全部必要请求成功才显示完成；不开启天气也能成功；离线期间的本机照片与字段不丢失 | 未运行 |
| P09 | 云端空账本正常、200但ok=false/坏响应、401、5xx、请求超时分别核对：成功必须有服务器确认；401仍要求重新登录，其他错误不冒称权限已拒绝 | 未运行 |
| P10 | 免费/会员、深浅色、大字号、VoiceOver、两套实际配置；天气关闭不弹权限，说明/设置/重试按钮可用，照片云端边界与账本归属确认保持 | 未运行 |

Windows 源码门禁及新增 URLProtocol/XCTest 夹具不模拟 iOS 系统授权弹窗；本节真机证据齐备前不得标 VERIFIED。

### 8.6 2026-09-20 封版差距复核

本节为会员升级提交前的复核快照；后续测试分支交付见总台账第 184 节。代码推送不等于 TestFlight 已更新，新流程仍须在包含该提交的实际 Build 上签收。

- 已有证据：用户再次明确沙盒账号订阅购买正常；台账第 174/178 节亦已记录生产配置 TestFlight 的 Sandbox 购买、同账号恢复和加速到期正常，不再要求重复证明相同构建上的基础路径。第 182 节永久升级代码与完整 Windows 门禁已完成，不等于新入口和新恢复选择流程已经进入用户所测构建。
- 最终候选：只读 `git ls-remote` 核对远端测试分支为 `7ada3f5`、生产分支为 `b60bf71`。JWS 验签及首次定位/联网备份反馈修复在测试分支，尚未定向交付生产；独立首页滑动补丁也存在分支差异。永久升级仍在工作区未提交。需明确本版纳入的补丁后固定生产 commit、实际 Archive/Build 与后端版本，保留生产/测试配置隔离；本文件历史 Build 10 不能代替最终产物核对。**本节快照已被第 8.10 节取代：上述补丁已于 2026-09-21 同步进生产分支。**
- 支付交付：Apple JWS 签名链已实现并通过本地测试，不能再写成“尚未实现”；仓库尚无其线上部署/真实验签证据。部署前需生产 `APPLE_APPLE_ID`、根证书及依赖就绪，并验证新后端的真实交易验单。历史 Production 401 尚无独立授权排查闭环，沙盒成功不自动证明 Production 授权；当前未重新请求线上接口，不断言现服务仍返回 401。
- 新会员流程：按 `RELEASE_GATE_AND_DEVICE_MATRIX_v1.md` 第 8.2.1 节验订阅转永久、确认取消/失败/pending、登录后继续、永久和订阅共存恢复、跨叙账账号拒绝后继续核对其他合法候选、旧订阅手动取消不影响永久、主题入口/价格重试。基础沙盒购买已过的结论保留，只补新变化及未覆盖边界。
- 最终包验收：当前候选的 Debug/Release、完整 XCTest/Archive、图片/备份恢复/双设备同步、连续记账、首次定位/弱网失败提示、1,000/5,000 条性能及大字/VoiceOver仍缺回填记录。此为证据缺项，不表示用户从未测试或已知功能失败；已完成项目补实际 Build/设备/结果即可，不因旧表 NOT_RUN 全量宣称失败。
- 提交前核对：审核专用账号能在实际待审包登录且有效期覆盖审核，App Store Connect 的 IAP 商品/价格与首月优惠资格、协议税务银行、商品截图/隐私/EULA/审核备注与最终包匹配。仓库具备配置和文案不等于在线配置已验收，本轮未登录 App Store Connect、未部署或修改审核账号。
- 收口顺序：固定最终生产候选和后端版本 → 支付部署/授权闭环 → 最终构建与针对性真机签收 → 上架资料核对。个人 AI 文案池仍为 2.0，不以补新功能扩大 1.0 范围。整体封版结论仍待以上证据，不改变各实现任务 CODE_DONE，也不冒充全量 VERIFIED。

### 8.7 2026-09-20 新沙盒账号会员流程用户签收

- 用户确认通过：新沙盒账号购买月度会员 → 升级永久会员 → 从 App 打开管理订阅 → 手动取消旧订阅；取消后永久会员资格正常。该成功流程不再列为未测，手动取消已有直接用户证据。
- 后续用户确认：关闭重开 App、恢复购买后永久权益均正常；用户清单第 1 条（升级入口/年度场景）、第 6 条（到期续费或升级）也已确认通过，不再要求重复测试。
- 签收边界：Build、设备/iOS 与实际后端版本未提供，未确认失败分支，不据此标记整组会员或全量封版通过。具体子项见 `RELEASE_GATE_AND_DEVICE_MATRIX_v1.md` 第 8.2.3 节及总台账第 187/189 节。换新沙盒账号成功不等于已证明旧账号清历史/清库后的错误根因。

### 8.8 用户 16 项手测清单进度（2026-09-20）

编号对应本线程向用户列出的 16 项简化手测清单，不与本文原工程用例 ID 混用。用户此前确认通过：**1、3、4、6、8、9、11**。8（连续记账/保存）、9（首页滑动与删除）、11（照片查看与修改）的成功证据保留；随后第 8 项新增备注焦点故障反馈，仅该子项重新列入待复验，见第 8.9 节。

| 原编号 | 剩余待测 |
|---|---|
| 2 | 取消 App 内确认、取消 Apple 购买窗口及快速重复点击 |
| 5 | 登录后继续升级、取消登录、已有永久账号防重复购买 |
| 7 | Apple 商品实际加载失败后恢复网络重试；已缓存或本地备用价格可见不算触发失败场景 |
| 8（焦点子项） | “自己写一句”自动聚焦、连续删除/输入、备注与金额切换；先前保存通过记录保留 |
| 10 | AI 指令台批量保存、最后一条保存、清空结果 |
| 12 | 本地备份导出与恢复，核对账单及照片 |
| 13 | 首次定位/天气权限允许、拒绝、关闭或撤销 |
| 14 | 断网同步与联网重试、双设备新增修改删除 |
| 15 | 账号切换、交易归属与审核账号登录 |
| 16 | 深色、最大字号及 VoiceOver 下关键操作 |

以上通过依据为用户反馈，不替代未报告的开发专项、XCTest、Instruments 或最终生产包记录。下一步继续上述未确认项及新反馈的焦点复验，其他已测项不再重复要求。

### 8.9 2026-09-20 备注输入焦点回归（待新包复验）

用户报告新建点“自己写一句”未聚焦，已有备注删除一字后键盘收起。对应台账第 190 节，修复三个备注入口的 UIKit 焦点状态；不撤销第 8 项此前成功的保存证据。

| 操作 | 通过标准 | 状态 |
|---|---|---|
| 记账输入金额后点“自己写一句”；收键盘后再点；编辑弹窗展开备注 | 无需再点输入框即显示光标和键盘；记账页已展开也可重新聚焦；弹窗折叠后不迟到弹键盘 | 待复验 |
| 新建、历史编辑弹窗、首页聚焦编辑分别连续删除已有备注、删空再输入，中间位置修改，中文拼音选词 | 每一步仍可继续输入，键盘不自行收起，光标不跳到末尾；组词结束后正文和分类按原规则更新 | 待复验 |
| 备注与金额往返切换；输入后立即选分类、改时间、点完成或保存 | 焦点只在当前输入框；主动结束后不再自动弹起；最后文字不丢失，保存再打开一致 | 待复验 |
| 小屏记账页点“自己写一句”，中文候选栏/中英文键盘切换、收起重开，再切金额 | 整个备注输入框自动位于键盘上沿以上；高度变化后仍可见、不来回跳动；收起及切金额后恢复原区域，Tab 栏不被顶起 | 待复验 |

最后一行对应台账第 191 节局部键盘避让补充。记录新 Build、设备/iOS 与结果；Mac 执行 `CommittedRecordNoteFieldTests`（定义在 `NativeDemoAppTests/StateRegressionTests.swift` 内）及完整 XCTest。Windows 源码门禁及键盘几何测试不能证明真实键盘或中文输入法已通过。

### 8.10 2026-09-21 生产候选基线固定（本节取代 8.1、8.6 中的分支/commit 快照）

2026-09-21 已将测试分支同步进生产分支。本节是后续一切真机签收的唯一候选基线；**在此之前按 8.1/8.6 旧快照进行或计划的签收一律作废**，需在新候选上重做。

- 生产候选分支/commit：`xuzhang1.0-release-2026@77b90a33f756230df157bae8701cf153eae477b2`（本地与远端一致，已用只读 `git ls-remote` 核对）。
- 测试分支：`feature/xuzhangapp-staging@2aeb0157ad6f48904e836e2096221dcc4932a2da`。
- 同步方式：以 `feature/xuzhangapp-staging` 合并进生产分支，合并提交 `77b90a3 Sync staging into production release branch`；16 处冲突全部取测试分支侧（生产侧为空或是同一功能的上一代实现）。相对原生产 `b60bf71`，净差异 48 个文件、+3172/−380，无删除。
- 版本号：`Info.plist` 仍为 `CFBundleShortVersionString 1.0` / `CFBundleVersion 10`。**版本号未随本次同步变更，因此不能凭版本号区分新旧候选**，必须按 commit 核对（见第一节 BUILD-01 与签收表）。
- 生产配置隔离（本次同步的硬约束，已核对）：`project.pbxproj` 保持生产侧，不含 `SWIFT_ACTIVE_COMPILATION_CONDITIONS = "STAGING $(inherited)"`；`AppSettings.swift` 的 `#if STAGING` 在生产构建下编译走 `#else`，请求 `https://api.xuzhangapp.com`。天气/定位默认值已确认为 `false`（`AppSettings.default` 与 Codable 解码回退两处一致）。
- 本次同步带入生产分支的用户可见变化：Apple JWS 签名链验签（后端）、IAP 环境门禁与篡改反向测试、封面模板按场景种类数解锁、`Info.plist` 位置权限文案更新、首次定位/联网备份修复、备注输入 UIKit 焦点与键盘避让修复。
- 已在 Windows 侧执行并通过：`record_continuous_intent` / `quick_note` / `amount_input` / `home_swipe` / `first_use_permissions_sync` / `record_preview_feedback` 六项回归，全部 lint 专项，`validate_release_gate.py`，以及后端九个 `verify-*.mjs`（含新增 `verify-iap-jws-signature`）。**这些都不能替代 Xcode 编译、XCTest、Archive、StoreKit 真机交易与 Instruments。**
- 尚未执行（本机无 Swift 工具链）：Debug/Release build、`NativeDemoAppTests` 全部 XCTest（含本次新增的 `LaunchCoverTemplateTests` 场景闸门用例与 `StateRegressionTests` 键盘避让用例）、生产 Archive 与实际产物的 commit/域名核对。

签收前必须先确认：待测包的实际 commit 等于 `77b90a3`，且该构建的请求只到生产域名。两者任一不符，本轮签收无效。
