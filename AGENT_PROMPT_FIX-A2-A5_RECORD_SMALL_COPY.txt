你在 xuzhangapp 实现 Fix A2～A5 — 记账小体验四连修（小 PR · 4 文件）。

================================================================================
边界
================================================================================

只做下列 4 项，禁止改文案引擎 tier 规则、PlaybackService、Stats/Insight、git commit。

================================================================================
A2 · 往年账单列表显示年份
================================================================================

文件：NativeDemoApp/Models/HomeItem.swift（Date.zhBillDateTime）

规则：今年 →「M月d日 HH:mm」；非今年 →「yyyy年M月d日 HH:mm」
OCR 解析年份已存对，只改展示一处全局生效。

================================================================================
A3 · OCR 状态小字随待整理清空
================================================================================

文件：NativeDemoApp/ViewModels/HomeViewModel.swift

现象：导入后 ocrStatus 写「已导入 N 条…」，clearResolvedOCRDrafts 后仍显示。
修复：待整理 drafts 全部清空/解决后 ocrStatus = ""（或等价：无 pending 时清）。

================================================================================
A4 · 写细节删空不再被预填抢写
================================================================================

文件：HomeViewModel.refreshRecordPrefill + RecordView.onChange(inputTitle)

根因：用户删空 inputTitle → onChange → refreshRecordPrefill → trimmedNote.isEmpty 时 inputTitle = habitTitle。

修复：标题 onChange 时 refresh 但不 applySuggestedTitle；仅金额/日期变更时才允许自动填标题。

================================================================================
A5 · 过滤 OCR 脏时间标题进习惯池
================================================================================

文件：NativeDemoApp/Services/RecordPrefillService.swift isHabitTitle

拒绝形如：08:38、05-20 08:、M月d日 等时间/状态栏误抓串。

================================================================================
验收
================================================================================

[ ] 2024 年账单列表显示带年份
[ ] 待整理清空后 Record 页 OCR 小字消失
[ ] 写细节删到空不再弹回 habit 标题
[ ] 脏 OCR 时间不会成为预填标题
[ ] 金额变更仍正常预填分类/标题

@NativeDemoApp/Models/HomeItem.swift
@NativeDemoApp/ViewModels/HomeViewModel.swift
@NativeDemoApp/Views/RecordView.swift
@NativeDemoApp/Services/RecordPrefillService.swift
