# 云端照片备份边界 v1

> 对应台账：`DATA-04`
> 决策日期：2026-07-15
> 结论：采用方案 B——云端暂不备份记忆照片，提供含可用照片的本地导出。

## 1. 为什么选择方案 B

当前真实能力只支持 `/v1/ledger` 的 JSON 账单字段同步：iOS `LedgerDTO` 和后端 ledger payload 都没有图片字段，后端也没有对象存储、图片上传/下载、配额、过期清理、内容类型校验或断点恢复能力。

如果直接把界面写成“换机备份”，用户会合理地理解为照片也能在新设备恢复；这与真实能力不符。现在贸然选择方案 A 还会引入以下未完成边界：

- 照片隐私、加密和访问控制。
- 大文件上传、弱网重试、后台任务和断点续传。
- 服务端容量/会员配额与删除策略。
- 下载校验、重复文件、缺图和跨设备冲突。
- 账号注销、云端账本删除时的照片级联删除与审计。

因此本版本先保证承诺准确：云端只同步账单字段；记忆照片仅保存在本机，换机/卸载前由用户手动导出本地完整备份。

## 2. 用户承诺

所有云端入口统一说明：

- 会同步：金额、分类、备注、日期、来源、编辑标记、草稿状态、记忆上下文、场景包等现有 `LedgerDTO` 字段，以及会员/账号状态。
- 不会同步：记忆照片、OCR 原始图片、分享图和本地图片锚点文件。
- 云端合并：仍按现有更新时间规则处理账单字段，不修改冲突结论。
- 删除云端数据：只删除服务器账单字段；本机照片不受影响。
- 清空本机：会删除本机记录及对应照片；云端无法恢复照片。

禁止继续使用没有边界说明的“换机备份后都能找回”“完整账本已在云端”等表达。

## 3. 本地导出格式

设置页“备份与联网”提供 `.xuzhangbackup` 包，内容为：

```text
叙账本地备份-<date>.xuzhangbackup/
├─ manifest.json
├─ ledger.json
├─ README.txt
└─ images/
   └─ <record-id>/
      └─ <sha256>.jpg
```

- `ledger.json` 只保存账单字段、照片顺序、封面和稳定引用，不嵌入 Base64 图片。
- `images/` 保存导出时可读取的照片；相同内容文件只保存一份，但引用顺序仍可重复。
- `manifest.json` 明确记录记录数、照片引用数、实际文件数、缺图数和 `cloudPhotoBackupSupported: false`。
- 遇到已有稳定引用但文件缺失时，账单仍可导出，并明确报告未写入的缺图数量；既没有数据也没有稳定引用时禁止声称导出完整成功。

本任务只提供本地导出，不改变生产存储或云端 DTO。自动导入/恢复可在后续独立任务中基于版本、校验、合并和覆盖确认完整设计，不能隐式复用现有云端合并规则。

## 4. 回滚与后续升级

- 文案/导出功能可独立回滚，不影响本地 SQLite、照片文件和现有云端账单同步。
- 将来若升级到方案 A，必须先完成对象存储、隐私与配额设计、上传/下载失败恢复、删除级联、跨设备冲突和真机弱网验收，再把 `cloudPhotoBackupSupported` 改为 `true`。
- 在方案 A 真正上线前，任何会员页和设置页都继续使用本文件定义的方案 B 承诺。
