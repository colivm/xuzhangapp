# 记账连续操作：原因核对与修改方案

日期：2026-09-18。第 1–7 节为先前只读诊断与方案；用户随后授权实施，当前代码与验证进展见第 8 节。尚未交付新安装包或完成真机签收。

本次用户要求先说明原因，再提出细致方案。核对对象是用户提供的 11 张截图、当前 staging HEAD `58e4975`、工作区已有未提交补丁，以及新建、预览、快捷备注、保存、两个历史编辑入口。没有修改业务代码、测试、发布配置或用户账本，没有提交推送。

用户随后确认的边界：**“先看候选，再采用”仅限真实手写备注后的润色。其他情况不改操作方式。** 空备注帮写、纯系统文案换句、快捷备注点选、换角度保留现状；分类推荐可见性与历史编辑的意图延续仍是本次原请求中分别需要修正的问题。

用户最新澄清：**分类以最后一次明确用户意图为准。先选购物、后手写夜宵，应在首次保存前变成餐饮；先写夜宵、后主动选购物，才保留购物。** 本文已据此修正前半轮“手选分类永远优先”的误解。自动推荐、生成文案、候选刷新、保存按钮与无修改更新不算新的分类意图。

## 1. 结论与证据边界

问题由三处确定的逻辑分歧串起来：

1. 新建把人工分类锁当作永远优先，后来的明确手写意图不能覆盖；编辑更新又忽略既有分类决策，重新按备注判断。两个阶段规则相反。
2. 换句直接替换输入备注，再把操作意图改成“分类”；预览标题、生成句和情绪标签又分别作兼容判断。
3. 已提交版本把分类显示绑定到备注预览层级，因此能先显示分类推荐文字，却只显示日期。工作区第 171 节补丁已经处理这一点，但尚未提交，不能视为手机包已交付和验收。

“购物 + 夜宵”可以共存，但是否应为购物取决于操作顺序。用户本次是先选购物、后写夜宵，因此首次存成购物已经违背最后意图；编辑后才变餐饮只是另一条解析路径碰巧采用了备注，不能算正确修复。反向顺序最后主动选购物时，无修改更新就必须继续保留购物。

截图 2、3、5 的上方“日常添置”与输入框的餐饮句不一致，截图 4 又暂时一致；截图 6–8、11 显示“夜宵 + 购物”，截图 9、10 显示更新后的“夜宵 + 餐饮”。这些视觉事实与代码路径吻合。但截图不是完整录像，不能还原保存前最后一次输入，也不能据此断言某一句生成备注最终被存成了“夜宵”。截图没有足够的 Archive commit / Build 映射证据。

## 2. 原因逐项定位

### 2.1 新建阻断后写意图，历史编辑又丢失既有决策

- `RecordView.swift:607–617` 的 `manualNoteOverrideCategory` 先检查 `!categoryLockedByUser`，选过分类后就直接返回 nil；因此后写的明确餐饮备注根本没有机会变更类别。
- `HomeViewModel.swift:3239` 的 `preferNoteSemanticsForCurrentDraft` 仍有解除旧分类锁的实现，但当前没有调用。仅将 `lastDraftIntent` 设为 note 不会释放实际锁。
- `HomeItem.swift:1305` 的局部 `strongManualNoteOverrideRules` 又不含“夜宵/宵夜”等词，而通用语义词典能识别夜宵为餐饮。不能只去掉一道 guard 就宣称完整解决；同一手写意图必须使用一致的明确证据策略。

- `NativeDemoApp/ViewModels/HomeViewModel.swift:1449`：新建取得 `categoryLockedByUser`；1486 行将其存为 `userEditedCategory = true`。
- `NativeDemoApp/Services/RecordDraftResolutionService.swift:351`：解析器收到锁就沿用购物，没有锁才按品牌/语义重新决定。
- `NativeDemoApp/Views/RecordEditSheet.swift:104`：编辑解析只判断“本次分类与原值不同”或品牌冲突，没有继承 `item.userEditedCategory`。
- `NativeDemoApp/ViewModels/HomeViewModel.swift:2078`：最终更新再次只传 `categoryWasEdited || categoryOverridesBrand`，同样漏掉原记录人工锁。“夜宵”没有品牌，分类未改，便被当作无锁输入，解析为餐饮。
- VM 2102 行随后又保留 `userEditedCategory = true`，于是最终数据变成“系统改了分类，仍声称这是用户选择”。

字段并未丢失于存储或同步。`HomeItem.swift:916,956`、`LedgerMetadataStore.swift:430,611`、`LedgerSyncService.swift:95,164` 已读写人工分类标记。根因同时包括新建的优先级错误、草稿未正确更新最终来源，以及编辑不尊重已保存基线。单纯让编辑永远尊重旧锁，会把首次存错的购物固定下来，不能满足本次用户规则。

### 2.1.1 以前确实优化过，后来覆盖逻辑被改掉

Git 可核对的历史：

- `48db02e`（2026-06-24，Polish record semantics and playback boundaries）存在 `shouldManualNoteOverrideSelectedCategory`：后来的手写备注有明确且与当前类别冲突的语义时，调用 `preferNoteSemanticsForCurrentDraft()`，解除先前分类锁。
- `9f63042`（2026-06-26，Tighten life semantic boundaries）将它改为 `manualNoteOverrideCategory`，新增 `guard !homeViewModel.categoryLockedByUser else { return nil }`，并将解除锁调用替换成 `applyRecommendedCategory`。选过分类后，手写覆盖路径由此被阻断。
- 最近第 151–153 节又把“手写不解锁”作为沿用边界；第 161、166、171 节分别修快捷备注、情绪和分类可见性，没有恢复最后明确意图顺序。

因此，用户关于“这条逻辑早优化过”的记忆有版本证据；当前不是完整保留了那次行为。最新用户澄清优先于台账此前沿用的冲突边界。本轮只更新方案，不改历史提交、全局词典或产品代码。

### 2.1.2 前几次修改各自在保护什么

用户进一步要求复盘前几轮的修改意图，不能仅按当前现象推翻旧保护。下面区分文档直接证据与根据 diff 的推断：

| 修改 | 当时要解决的问题/保护的行为 | 当时实际采取的办法 | 本次应保留与补足 |
|---|---|---|---|
| `d0845f6`，6 月 23 日 | 从 diff 可确认，选角度时保留手写原话、换句沿原话锚点、场景参与预览和保存 | 引入原话锚点；保留原话时不强改 note 意图，但分类锁不再依赖 lastDraftIntent!=note | 保留原话及场景事实；lastDraftIntent 当时已经无法单独表示最后分类选择，不能简单恢复对该枚举的旧判断 |
| `48db02e`，6 月 24 日 | 回归文档明确要求“基于备注换句不跳场景”“快捷备注不能错误改餐饮”“免费不能隐式调用锁定包” | 真手写冲突才解除先前分类锁；快捷备注使用 suppressNextNoteSemanticUnlock，不伪装手写；限制隐式包来源 | 这是区分用户输入与系统填字的早期尝试，应保留区别，不能恢复成任何文本变化都解锁 |
| `9f63042`，6 月 26 日 | 提交题为收紧语义边界；同时增加强备注名单、4 元茶叶蛋/手机充电器回归，禁止空备注泛化推荐、20 元内近似金额习惯混用，收紧通勤/泛日用线索 | 后写覆盖改为仅无人工锁时可执行，并要求唯一强语义类别；取消真实解除锁调用 | “避免弱词/金额猜测推翻用户决定”与该 diff 一致，但提交无长说明，不能断言锁 guard 对应哪个用户反馈。保护范围过宽，连后来的明确手写也挡住了；保留强证据/模糊不猜，放开真实更晚的明确意图 |
| `5516a67`，9 月 17 日第 153 节 | 台账直接记录：低可信候选说已推荐但实际没采用；习惯/常用金额互相竞争；生成句被当成新事实导致类别漂移 | 统一 adoptedPrefillResult；生成来源保护；旧请求立即失效；预览与保存识别生成来源 | 全部保留。人情“请客朋友”被系统换成“一起吃顿饭”，不能因此变餐饮；但用户后来真手写新的消费事实需重新裁决。该轮明确沿用了“手写不解锁”边界，没有解决先后顺序 |
| `1676b19`，9 月 17 日第 161/162 节 | 台账直接记录：日用快捷句“便利店补一袋日常”反过来命中餐饮；候选假定加班/出差；金额逐键昂贵重算 | 快捷句统一登记生成来源、中性模板/同场景过滤、后台有界历史池；金额 180ms 合并与动作前 flush | 保留快捷点选为系统候选来源、原一步采用、原排序/门槛及输入性能；不能因恢复手写意图，把快捷句又当成真实手写去解锁 |
| `c50c360`、`4a27e6e`，9 月 17 日第 164/166 节 | 台账直接记录：三餐/咖啡情绪候选不足，显示可切却无可用变化 | 补同场景候选、统一候选/展示/保存事实守卫，限制点击区域 | 这些处理的是情绪胶囊，不是大字备注的来源或编辑分类。保留事实/奖励守卫，不归因成它们改坏分类 |
| 未提交第 171 节 | 台账直接记录：推荐已采用却仅显示日期、推荐关键路径延迟、夜间通勤单候选 | 分类显示从备注层级拆开；快捷历史与分类预填分开；局部夜间候选 | 保留并核对实际构建交付。这项没有改手写锁或编辑更新，也没有真正交付到两个分支提交版本 |

历史意图并不互斥：**自动/生成结果不能覆盖人的决定，而人的下一次明确操作可以改变自己的决定。** 当前是用一个布尔分类锁和一个混合用途的 lastDraftIntent 承担两者，漏掉了“谁产生了这段文本、哪个用户事件更晚”。此外，保存后的编辑流程没有把已保存结果当基线，所以无操作也重跑识别。

不应采取的修法：

- 不能直接撤回 6 月 26 日整包：会丢掉强语义、空备注、小金额和生活线索收紧等有效保护。
- 不能只删除 View 的锁 guard：VM 的 `applyRecommendedCategory` 仍拒绝有锁类别，局部强备注名单还缺夜宵，预览/提交链仍可能分歧。
- 不能对所有 `inputTitle` 变化调用解除锁：程序帮写、快捷句、载入旧记录和迟到回调也会改这个值。
- 不能让所有原样编辑都重新按备注分类，也不能只把原人工锁永久继承：前者无故改账，后者固定新建阶段已经存错的类别。

正确增量是保留前述各项保护，只将“真实用户明确意图”的来源和先后从系统文本变化中分离，并让新建、预览、提交与再次编辑共用这份裁决。

### 2.1.3 编辑为什么会重解析，旧保护为何没有闭合

编辑重解析也有历史原因，不能直接整段取消：

| 修改 | 可核对的历史事实 | 本次保留的能力与需要补足的条件 |
|---|---|---|
| `f89b494`，6 月 8 日 | `updateItem` 从直接保存改为依据品牌重求分类、标题、情绪 | 真实修改商户或相关事实后，应更新衍生结果。没有文档证明当时希望无修改也重新分类，不能把无条件调用视为明确产品要求 |
| `5567e60`，6 月 11 日 | 加入 `categoryWasEdited || categoryOverridesBrand`，防止品牌默认类别盖过用户选择 | 例如旧品牌默认餐饮，用户明确选择日用，品牌不能抢回类别；此时还没有持久化人工标记，只能用值差异/品牌冲突代理判断 |
| `16d702a`，6 月 15 日；`d0f8237`、`2532ceb`，6 月 16 日 | 先将旧锁条件迁入统一解析器，再增加非品牌备注语义分类及持久化人工纠正标记；编辑在解析之后延续标记，没有把它接回解析入口 | 共用解析和纠错学习均保留；缺口是旧判断未随新数据来源升级，无品牌“夜宵”可在原样更新时改类，然后又保留人工标记 |
| `92c8210`，6 月 17 日；`eb5da74`，6 月 22 日 | 编辑情绪预览接入解析；随后字段未变时展示原情绪，沿用记录存储的天气证据 | 保留真实修改后的预览更新及原天气；“无修改保留”还须覆盖保存，而非只保护屏幕展示 |
| `8935376`，6 月 25 日；`0fc1de3`，7 月 6 日 | 真改标题后清掉不匹配旧品牌；解析器保留手写原话并返回受抑制后的品牌绑定 | 保留品牌解绑和原话。编辑应根据真实改动重算，不能因为原样更新修复而禁止新备注触发这些必要变化 |
| `abd49ce`，7 月 16 日；`bc465cb`，8 月 27 日 | 前者只迁出完整编辑器类型；后者修“手动改分类覆盖原标题”，台账第 71 节有用户原反馈 | 文件拆分不是这次缺陷的引入点。保留“徐记花甲鸡爪｜宿豫店”等原话，不能为凑分类一致性改写成购物模板 |

更具体的边界：旧购物记录最后真手写“瑞幸”，新备注应有机会裁决餐饮，不能因为 `购物 != 瑞幸默认餐饮` 又被 `categoryOverridesBrand` 锁回购物；若随后主动选择购物，购物才重新胜出。品牌冲突判断服务于最终用户意图，不能替代事件顺序。

人工纠错学习同样需要修正来源：备注使购物变餐饮，不等于用户点击过餐饮，不能仅凭 category 值变化写入 `userEditedCategory = true` 或累计一次人工纠错。旧标记是进入编辑时的基线；更晚明确手写取代旧选择后，旧纠正来源不能继续参与当前草稿。以上历史事实来自实际 diff；除台账、回归文档或源码注释明确说明的目标外，对修改目的的描述为行为推断，不冒充当时作者或用户的原始指令。

### 2.2 编辑界面所见与提交行为不同

`RecordEditSheet.swift:67` 在字段没变时显示原情绪，327 行直接显示当前选择的购物分类；用户看到的仍是原记录。但保存按钮在 498–508 行无条件重组记录、更新修改时间、重新求品牌和情绪，再交给 VM 重新解析。

即使只补分类锁，VM 2089–2097 行无条件重算情绪的问题仍在：用户之前选中的情绪表达可能在无修改更新时变回默认。2118–2123 行还会更新时间、落库、刷新回放和触发上传。

另一个入口 `FocusedRecordEditor.swift:576` 同样在无修改时重组记录并调用 `updateItem`。修复不能只覆盖截图中的 Sheet。

### 2.3 换句同时触碰文本、来源和意图，但预览另有一套选择

页面上有四个不同区域：大字备注预览、小胶囊情绪标签、保存按钮下方的备注输入框、输入框下方的快捷备注候选。输入框和预览本应描述同一份待保存备注；候选只是供选择，不应成为另一份实际值。

实际链路：

1. `RecordView.swift:1360,1381` 的会员/免费入口都读取当前备注及手写锚点。
2. 1428 行通过 `applyGeneratedRecordTitle` 换句；VM 3256 行直接修改 `inputTitle`，因此下方输入框会被替换。
3. View 1378、1397 行把 `lastDraftIntent` 改成 `.category`，即使用户只是请求改写句子，也会丢失原先“当前是手写备注”的判断。
4. View 746 行的大字预览不是直接展示最终待保存结果：锁定分类时，还要求备注与分类兼容，或 `lastDraftIntent == .note`；不满足便显示分类兜底标题。
5. 换句保留“夜宵”锚点，通过 1540、1679 行附近的分支生成餐饮句；分类仍受人工锁保护为购物。此时输入框是一句餐饮表达，上方却可能退回购物文案。

只读词典匹配结果：

| 句子 | 分类关键词命中 | 对截图的解释 |
|---|---|---|
| 夜宵 | 餐饮：夜宵 | 手写时可以原样显示；编辑漏锁后会触发餐饮重分类 |
| 晚点补点能量 | 餐饮：补点能量 | 换句后与购物不兼容，上方退回“日常添置” |
| 夜里吃点热乎的 | 餐饮：热乎 | 同上 |
| 这顿先记下 | 无 | 被兼容策略视为可保留，上下暂时相同 |
| 日常添置 | 无 | 32 元购物的兜底标题 |

无关键词默认兼容见 `HomeItem.swift:1430`；购物兜底见 1550 行附近。这不是随机文案服务或网络回包导致的现象。

保存解析还有一道标题修复：`RecordDraftResolutionService.swift:380` 只在未人工锁类时直接保留生成来源文本；人工锁类且文案冲突时可能再次修复标题。预览的标题、输入框的原始值和最终落库解析并非同一个结果。

### 2.4 快捷备注和情绪为何仍像购物

`HomeViewModel.noteSuggestions`（3309 行附近）按选中分类、时段和历史构造候选池，随后用 `inputTitle` 筛选部分事实。`RecordQuickNotePolicy.Evidence` 主要检查品牌、具体食物、母婴/宠物，不等价于完整“手写夜宵”场景校验；购物默认模板仍可能出现。

生成动作会改变输入锚点并失效预填，候选的派生输入也跟着变化；已有截图中三枚购物快捷候选实际保持为购物，不能把它们全部描述成每次都轮换。

情绪标签又根据最终分类及解析标题生成，因此可能是“这件小物挺实用”，与输入框的餐饮句冲突。不能只修大字标题而留着这层矛盾，也不能为让文案统一就把用户分类改成餐饮。

### 2.5 已有推荐文字，分类仍不显示

当前 HEAD 的 `RecordView.previewCardMeta` 对 `.whisper` 强制仅返回日期；`recordLearningHint` 却允许显示“这个时间附近常是某分类”。空备注还会屏蔽某些预填来源对预览层级的提升，所以分类直到点“帮我写一句”、切到 `.confirm` 才出现。

这不是推荐文字挤占了分类的位置。工作区已有第 171 节修复：`RecordPreviewTier.showsCategory` 与 `hasCurrentRecordCategoryRecommendation` 将分类可见性与备注是否生成分开。该差异尚未进入 HEAD，且本次没有手机包映射证据，不能说它已在用户当前安装包生效，也不能把它算作已完成真机验收。

### 2.6 为什么前面检查通过仍漏掉

现有 XCTest 有“传入 true 的分类锁胜出”和“预览/新建保留生成分类”，但没有穿过两个编辑入口与 `updateItem` 的完整往返。测试把正确的锁直接传给解析器，无法发现调用方没有读取锁。

本轮运行四项原专项均退出 0：`record_preview_feedback_regression.py`、`quick_note_regression.py`、`amount_input_regression.py`、`meal_emotion_regression.py`。它们主要核对源码合同与词典，不执行 SwiftUI 连续操作。这能解释覆盖缺口，不能证明问题不存在或已经修复。

## 3. 建议固定的产品规则

建议把以下规则作为本次独立修复的验收合同，再实现；不要继续分别给按钮补互相竞争的条件。

| 对象 | 应遵守的规则 |
|---|---|
| 分类 | 最后一次明确用户意图优先：主动选类与后来明确手写语义按真实事件顺序裁决 |
| 备注 | 用户原话独立保留；分类不兼容不是抹掉原话的理由 |
| 自动推荐 | 不能盖过最后明确用户意图；生成句不算用户新增消费事实 |
| 换一句 | 更换表达，不改变分类、金额、日期或场景包身份 |
| 快捷备注 | 未点击只是候选；候选刷新不得写回备注或分类 |
| 情绪标签 | 只改变表达；不能反过来制造消费、通勤、天气或奖励事实 |
| 预览与保存 | 同一草稿修订使用同一份最终解析值；不能保存时才悄悄换标题或分类 |
| 无修改更新 | 原记录保持原样，包括情绪、来源、修改时间；不写库、不上传 |

操作顺序必须可验证：购物→手写夜宵为餐饮；夜宵→购物为购物；购物→夜宵→再选购物仍为购物。未提供明确新语义的模糊备注不重分类。系统生成的“换一句”不推进用户意图顺序。

## 4. 具体交互方案

### 4.1 输入金额与展示推荐

- 有效金额时保留分类入口；分类的可见性不依赖帮写、备注是否为空、是否展开编辑器。
- 推荐已采纳时，同一结果同时发布分类值、推荐依据、分类网格标记。出现“这个时间附近常是餐饮”，分类行就必须是餐饮。
- 无可靠依据时只显示实际将保存的当前分类，例如“其他”；不把候选说成已采用，不为了出现分类而先生成备注。
- 保留已有 180ms 金额合并和后台历史计算。更换金额、日期、备注或手选分类后，使旧请求失效；不要增加同步扫描或让保存等待历史推荐。
- 推荐尚未就绪而立即保存时，保存当前已展示的合法分类；不能等后台完成后再补改已保存记录。

### 4.2 手选购物，再写夜宵

- 点击购物：设为本笔明确分类，收起分类面板；不改已有备注。
- 后来真实输入夜宵：备注预览显示夜宵，同时依据新的明确意图改为餐饮；清除已经被取代的旧购物分类锁/纠正来源，旧推荐异步回调不能恢复购物。
- 分类行此时显示餐饮；相关快捷候选、情绪按已裁决餐饮同步更新，不能留下购物文案。原话不被模板替换。
- 如果随后又主动点购物：这次分类选择成为最新意图，显示“购物 · 已手动选择”，仍保留夜宵原话；后续帮写、润色与无修改更新不得再按旧备注把它改回餐饮。
- 仅在这种“最后主动选类，明确备注仍冲突”的手写分支，抑制会编造购物事实的快捷模板/情绪；可不显示无可靠内容的候选/标签。其他分支候选规则不变。

### 4.3 明确“帮写 / 换句 / 润色”的作用

| 备注状态 | 按钮 | 行为 |
|---|---|---|
| 空备注 | 帮我写一句 | 基于已采用分类及真实上下文生成，原有一步操作保留 |
| 纯系统生成备注 | 换一句 | 保留现有直接轮换，不添加候选确认、撤销或其他新操作 |
| 用户手写备注，或基于手写内容已采纳的改写 | 润色这句 | 原文保留，展示一条明确标注的候选；点击“用这句”才替换，支持“保留原文”及撤销 |

用户已在本轮明确选择“先看候选，再采用”，随后限定“只能是手写备注后的，其他情况不要动”；方案据此确定，尚未实现。只有真实手写输入或继续润色该手写内容才进入此分支；只打开输入框、焦点进入、程序填充、快捷备注点选均不算手写。手写分支采用行内候选，不弹模态确认，也不让候选冒充已保存预览。例：原文“夜宵”，候选“夜宵这笔记下”；确认前大字预览和输入框仍为“夜宵”。确认后两者同时变成候选，分类沿用最近明确意图的裁决：本次购物→夜宵是餐饮，反向顺序则是购物。生成/采纳同义改写本身不算新的分类意图。

润色只改变说法，不增加“加班、晚归、热食、便利店、某商户”等原文没有的事实。无法安全改写时保留原文。原始手写锚点在本草稿内保留，不用第 N 次生成结果逐步替代事实来源。

快捷备注保留现有直接采用方式，不加确认或撤销步骤；单纯重新计算/刷新候选不能采用它。用户手动修改已生成/已点选备注的内容后，才转入手写分支。继续润色已经采纳的手写改写仍保留原始手写来源；主动选用无手写关联的快捷备注则回到原有快捷备注分支。纯生成句、快捷句与手写衍生改写应区分来源，不把生成内容伪装成新手写证据。

### 4.4 分类、备注、候选、情绪分别更新

- 新增内存层的字段来源/修订信息：分类明确选择事件序号、手写语义事件序号、备注来源、原始事实锚点、未采纳候选、当前草稿修订号。用单调事件序号比较先后，不依赖墙钟时间或 SwiftUI observer 到达顺序。沿用现有模型小范围补足，不重构整个页面。
- `lastDraftIntent` 不能再同时代表“用户最近按了什么”与“备注是否受保护”；编辑器是否展开、是否有焦点也不应决定备注所有权。
- 类别选择与真实手写提交分别登记用户事件；明确手写可在其事件发生时裁决分类，模糊/混合多义文本则保留现值，不因一个弱关键词覆盖。输入法尚在组词时不发布半句推断；识别稳定后发布，立即保存必须先同步裁决当前已提交文本。生成/润色只修改表达，不写明确意图序号；候选刷新不修改实际字段。
- 分类推断完成时仍检查草稿修订和事件序号；稍后手选的分类不能被较早发起的手写识别任务覆盖。反之，晚于分类选择的明确手写也不能被残留布尔锁阻断。
- 本次保证手选、手写、换句、保存及再次编辑之间的字段来源延续。台账已单列的 D3“只打开编辑器被当清空”、D4“清空金额丢手写意图”仍是相邻已知问题，作为对照观察登记，不在本方案中默认合并实现；日期面板与整个草稿生命周期保持原范围。
- 在手写润色分支，改金额、日期、分类、备注或实际角度后，失效旧润色候选；纯金额格式变化按等价语义处理。原有情绪选择失效规则保持，不借此调整其他分支。
- 新的一笔清空内存来源、候选和撤销；切 Tab 返回保留当前未提交草稿。会员与免费走同一文本状态策略，权益判定维持原规则。

### 4.5 统一所见与保存值

- 在现有 `RecordDraftResolutionService` 旁收敛一个可复用的当前草稿结果：备注、分类、情绪、品牌、场景及来源。大字标题不再单独用分类兼容性兜底，覆盖正在编辑的真实备注。
- 将“保留已确认文本”和“是否用户手写”分开。用户采纳润色后可以保留该文本，无需伪造 `userEditedTitle = true` 或改变历史学习权重。
- 同一修订下，预览和提交共用相同输入与纯策略；保存时按最新输入验证，缓存必须核对修订，不能把旧结果写入。
- 新建解析先完成最终意图裁决，再使用该类别和实际备注；只有“最后主动选类但仍保留冲突备注”才允许分类/语义并存，情绪缺少共同证据时可为空。空标签在现有 `HomeItem.displayEmotionTag` 会保持为空，但新建、记忆增强、编辑展示/更新均需验证不会重新填入冲突文案。
- 上述情绪抑制只作用于本次明确冲突草稿/编辑流程；不批量改写存量情绪，不改全局生活线索、奖励及词典识别规则。

### 4.6 保存后再编辑

两个编辑入口共用一份编辑决策，输入为原记录、本次真实字段改动及明确选择事件。

- 打开编辑器以已保存记录为基线，不重演历史操作，也不把已有备注当作刚输入。继承最终分类及来源；本次主动选类、真正手写改动再产生新的明确意图事件，沿用与新建相同的顺序裁决。点选原分类也应算确认，不能只用新旧值不等判断。
- 完全无修改直接成功关闭，VM 亦提供一致保护；不运行分类/情绪解析，不更新时间、不落库、不刷新/上传。
- 无修改判断以编辑基线和字段变更为准。不能照搬现有金额 `<0.005`、日期 `<1 秒` 的展示容差；金额初始格式化到两位小数也不能改掉旧记录原始精度。未操作字段原样沿用原值。
- 点选相同分类但首次建立人工确认，不属于“完全无修改”：允许仅持久化该明确来源变更，文案和其他事实不重算。
- 仅改金额、仅改日期：保留原分类。相关情绪是否失效按真实上下文变化和既有事实守卫决定。
- 修改备注：本次真正新增明确语义，就是晚于原记录基线的新意图，可以取代此前分类；若用户随后再主动选类则以后者为准。模糊或未真正改变的备注不触发重分类。提交前显示最终裁决，不能只在点击更新时才变化。
- 主动改分类：保留备注原话，立即预览最终类别；更新纠正来源，按既有规则清理不适用场景/品牌。打开面板再取消不算改分类。旧品牌冲突保护不能阻断更晚真实手写的新品牌证据；只有最终主动选类胜出时，才用该保护压制冲突品牌。
- 保留照片、来源、记忆上下文和未变字段。`updateItem` 还有 OCR/图片等调用方，应通过明确编辑意图或独立编辑请求限定新规则，不能把所有更新永久锁类，也不能忽略其他调用方实际修改的图片/元数据。
- 不扫描并猜测修复已经被误改的历史记录。现有标记不足以恢复原分类；已受影响的记录需用户明确纠正，不能用备注批量改类。
- 持久化不需要完整操作日志：保存最终 category；只有最终胜出的意图是主动选类时才保持 userEditedCategory，手写取代它则清除已失效的分类锁/纠正来源。userEditedTitle 仍只表达真实手写来源。只增加内存事件顺序，不扩 DTO；旧记录无操作顺序，原样更新继续保留现状。

## 5. 建议文件范围与实现顺序

将其作为一个“记账新建至编辑一致性”定向任务，内部依次完成，不并行推进其他路线图任务。

| 顺序 | 文件范围 | 修改目标 |
|---|---|---|
| 1 | `HomeViewModel.swift`、`RecordView.swift`、`RecordEditSheet.swift`、`FocusedRecordEditor.swift`、`RecordDraftResolutionService.swift` | 统一最后明确意图裁决、最终来源保存及编辑基线；保护无修改更新，不把首次错误类别永久锁住 |
| 2 | `RecordView.swift`、现有草稿来源模型/服务、`LifeEntryPreviewCard.swift` | 仅真实手写分支区分来源、统一预览保存并提供润色候选采纳/撤销；其他按钮操作方式保持 |
| 3 | `HomeViewModel.swift` 的快捷备注策略、记录页情绪接入 | 仅手写分支用事实约束展示候选；冲突不编造购物文案；其他候选规则/排序与点选行为保持 |
| 4 | 已有 `RecordPreviewTier.swift`、`RecordView.swift`、VM 第 171 节补丁 | 在保留已有改动基础上确认已采用推荐与分类同显，不再依赖帮写；核对实际包来源 |
| 5 | `StateRegressionTests.swift`、专项补充、既有真机签收文档、全局台账 | 增加真实状态转换与往返测试，执行原门禁，记录 commit/Build/设备证据 |

不新增持久化字段，不迁移账本，不改全局分类词典、OCR/AI 导入规则、会员/场景包权益、首页框架、支付、两套环境配置和个人 AI 池。需要按用户最新要求明确替换“手写永不解分类锁”的旧局部合同；明确备注证据复用已存在的分类事实并补局部一致性策略，不能简单扩大全局关键词来修事件顺序。已有断言若固化旧优先级，应改为正反顺序/模糊文本/系统生成的真实行为断言并说明原因，不删测试来掩盖变化。

## 6. 连续验收矩阵

每一行都同时核对：输入框值、预览标题、分类/图标、情绪、快捷候选、提交后的原始字段、重新打开后的显示。不能只验证解析器返回值。

| 编号 | 操作序列 | 必须结果 |
|---|---|---|
| C01 | 32 → 手选购物 → 手写夜宵 → 保存 → 原样更新两次 | 首次保存前已为餐饮 + 夜宵；首次正常写入，随后两次原样更新均不写入/上传，首次保存后的情绪与修改时间保持不变 |
| C02 | C01 分别走 Sheet 与 Focused 编辑入口 | 行为一致 |
| C03 | 手写夜宵 → 润色候选 → 保留原文 → 保存 | 原话仍为夜宵，分类不变 |
| C04 | 手选购物 → 手写夜宵 → 润色 → 用这句 → 保存 → 原样更新 | 前后同一已采纳备注，最近手写裁决的餐饮持续保留；同义生成不产生新的分类意图 |
| C05 | C04 采纳后撤销，继续改字/再润色 | 原文可恢复；旧候选不覆盖新输入 |
| C06 | 购物 → 夜宵 → 连续请求 10 次候选 | 不冒出购物事实、早餐、加班或热食；原文未采纳前不变 |
| C07 | 无备注 → 帮写 → 连续换句 → 保存 | 原有一步帮写/直接轮换保持，没有新候选确认或撤销步骤；原有类别/来源守卫不退化 |
| C08 | 输入备注时后台快捷历史完成 | 只更新候选，不改备注/类别；手写冲突时不强塞购物模板 |
| C09 | 点击快捷备注 → 再手写 → 润色 | 快捷点选仍直接采用；真正手写修改后才出现候选采纳，不把程序填充/焦点变化算手写 |
| C10 | 手选购物 → 写夜宵 → 再选购物 → 仅改金额/日期 → 保存和编辑更新 | 最后主动选择的购物与原话保留；不能被旧手写推断反写 |
| C11 | 原记录人工锁购物 → 真正手写修改为夜宵/午饭/牛肉面 | 新明确手写意图生效为餐饮；如果随后再选购物则以购物为准，预览/保存/再编辑一致 |
| C12 | 原记录无人工锁 → 原样更新/仅改金额/仅改日期 | 类别不因旧标题重识别而变化 |
| C13 | 主动选购物 → 模糊备注“记一下”/多义混合文本 | 不强制猜新类别；明确夜宵餐饮为正例，弱/多义输入为反例 |
| C14 | 打开分类再取消；主动选原分类；主动选餐饮 | 三种事件有区别；取消零变化，同类确认只改必要来源，餐饮选择立即生效 |
| C15 | 自选情绪 → 保存 → 原样更新 | 选中标签不回默认；不触发新的生活印记/奖励 |
| C16 | 有可靠习惯推荐，空备注，无帮写 | 推荐文字与分类同时显示 |
| C17 | 低可信、无历史、计算中、旧金额结果迟到 | 不冒充已采纳推荐，不回写旧分类 |
| C18 | 快速输入/金额格式变化 → 立即帮写或保存 | 当前连续操作来源正确，旧异步任务不覆盖本次；无需等待可选历史池 |
| C19 | 只打开备注编辑器再关闭；清空金额后重输 | D3/D4 对照观察项：记录既有表现及是否新增退化，不把它们冒称已修或默认并入本次实现 |
| C20 | 切 Tab 返回；保存后记下一笔 | 前者保留当前来源与草稿；后者不继承锁/锚点/撤销候选 |
| C21 | 手选角度 → 写备注 → 换句；角度打开取消 | 不隐式换包/解锁或扩大免费权益；取消零应用 |
| C22 | 旧记录带品牌/照片/高精度金额/秒级日期 → 原样更新 | 原始值、附件和来源无格式化损失 |
| C23 | 购物 → 真手写瑞幸；再选购物；改备注解除旧品牌；OCR/图片更新调用回归 | 新手写品牌可裁决餐饮，再主动选购物则购物胜出；保持原品牌解绑和各调用方字段合同，不被编辑专用规则误伤；语义改类不伪记为主动分类纠错 |
| C24 | 免费/会员、深浅色、大字号、VoiceOver、离线 | 同一事实规则；操作语义清楚、候选区域可用，触控目标保留 |

除明确标注的 C19 相邻问题观察项外，上表为本次方案的通过条件。XCTest 必须覆盖 VM 编辑入口或抽取后的真实编辑状态转换，保存后通过持久化解码再编辑；加写入/上传计数断言验证无修改路径。SwiftUI 真机序列覆盖焦点、onChange、快速点击与异步迟到。原 Windows 全门禁和两环境检查继续完整执行，不能用新增专项替代。

## 7. 本轮实际验证、剩余风险与下一步

- 已验证：源码逐入口交叉审阅；HEAD/工作区差异；词典原数据命中核对；四项上述专项均通过。Windows 仅发现 Python，未发现 Swift/swiftc/xcodebuild。
- 未验证：Swift 编译、XCTest 执行、iPhone 连续 UI 操作、实际安装包包含哪些补丁。本轮不声称修复已完成。
- 既有未提交代码/测试、台账历史、签收文档、用户环境模板和素材均保留。仅新增本报告并在全局台账追加本次诊断。
- 下一步：以本文合同安排一个定向实现任务，先统一新建与编辑的最后明确意图及最终来源、保护无修改更新，再收敛手写备注/候选/预览，最后完成已有分类展示修复的实际包验收。不能先把新建阶段错误保存的购物永久锁住。开发阶段记录 CODE_DONE，只有所需 Mac/真机证据完成后才可 VERIFIED。个人 AI 池继续留在 2.0。
- 本轮结论修正：此前以“人工分类永久优先”为目标的建议不再采用。用户最新规则为最后一次明确意图，Git 历史已确认旧手写覆盖路径及随后移除；最终实现应恢复并补全该规则，同时保留用户已限定的“候选采纳仅手写后”。

## 8. 用户授权后的实现记录

用户在上述分析后要求“这次一次要改好”，本轮按已确认合同实施，对应台账第 176 节。先前章节保留为原因和验收依据，其中行号为诊断时快照。

- 新建和两个编辑器共用 `RecordExplicitIntentState`：真实手写和分类点击同步登记单调事件；分类锁从最终来源计算，删除另一个可独立变化的布尔锁及失去调用的解锁入口。已有关键词按具体词证据和唯一类别裁决，夜宵/午饭/瑞幸可生效，弱词及混合类别不强猜，未改全局词典或金额推荐门槛。
- `HomeViewModel` 区分真实手写、系统生成和手写衍生改写，保留原始手写锚点；系统生成不推进意图，手写取代旧选择时清除旧纠错来源。主动再选类保留文本来源；异步推荐不能覆盖最终明确决定。
- `RecordView` 只在手写分支提供行内候选、“用这句”和撤销；候选保留原事实，未采用不写草稿，上下文变化即失效。其他帮写、换句、快捷点选及角度入口保留操作方式。预览直接使用保存的解析构造器，冲突手写不制造购物情绪；手写后的快捷候选额外过滤餐次、商户、食物及环境新事实。
- 三个备注输入位置使用同一 `CommittedRecordNoteField`：只提交真实已上屏的输入，不把拼音组词中间态当语义；分类和保存前同步结束编辑。单纯打开关闭旧长备注不截断原值；适配动态字体及编辑入口原文字对齐。
- `RecordEditIntent` / `RecordEditPolicy` 固定首次进入的基线，预览与提交共享策略；四处 Home/Stats 回调传递编辑请求，VM 仅合并实际改动到最新账本行。完全无修改在写入权限检查、解析/时间戳、落库、统计事件、回放、上传前返回；非编辑调用保留原处理规则，只补全等幂等保护。未碰的高精度金额、秒级时间、原情绪、来源、照片保留；外部同步刷新不移动初始比较基线。
- 原品牌解绑、兼容品牌保留、原话保护、存储天气及自动照片语义更新继续用于真实改动。备注识别变类不伪记人工分类纠错；主动选原分类可仅建立来源确认，不重算其他事实。没有批量迁移或猜测修复历史账单。
- 新增 16 项 XCTest：14 项连续状态/解析/解码编辑往返/来源/精度/并发合并/候选事实测试，2 项 UIKit 已提交输入与旧长备注测试。新增 `scripts/record_continuous_intent_regression.py` 并接入原完整门禁，检查真实输入接线、共享解析、候选隔离、两个编辑入口、固定基线和无修改提前返回。原两条静态规则改为检测共享构造器及明确冲突弃用情绪，保留原保护意图，没有删测试。
- 四项既有专项、新连续链源码专项已通过；语法解析未发现新增诊断，但不等于 Swift 编译或类型检查。测试/生产配置完整 Windows 门禁按各自真实配置独立验证，日志与代码哈希位于 `C:/Users/yf/AppData/Local/Temp/xuzhang-record-intent-baseline-t7ceoyzg/`。生产使用真实 `a6f0964` 的隔离兼容副本，不代表已同步生产分支。
- 必须剩余验收：本机无 Swift/Xcode，16 项新增 XCTest 尚未执行，尚未编译/Archive/真机验证；`RELEASE_1.0_DEVICE_SIGNOFF_TEST_CASES.md` 第 8.4 节列出 10 组逐阶段真机签收。最终候选需固定 commit/Archive/Build，运行两配置完整 XCTest 及实际操作矩阵后才可标 VERIFIED。本轮没有提交、推送、部署或改用户账本；保留既有未提交第 171 节修复与其他资料。
