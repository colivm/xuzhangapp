# 叙账 AI 能力承诺 v1

> 日期：2026-07-15
> 目标：用户看到的“AI、本地规则、联网模型、失败回退”必须与真实代码路径一致。

## 1. 能力矩阵

| 能力 | 实际执行方式 | 是否联网 | 可以承诺 | 不可以承诺 |
|---|---|---|---|---|
| AI 指令台 | 本机规则引擎：先归一化口语/繁简体/标点，再按动作、时间、分类、金额和证据识别查账、对比、重复、记忆与通勤补记 | 否 | 在受支持范围内理解常见同义表达和语序变化，并给出可回到账本证据的结果 | 自由聊天、任意语义理解、模糊猜测、调用远程大模型、知道账本外事实 |
| 周记 / 月章 | 本地模板、聚合和叙事规则始终先完成；开启联网整理时，仅对本地已选定的脱敏事实提前请求轻润色 | 可选 | 播放不等待联网；远程失败、超时或校验失败时完整使用本地章节 | 让远程模型选择事实、改变章节/额度，或把本地模板包装成远程 AI |
| 生活线索 | 本地语义规则与聚合 | 否 | 展示有记录支撑的场景、节奏和上下文 | 推断健康诊断、关系状态、职业结论或心理状态 |
| 今日小记 | 默认本地规则；用户开启联网整理且配置/额度可用时尝试远程模型 | 可选 | 生成时说明会尝试远程模型，失败自动使用本地规则 | 暗示每次都调用远程模型，或对历史已保存小记伪造来源标签 |
| 月度整理 | 默认本地规则；用户开启联网整理且配置/额度可用时尝试远程模型 | 可选 | 明确显示“尝试远程 / 远程已生成 / 本地规则已生成 / 远程失败已回退” | 在缺 Key、超额度或网络失败时仍显示远程已生成 |
| OCR | 系统/本机识别和本地规则整理 | 否 | 图片识别后由用户确认导入 | 声称上传图片给远程模型 |

## 2. AI 指令台支持范围

- 按今天、昨天、本周、上周、本月、最近 N 天/周/月等范围查账。
- 按分类、关键词、金额、次数查询和汇总。
- 对比相邻时间段。
- 查找最大一笔、最近一次、生活线索相关记录。
- 检查疑似重复记录。
- 根据明确规则生成通勤待确认草稿；确认前不写入。
- 不支持的开放问题必须返回“需要更具体”，不能编造答案。

### 2.1 识别可信边界

- 输入先统一全半角、大小写、常见繁简体和口语时间，例如“這週”“这礼拜”“前仨月”。
- 任务方向由明确动作决定，时间、分类、金额、生活线索等槽位只用于限定范围，不能靠单个弱词跨任务跳转。
- “上次买可乐”可查最近一笔；“这周打车是哪天”应列本周记录，不能因为“哪天”误当成最近一笔。
- “本月比上月多多少”可进入对比；“今天怎么样”缺少账本范围，必须要求更具体。
- 通勤补记必须包含“补记、补录、漏记、补上”等强写入动作；“生成今天通勤”不能创建候选，“生成一份今天通勤统计”可按只读查询处理。
- “不要补记”必须零候选、零写入；若同一句明确要求“不要补记，查一下今天通勤”，只执行安全的只读查询。
- 心情、原因、价值判断、预测以及老板/同事等未支持主体不从时间或分类弱词借路，统一返回账本事实边界提示。
- 不使用模糊编辑距离把错别字强行映射到任务；低置信或冲突输入宁可回退，不猜测用户意图。
- 在“查记录”任务中，`高温通勤 / 热天通勤 / 酷热天上班`、`爱好类消费 / 兴趣消费 / 兴趣装备` 等同时包含高置信实体的名词短语可以直接进入只读查询；同样文本位于“补遗漏”任务时不会创建候选，仍需 `补记 / 补录 / 补上` 等强写入动作。
- 天气通勤必须同时满足记录自身的结构化天气与通勤证据；当前实时天气不能补写历史天气。旧记录只允许使用 `热天路上 / 冷天出门 / 雨天通勤 / 雪天通勤` 等受控事实片段回退。
- 爱好消费必须有渔具、露营、骑行、摄影、模型手办、乐器等具体物件或活动证据；`爱好里的小投入`、`辛苦了`、`治愈`、`值得` 等暖语气或价值判断不作为查询索引。
- 已识别但没有记录时，明确返回“该范围没有匹配记录”；不能把它降级成“未识别”，也不能为了召回结果扩大到普通购物、全部交通或账本外事实。

## 3. 事实边界

1. 所有结论必须能追溯到账本记录、用户备注、日期、分类、照片引用或已保存的上下文。
2. 天气、城市、照片、情绪标签只在记录确有对应字段时使用。
3. 不把消费记录解释成疾病、财务风险、关系好坏、人格或动机。
4. 不声称访问通讯录、定位历史、银行卡、支付平台账号或互联网信息。
5. 批量补记始终先预览、后确认；未来时间不提前生成。

## 4. UI 状态用语

- AI 指令台：`本机规则 · 不联网`。
- 联网整理开关：影响今日小记、月度整理和日/周/月轻总结的可选提前润色；不影响 AI 指令台和生活线索。周记/月章的事实选择、章节和本地保底始终不联网。
- 月度整理状态：`正在尝试远程模型`、`远程模型已生成`、`本地规则已生成`、`远程模型未接通，已用本地规则`。
- 今日小记加载说明：先按本地记录整理；联网开启时尝试远程模型，失败自动回退。
- 日/周/月轻总结：只在真实账本修订后后台预生成；播放、分享保存和模板切换不发请求、不等待。上传内容仅含临时证据编号、受控场景、次数和日期范围，不含照片、图片引用、账本 UUID、敏感标题或用户原句。

## 5. 回归要求

- 同一账本、同一时间和同一指令得到相同的本地结果。
- 旧请求不得覆盖新指令。
- 远程开关关闭时不发起 `AIReportService` 请求。
- 远程轻总结必须引用允许的临时证据编号；新数字、未知证据、推断词、旧修订或格式异常一律丢弃并使用本地结果。
- 缺 API Key、额度耗尽、内容保护、网络失败均返回本地结果并显示真实状态。
- 任何结果不得包含账本中不存在的商户、地点、事件或生活事实。
- 自然同义表达、繁体/口语时间、否定写入、主观问题和意图优先级必须有确定性回归用例。
