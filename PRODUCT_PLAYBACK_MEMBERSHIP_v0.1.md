# 叙账 · 回放与会员权益产品设计 v0.1

> 更新时间：2026-06-02  
> 状态：产品定稿（部分能力已在代码中，**免费/会员划界与统计页生活切片待开发**）  
> 关联：**[`PRODUCT_NORTH_STAR.md`](PRODUCT_NORTH_STAR.md)**（战略北极星）、`PRD_v0.1.md`（页面与字段）、`MemberPricingView`（定价展示）、`TODO.md`（排期）

---

## 1. 文档目的

汇总以下产品决策，供设计、开发、商店文案与 TestFlight 验收统一参照：

- **三种「回看」** 的定位与边界  
- **统计页 · 生活切片**（长时间跨度、总结性回访）  
- **首页 · 今日回放**（已有，保留）  
- **会员专属能力** 与 **免费次数限制**（场景包、宠物名、OCR、回访等）  
- **会员页 · 权益五条** 与商店文案定稿  
- **切片感染力**（旁白、幕设计、模板句）  
- 统计页信息架构与演进阶段  

---

## 2. 产品定位一句话

与 **[`PRODUCT_NORTH_STAR.md`](PRODUCT_NORTH_STAR.md)** 一致：

**叙账** = **生活回望** · 本地记账积累素材，用周/月 **生活切片** 把一段时间温柔「叙」出来（**本地模板即可，不依赖大模型**）。宠物是讲述者；**小 AI 说为可选增强**，非核心。会员主卖 **深度叙（切片无限）+ 省力记（场景包/OCR）**。

---

## 3. 五个 Tab 分工

| Tab | 名称 | 核心任务 | 回访/会员相关 |
|-----|------|----------|----------------|
| 今日 | 首页 | 记账、今日总览 | **今日生活回放**（短、逐笔） |
| 记账 | 记账 | 手动 / OCR | 场景备注包、OCR 次数 |
| **看看花** | **统计** | 查账、看趋势 | **生活切片 / 生活章**（总结性回访） |
| 小 AI 说 | AI 复盘（**可选**） | 播后「多聊一句」 | 远程 AI；无网络时用本地 fallback，不挡切片 |
| 我的小窝 | 设置 | 账号、会员、宠物开关 | 宠物昵称、协议入口 |

```text
记完账 → 月底打开「看看花」→ 播「本月生活章」→ 可选「去 AI 复盘」要建议
```

---

## 4. 三种「回看」不混用

| 形态 | 时间跨度 | 体验 | 主要入口 | 实现状态 |
|------|----------|------|----------|----------|
| **流水回放** | 今日 | ~10s，时间轴逐笔高亮 | 首页 | ✅ iOS `BillPlaybackSheet`、Web |
| **生活切片** | 本周 / 本月 | 30～90s，**章节卡片**总结叙事 | **统计页** | ⏳ 待开发 |
| **AI 复盘** | 日 / 月 / 季 / 年 | 阅读型：summary + action + encourage | AI Tab | ✅ 远程 AI + fallback |

**原则**

- 生活切片 = **看见**这段时间（数据 + 轻旁白），**核心能力**，不说教。  
- AI 复盘 = **可选**「多聊一句」；切片本地讲完即闭环，不依赖 AI。  
- 播完生活切片：**主 CTA** 会员/下次再叙；**次 CTA**「想多聊一句？」→ 小 AI 说（可跳过）。

---

## 5. 统计页 · 生活切片（核心新功能）

### 5.1 为什么放在统计页

- 用户心智已是 **「这段时间花了多少、结构如何」**，与周/月筛选一致。  
- 现有统计页偏薄：筛选 + 总支出 + 趋势图 + 列表；生活切片补上 **「把数据讲成故事」**。  
- 首页保留 **今日回放**，避免重复。

### 5.2 页面结构（目标态）

```text
账单（统计 Tab）
├─ 筛选：时间（本周 / 本月 / 本年 / 自定义）+ 分类
├─ 【生活切片卡片】← 主 CTA，与当前时间筛选联动
├─ 总览：总支出 + 近 30 天趋势 + 一句 insight（已有）
└─ 记录列表（已有）
```

**生活切片卡片位置**：筛选栏 **下方**、总览 **上方**（黄金位）。

**卡片随 `selectedPeriod` 变化**

| 用户选择 | 卡片标题 | 播放内容 | 时长 | 免费用户 |
|----------|----------|----------|------|----------|
| 本周 | 播放本周生活切片 | 周度 5 幕 | 30～45s | ✅ **每自然周 1 次**（周一 0 点起算新周） |
| 本月 | 播放本月生活章 | 月度 6 章 | 60～90s | ✅ **终生 3 次**（用完仅会员） |
| 本年 | 年度长卷（v0.2）或「即将上线」 | — | — | ❌ 会员 |
| 自定义 | 播放该时段总结（v0.2）或仅列表 | — | — | ❌ 会员 |

**卡片示例 · 免费 + 本周**

```text
🎬 本周生活切片
12 笔 · ¥1,280 · 餐饮为主
[ 播放 ]     本周剩余 1 次 · 会员可无限
```

**卡片示例 · 免费 + 本月（有终生额度）**

```text
🎬 本月生活章
23 笔 · ¥3,280 · 6 章看完整月节奏
[ 播放 ]     新用户专享剩余 2/3 次 · 用完后需会员
```

**卡片示例 · 免费 + 本月（终生 3 次已用尽）**

```text
🔒 本月生活章
会员专属 · 你的 3 次新用户体验已用完
[ 了解会员 ]   本周切片仍可按周免费播放
```

### 5.3 周度生活切片（5 幕）

| 幕 | 标题 | 内容（本地聚合即可） |
|----|------|----------------------|
| 1 | 开场 | 日期范围、笔数、总支出 + 宠物/简洁旁白 |
| 2 | 节奏 | 最忙/最省的一天（按笔数或金额） |
| 3 | 主角分类 | TOP1 分类 + 占比 |
| 4 | 高光一笔 | 单笔最大或备注最丰富的一笔（可链到列表） |
| 5 | 收尾 | 一句总结 + 按钮「生成周度 AI 建议」（可选） |

### 5.4 月度生活章（6 章）

| 章 | 内容 |
|----|------|
| 开篇 | 月总支出、记账天数、较上月 ±%（有历史时） |
| 上旬 / 中旬 / 下旬 | 各旬金额 + 一句归纳（如「上旬偏通勤，下旬外出多」） |
| 生活构成 | **生活配方**（见 §5.4.1）：环形/条形分类占比 + 一句人话 |
| 变化点 | 新出现分类、连续消费日等 |
| 宠物结语 | 依 **宠物开关**：暖心版 / 简洁版两套模板 |
| 行动 | 跳转「月度 AI 复盘」 |

#### 5.4.1 Web「生活配方」概念（复用说明）

**「生活配方」** 是产品侧命名，指：把一段时间内的支出按 **分类占比** 呈现成「你这阵子生活由什么构成」—— 像配方比例，温柔呈现、生活化描述。代码里 **没有** 名为 `生活配方` 的变量；Web 已有等价实现，iOS 月度生活章 **「生活构成」** 幕应对齐同一套数据与视觉语言。

| 维度 | 说明 |
|------|------|
| 数据输入 | 当前周期内 `amount > 0` 的账单，按 `category` 汇总金额 |
| 展示层级 v0.1 | **TOP1 环形占比**（必选）+ 可选 TOP2～3 条形或列表（月章比周卡可多做 1 档） |
| 旁白 | 暖心：`团子看到，这个月大约 {ratio}% 花在「{category}」上，像日常的主料。` 简洁：`{category} 约占 {ratio}%。` |
| 与 AI 复盘 | 生活配方 = **看见结构**；AI `action` 里可写「饮食占比、通勤稳定」等，但切片幕内 **不展开长文建议** |

**Web 现有实现对照**

| 能力 | 文件 / 函数 | 与「生活配方」的关系 |
|------|-------------|----------------------|
| 分类聚合 TOP3 | `web-preview/app.js` → `topCategoryStats(items)` | 供 AI Prompt、`buildInsightFacts` 的 `topRatio`、`categories` |
| **环形占比 UI** | `downloadWeeklyShareCardImage()` → Canvas 绘制 | 标签 **「TOP类目占比」**：圆环弧长 = TOP1 金额 / 周期总支出，`ratioText` 为百分比 |
| 周度分享卡入口 | `index.html` → `#weeklyShareBtn`「生成周度分享卡」 | 近 7 天数据 + 同屏 **近7天小趋势** 柱条（与配方并列，非配方本身） |
| 月度结构文案（非环形） | `buildLocalMonthlyInsightFallback` 等 → `structure` 字段 | 文本占比句，可作月章旁白 fallback |

**周度分享卡右侧环图（即生活配方 v0.1 视觉原型）**

```text
┌─────────────────────┐
│  TOP类目占比         │
│      ╭───╮          │
│      │38%│  ← 弧长 = topCategoryAmount / weekTotal
│      ╰───╯          │
│  餐饮 · ¥520        │
└─────────────────────┘
```

**iOS / 生活切片落地建议**

| 场景 | 配方粒度 |
|------|----------|
| 周切片 · 第 3 幕「主角分类」 | 仅 TOP1 + 占比（与 Web 环图一致，可简化为单环 SwiftUI） |
| 月章 · 「生活构成」 | TOP1 环图 + TOP2～3 横向条（占比条），总时长仍控制在 1 幕内 |
| 分享图（Phase D） | 直接复用 Web Canvas 布局或导出同结构 PNG |

**扩展 v0.2（可选，非 v0.1 必须）**

- 多环/堆叠条：全部分类占比和为 100%（需 ≥3 笔且分类 ≥2 才有意义）  
- 昵称/宠物模板注入 `{petName}`  
- 与「生活配方」区分：**生活切片** = 可播放多幕故事；**生活配方** = 其中 **分类构成** 那一幕的可复用组件  

**禁止**：月度回放逐笔滚动几十条（像流水账，累）。

### 5.5 播放形态

- **章节卡片**切换 + 数字滚动动画（复用今日回放的播放控制器思路）。  
- 每幕：**1 个核心数字 + 1 句人话**。  
- 宠物名：`{petName}` 替换（会员自定义；免费用默认名如「团子」）。

#### 5.5.1 视觉分层（底图 + 数据，v0.1 定稿）

**原则**：采用 **「主题底 + 上层数据」**，与 Web 周度分享卡（渐变底 + 柱条/环图/文案）一致；**不**为每章使用全屏摄影/重插画底图（数据每周/每月都变，静态大图易喧宾夺主且资产维护成本高）。

```text
SummaryPlaybackSheet（ZStack）
├─ 层 1 · 底（chapterBackground）
│     宠物开：暖色渐变（对齐 Web isPetTheme #fff3e8 → #ffe9f2）
│     宠物关：冷色简洁渐变（#f3f6fb → #edf1f7）
│     按「章类型」微调色相/装饰（intro / rhythm / category / highlight / outro）
│     可选：低对比纹理或 SF Symbol 大图，非全屏照片
├─ 层 2 · 数据（随章节切换动画更新）
│     核心数字（金额、笔数、占比%）+ iOS 17 contentTransition(.numericText())
│     「生活构成」幕：生活配方环图（§5.4.1）+ TOP 分类文案
│     幕标题 + 旁白（warm / plain 模板）
└─ 层 3 · Chrome
      章节指示点、播放/暂停/重播（复用 BillPlaybackSheet 状态机）
```

| 方案 | v0.1 | 说明 |
|------|------|------|
| 程序化渐变 + 材质 | ✅ **默认** | 复用 `AppColors`、`webCardBackground` 气质；2 套主题即可 |
| 按章类型轻量插图 | ⏳ 可选 | 5～6 张统一风格小图或图标，周/月共用；作低对比底，保证数字可读 |
| 每章一张摄影/重插画 | ❌ 不做 | 留给 Phase D **分享导出 PNG** 或运营素材，与播放 Sheet 分离 |
| 按用户数据生成底图 | ❌ 不做 | 实现重、与「数字即内容」冲突 |

**章节切换动画**

- 底 + 数据 **一起** `opacity` / 轻微 `scale`（0.98→1）交叉，或 `TabView(.page)`；避免「只换壁纸、数字不变」的错觉。  
- 今日回放（`BillPlaybackSheet`）为 **时间轴列表高亮**，生活切片为 **全屏章节卡**，仅共享 `activeIndex` + `Task.sleep` 定时逻辑，**不**照搬列表 UI。

**可读性与无障碍**

- 数据层与底对比度 ≥ WCAG 大意（深字浅底 / 白字深 overlay）；占比、金额字号 ≥ 今日回放金额行。  
- 宠物/简洁两套旁白模板；关宠物时用 `plain` 文案 + 冷色底。

**Swift 实现要点（iOS 17+）**

| 项 | 建议 |
|----|------|
| 视图 | `SummaryPlaybackSheet` + `SummaryChapter` 模型；`PlaybackService` 扩展周/月聚合 |
| 数字动画 | `Text(value, format: …).contentTransition(.numericText()).animation(…, value: value)` |
| 环图 | `Circle().trim(from:to:)` 或 `Canvas`，参数来自 `topCategoryStats` |
| 控制器 | 从 `BillPlaybackSheet` 抽出 `activeIndex` / `isPlaying` / `playbackDone` / 均分 `durationSec` |

**与今日回放对照**

| | 今日流水回放 | 生活切片 |
|--|--------------|----------|
| 结构 | 逐笔时间轴 | 5～6 章固定卡片 |
| 背景 | `ultraThinMaterial` 行高亮 | 主题渐变全卡底 + 数据层 |
| 时长 | 固定 ~10s | 周 30～45s / 月 60～90s |

### 5.6 免费次数规则（周切片 · 月章）

| 权益 | 重置规则 | 免费额度 | 扣次时机 |
|------|----------|----------|----------|
| **本周生活切片** | **每个自然周**更新 1 次（`weekKey` = 当年 ISO 周或 `yyyy-'W'ww`，以设备本地时区 **周一 00:00** 为新周） | 1 次/周 | 完整播完 5 幕（或用户主动播完 ≥80% 进度）扣 1 次 |
| **本月生活章** | **不重置**（终生累计） | **3 次/账号** | 完整播完 6 章扣 1 次；**不按自然月刷新** |

**会员**：上述两项均 **不限次数**。

**实现字段（本地 v0.1，登录后可同步）**

- `playbackWeekKey` + `playbackWeekUsed`（bool 或 0/1）  
- `lifetimeMonthChapterRemaining`：初始 **3**，每次成功播放本月章 **−1**，最小 0  

**原则**

- 周、月 **独立计数**，不共用额度池。  
- 识别失败、无数据未播完 → **不扣次**。  
- 注销重注册：**不重置**终生 3 次（以后端 `userId` 为准；仅本地时绑定设备+账号最佳努力）。

### 5.7 空状态与弱数据

| 情况 | 表现 |
|------|------|
| 无记录 | 卡片灰显：「这个时间段还没有记录，记几笔再来看切片」 |
| 记录 &lt; 3 笔 | 缩短为 3 幕，或提示「再多记几笔，切片更有趣」 |
| 本年 | v0.1 不播，按钮引导切换到「本月」 |

### 5.8 切片感染力（旁白与幕设计）

与 **[`PRODUCT_NORTH_STAR.md`](PRODUCT_NORTH_STAR.md)** NSM 一致：**感染力**决定播完率与复播；**动线**（§6）决定找不找得到入口。切片应用 **本地模板 + 聚合数据** 即可，不依赖远程 AI。

#### 5.8.1 感染力是什么

用户播到中段或播完时感到 **「说的就是我这段时间」**（认出自己），而不是停留在抽象数字上。

| 感受 | 触发方式 |
|------|----------|
| 认出自己 | 具体星期几、一笔有备注的消费、真实分类名 |
| 看见结构 | 占比环图、上中下旬差异、较上月 ±% |
| 叙事弧 | 开场 → 节奏/构成 → 高光 → 收尾，非报表翻页 |
| 被尊重 | 无评判词；plain 模式不卖萌 |

**感染力 ≠** 动画炫、文案长、宠物话多。**核心** = 事实密度 + 叙事顺序。

#### 5.8.2 毁掉感染力的做法（禁止）

- 报表腔：重复「共 N 笔、总支出」而无新信息  
- 说教：「占比过高」「建议控制」「应该省钱」  
- 月章逐笔滚动、播到一半强弹会员  
- 弱数据硬凑 5 幕 / 6 章  
- 旁白与数据不符（有备注「给妈妈买花」却只念「餐饮」）  
- 宠物旁白 &gt; 核心数字（喧宾夺主）

#### 5.8.3 周切片 · 每幕感染力要点

| 幕 | 感染力关键 | 屏上结构 |
|----|------------|----------|
| 1 开场 | 时间存在过 | 大字：笔数 · 总支出；小字：日期范围 |
| 2 节奏 | **真实 weekday** | 大字：最忙日 · 笔数；忌只写「某天」 |
| 3 主角分类 | **生活配方**（§5.4.1） | 环图 % + TOP1 分类名 |
| 4 高光一笔 | **有故事的一笔** | 优先备注最丰富，其次金额最大 |
| 5 收尾 | 总结不评好坏 | 一句收束；主 CTA 见北极星 §6.4 |

**最强两幕**：节奏（2）、高光（4）。周片总时长 30～45s，每幕约 6～9s。

**高光一笔选取规则（实现）**

1. 有 `note` / `title` 非空且非泛化分类名 → 优先  
2. 否则取周期内单笔金额最大  
3. 旁白必须带可得字段：`{highlightTitle}`、`{highlightAmount}`、`{highlightDayLabel}`

#### 5.8.4 月章 · 感染力要点

| 章 | 关键 |
|----|------|
| 开篇 | 记账天数 + `较上月 ±%`（有上月数据时） |
| 上/中/下旬 | 旬金额对比 + 一句节奏（如「上旬偏通勤，下旬外出多」） |
| 生活构成 | 环图 + TOP2～3 条，月叙「招牌幕」 |
| 变化点 | 新出现分类、连续消费日等 **记忆点** |
| 宠物结语 | ≤2 句，承上，不新开建议 |
| 行动 | 次 CTA；主 CTA 仍为会员 / 下月再叙 |

#### 5.8.5 旁白原则与变量

**公式**：`事实 + 可选轻隐喻`；禁止词见 PRD Prompt（超支、浪费、克制、理性消费等）。

| 变量 | 用途 |
|------|------|
| `{petName}` | 暖心幕；默认「团子」 |
| `{rangeLabel}` | 3月24日—3月30日 |
| `{count}` `{total}` | 笔数、总金额（已格式化） |
| `{busiestDay}` `{quietestDay}` | 星期或 M/D |
| `{topCategory}` `{ratio}` | 生活配方 |
| `{highlightTitle}` `{highlightAmount}` | 高光一笔 |

`warm` / `plain`：**事实相同，语气不同**；关宠物全程 `plain`。

#### 5.8.6 旁白模板库（v0.1 可直接实现）

**周切片 · 开场**

| 风格 | 模板 |
|------|------|
| warm | `{petName}陪你看了 {rangeLabel}，{count} 笔小记录，一共 {total}。` |
| plain | `{rangeLabel}：{count} 笔，支出 {total}。` |

**周切片 · 节奏**

| warm | `最忙的是 {busiestDay}；{quietestDay} 几乎没花钱，节奏很分明。` |
| plain | `支出集中在 {busiestDay}，{quietestDay} 最低。` |

**周切片 · 主角分类**

| warm | `大约 {ratio}% 花在「{topCategory}」上，像这周日常的主料。` |
| plain | `「{topCategory}」约占 {ratio}%。` |

**周切片 · 高光一笔**

| warm | `印象最深的一笔：{highlightDayLabel} · {highlightTitle}，{highlightAmount}。` |
| plain | `单笔最高：{highlightTitle}，{highlightAmount}（{highlightDayLabel}）。` |

**周切片 · 收尾**

| warm | `这周有在认真记录生活，{petName}下周再陪你叙。` |
| plain | `本周记录完整，下周可再看切片。` |

**月章 · 开篇（有上月对比时）**

| warm | `{rangeLabel} 你来了 {activeDays} 天，总支出 {total}，比上月 {momPercent}。` |
| plain | `{rangeLabel}：{activeDays} 天，{total}（较上月 {momPercent}）。` |

**月章 · 生活构成**

| warm | `这个月约 {ratio}% 在「{topCategory}」；生活配方里，它仍是主角。` |
| plain | `{topCategory} 约占 {ratio}%，为本月最高分类。` |

**月章 · 变化点**

| warm | `{changeHint}`（例：新出现「旅行」分类 / 连续 {streakDays} 天有记录） |
| plain | 同上，去掉隐喻 |

**弱数据（&lt;3 笔）· 收尾替代**

| warm | `还只有 {count} 笔，再多记几天，{petName} 能讲得更完整。` |
| plain | `记录较少（{count} 笔），补充后切片会更准确。` |

实现：`SummaryChapter.narration.warm` / `.plain` 字段；聚合服务填充变量。

#### 5.8.7 弱数据与幕数

| 笔数 | 幕/章 | 说明 |
|------|-------|------|
| 0 | 不播 | 卡片灰显（§5.7） |
| 1～2 | **3 幕** | 开场 + 主角分类 + 弱数据收尾 |
| ≥3（周） | 5 幕 | 标准周切片 |
| 月章 | 6 章可略缩短「变化点」若分类单一 | 见 §5.4 |

#### 5.8.8 感染力验收（TestFlight）

| 指标 | 参考 |
|------|------|
| 播完率 | 开播用户 ≥70% 达到 ≥80% 进度 |
| 次周复播 | 免费用户下一自然周再次完整播放周切片 |
| 定性 | 访谈「像不像你的这一周/月」 |
| 幕流失 | 第 2、4 幕退出率应低于第 3 幕（若第 3 最高，加强 2/4 事实密度） |

可选：第 4 幕后 **非阻断** 反馈「像这一周吗？」👍/😐，仅调模板。

#### 5.8.9 v0.1 最小清单（开发勾选）

- [ ] 高光一笔按 §5.8.3 规则选取  
- [ ] 节奏幕使用真实 `{busiestDay}` 星期/日期  
- [ ] 第 3 幕（周）/ 生活构成（月）含环图或超大占比  
- [ ] 弱数据缩幕 + §5.8.6 弱数据收尾句  
- [ ] 播完主 CTA 非 AI（北极星 §6.4）  
- [ ] 模板走 `warm`/`plain`，与宠物开关联动  

---

## 6. 首页 · 今日生活回放（保持）

| 项目 | 说明 |
|------|------|
| 名称 | 今日生活回放 |
| 时长 | 固定约 10s，按笔数均分高亮 |
| 数据范围 | 当日 `HomeItem`，按时间排序 |
| 代码 | iOS：`BillPlaybackSheet`；Web：`billPlayback` |
| 播完 | 非会员可展示 Nudge（留存周/月切片，见 `playback_complete`） |

与 **生活切片** 分工：今日 = 习惯与爽感；周/月 = 深度与会员价值。

---

## 7. 会员 · 记账小帮手（场景备注包）

### 7.1 是什么

根据 **金额 + 场景包规则** 一键生成备注（**非 AI**），可选展开四个场景手动选。

| 场景包 | ID | 默认分类 |
|--------|-----|----------|
| 打工人通勤包 | `commute` | 交通 |
| 吃货专属包 | `food` | 餐饮 |
| 铲屎官宠物包 | `pet` | 日用 |
| 旅行预算包 | `travel` | 其他 |

### 7.2 入口与门槛

- **入口**：记账页 · 手动录入，金额有效后展示「小宠物的记账小帮手」。  
- **门槛**：**会员专属**（`monthly` / `yearly` / `lifetime`）。  
- **免费**：仅 **分类下 3 条静态备注 chips**（如「午餐简餐」「地铁通勤」）。

### 7.3 交互

1. **一键生成备注**：按金额猜测默认包，生成备注（iOS：尽量不改已选分类）。  
2. **展开更多场景**：点选四包之一，应用对应分类 + 备注。  
3. Web：宠物包支持 `{petName}`；iOS 待对齐昵称。

### 7.4 代码参考

- iOS：`ContentView.swift` → `scenePacks`、`memberScenePackSection`  
- Web：`web-preview/app.js` → `memberScenePacks`

---

## 8. 宠物、天气与两种 AI 风格

| 开关 | 作用 |
|------|------|
| 宠物陪伴 | 总开关；关 = 偏简洁工具文案 |
| 天气场景暖心互动 | 记账/复盘/切片旁白可带天气语境 |

**会员加深**

- **宠物昵称自定义**（2～6 字）→ 场景包、生活切片、AI 陪伴语。  
- 两种风格：**简洁** vs **暖心陪伴**（同一数据，不同模板库）。

**免费**

- 可使用基础宠物/天气能力（与当前代码接近），但 **昵称 + 场景包 + 完整切片** 属会员向体验。

---

## 9. OCR 智能导入

| 档位 | 额度 | 说明 |
|------|------|------|
| 免费 | **3 次 / 天** | 尝鲜；仅 **成功导入**（识别到 ≥1 条）扣次 |
| 会员 | **不限**（或软上限 50 次/日防滥用） | 商店写「会员无限识票」 |

- 计数按 **自然日** 重置（建议用户本地日历日，文档写清）。  
- v0.1 OCR 仍为占位流程，**额度规则可先落地**。  
- 第 4 次触发：引导开通会员。

---

## 10. 免费 vs 会员权益总表

| 能力 | 免费 | 会员 |
|------|------|------|
| 手动记账、本地统计、列表筛选 | ✅ | ✅ |
| 备注建议 chips（每类 3 条） | ✅ | ✅ |
| **场景包 + 一键备注** | ❌ | ✅ |
| **宠物昵称自定义** | ❌（默认名） | ✅ |
| **OCR 导入** | 3 次/天 | 不限 |
| **今日流水回放** | 1 次/天 | 不限 |
| **统计 · 本周生活切片** | **1 次/自然周**（每周一 0 点刷新） | 不限 |
| **统计 · 本月生活章** | **终生 3 次**（用完需会员） | ✅ 不限 |
| 远程 AI 日复盘 | 建议 1 次/天 或 5 次/月（待实现） | 不限 |
| AI 季/年复盘 | ❌ | ✅（ai-proxy 会员 JWT） |
| 云端同步 | 登录后可基础备份（待定） | 完整 |

**设计原则**

- 免费：**完整记账主流程**不阉割。  
- 会员：**省力三件套** — 场景备注 + OCR 畅用 + **周/月生活切片无限**。  
- 免费尝鲜：**每周 1 次周切片** + **终生 3 次月章**，降低首次点「本月」的逆反，又不养成每月白嫖习惯。  
- 各能力 **独立计数**，不共用一个「神秘额度池」。

### 10.1 会员页 · 权益五条（定稿）

用于 **iOS `MemberPricingView`**、Web 会员弹层、App Store 订阅说明副文案。五条只写 **会员解锁什么**；免费额度见 **§10.1.2**，不挤进五条主列表。

#### 10.1.1 五条主文案（展开前标题 · 展开后副文案）

| # | 标题（≤20 字为宜） | 副文案（展开详情） |
|---|-------------------|-------------------|
| 1 | **🎬 周/月生活切片无限回看** | 统计页「本周生活切片」「本月生活章」不限次数播放；用章节卡片看懂这段时间花了什么、节奏如何。**核心卖点。** |
| 2 | **📝 场景备注包 + 宠物专属昵称** | 通勤/吃货/宠物/旅行四包一键备注；自定义昵称（2～6 字）贯穿切片与记账旁白。 |
| 3 | **📷 OCR 智能识票不限次** | 拍照导入账单；免费用户每日 3 次尝鲜，会员不限（可设软上限防滥用，商店仍写「不限」）。 |
| 4 | **☁️ 云端备份 + 纯净无广告** | 登录后账单可同步云端、换机不丢；全程无营销弹窗。天气/季节 **暖心旁白加强** 随会员模板更完整。 |
| 5 | **💬 小 AI 说 · 播后可选深聊（不限次）** | 生活切片讲完后，若想多一句交谈式建议再用；含季/年深度复盘（需 ai-proxy 会员 JWT）。**增强项，非首图卖点。** |

**代码映射（`MemberPricingView.swift` → `benefits` 元组数组）**

```swift
private let benefits = [
    ("🎬 周/月生活切片无限回看", "统计页本周切片与本月生活章不限次数…"),
    ("📝 场景备注包 + 宠物专属昵称", "通勤/吃货/宠物/旅行四包…"),
    ("📷 OCR 智能识票不限次", "拍照导入；免费每日 3 次…"),
    ("☁️ 云端备份 + 纯净无广告", "登录同步换机不丢；无营销弹窗…"),
    ("💬 小 AI 说 · 播后可选深聊", "切片看完还想多聊一句时再用；含季/年…"),
]
```

实现时以 **§10.1.1 表格正文** 为准替换占位省略号。

#### 10.1.2 免费额度一句话（会员页底部 / 统计卡片脚注）

不列入五条，避免主列表过长；与 **§10 总表** 一致：

> **免费体验：** 本周生活切片每自然周 1 次 · 本月生活章终生 3 次 · OCR 每日 3 次 · 今日流水回放每日 1 次。开通会员后上述回访与识票不限。

#### 10.1.3 商店与合规表述

| 场景 | 建议写法 |
|------|----------|
| App Store 订阅说明 | 突出 **生活切片无限 + 场景备注 + OCR**；AI 作「播后可选」一句带过；注明免费「每周 1 次周切片、终生 3 次月章」 |
| 审核注意 | 勿写「全部功能免费」；勿写免费用户「无天气/无宠物」（与 §8 冲突） |
| 云端备份 | 若 v0.1 未上线完整同步，第五条副文案可暂改为「云端备份（陆续开放）」或商店下架该句直至可用 |

#### 10.1.4 五条中刻意不写的内容

| 不写进五条的原因 | 放在哪 |
|------------------|--------|
| 免费周/月/OCR 具体次数 | §10.1.2 脚注、统计页卡片 |
| 今日流水回放 | §6；免费 1 次/天在脚注一句带过即可 |
| 备注 chips（每类 3 条） | 免费默认可用，非转化卖点 |
| 「仅会员才有宠物」 | 误导；免费有基础陪伴，会员是昵称+场景包+模板加强 |

#### 10.1.5 策略评估（定稿依据）

| 维度 | 结论 |
|------|------|
| 与免费划界 | **合适**：记账主流程不阉割；切片周刷新 + 月章终生 3 次降低逆反 |
| 与定价 ¥9/88/168 | **撑得住**：前五条以 **切片 + 省力记** 为主；AI 排最后作加分项 |
| 可调旋钮 | 若 TestFlight 转化偏弱，可将 **终生月章 3→2**，五条标题不变，仅改 §10 表与脚注数字 |
| 代码现状 | ⚠️ `MemberPricingView` 仍为旧五条（AI/天气/备份/广告/备注），**待按本节替换** |

---

## 11. 次数用尽话术

**本周切片（次数用尽）**

> 本周的生活切片已经看完啦。下个自然周会再刷新 1 次免费次数。开通会员可无限回看周/月切片。

**本月生活章（仍有终生次数）**

> 本月还可播放 **{n}/3** 次新用户专享「生活章」。用完后需开通会员无限回看。

**本月生活章（终生 3 次已用尽）**

> 你的 3 次新用户「本月生活章」已用完。开通会员可无限播放，并享场景备注包与无限 OCR。本周切片仍会在每个自然周刷新 1 次免费次数。

**OCR**

> 今日免费识票次数已用完（3/3）。会员可无限智能导入。

**统计页卡片脚注（常显）**

> 免费：本周切片每自然周 1 次 · 本月章终生 3 次 · OCR 每日 3 次 · 今日回放每日 1 次  
> 会员：周/月切片不限 + 场景备注包 + OCR/AI 不限（见 §10.1 权益五条）

---

## 12. 与 AI 复盘的分工（AI 非核心）

与 **[`PRODUCT_NORTH_STAR.md`](PRODUCT_NORTH_STAR.md) §5.1** 一致：**生活切片是产品本体**，AI 是播后的可选增强。

| | 生活切片（核心） | AI 复盘（可选） |
|--|------------------|-----------------|
| 目标 | 看见这段时间 | 想「多聊一句」时再给轻建议 |
| 形态 | 可播放、可暂停、分幕 | 静态三行文案 |
| 数据呈现 | 章节、数字、占比（**本地即可**） | 自然语言（需远程或 fallback） |
| 使用频率 | 免费：周 1 次/自然周 + 月章终生 3 次 | 用户自愿，不驱动 NSM |
| 技术 v0.1 | 本地模板 + 聚合 | backend → ai-proxy → DeepSeek；**不可用不挡切片** |
| 缺失时 | 产品失败 | 产品仍成立 |

---

## 13. 会员定价（商店展示，与代码一致）

> 定稿：2026-06-02 · 主推年包；永久勿长期低于 1.5 年年费。

| 档位 | 常备价 | 周期 | 代码 tier | 展示顺序 |
|------|--------|------|-----------|----------|
| 月度会员 | **¥9** | 月 | `monthly` | 3 |
| 新订首月（推介促销） | **¥6** | 首月 | IAP Introductory Offer | 附在月付说明 |
| 年度会员 ✨推荐 | **¥88** | 年 | `yearly` | **1（默认 CTA）** |
| 永久会员 | **¥168** 常备 | 永久 | `lifetime` | 2（更多套餐内） |

**换算**：月付年化 ¥108；年付 ¥88 ≈ 省 ¥20（约 8.1 折）；永久 ¥168 ≈ **1.9 年年费**（88×2=176），年包仍为订阅最优。

**首发永久（可选，见 §13.1）**：上架后 **90 天内** ¥148，期满恢复 **¥168**；勿写假原价。

竞品参考：极简记账多为 ¥6/月、¥30～38/年；叙账以 **生活切片 + 省力记** 定价，月 ¥9 与竞品首月持平（推介 ¥6），不靠「AI 记账」溢价。

会员页 **权益五条** 以 **§10.1** 为准；`MemberPricingView` / Web 会员弹层价格与 §13 表一致。

### 13.1 首发促销与用户信任（避免反感）

| 做法 | 建议 |
|------|------|
| **月付首月 ¥6** | 仅用 App Store **新订户推介促销**（Introductory Offer），会员页写「新用户首月 ¥6，之后 ¥9/月」 |
| **永久 ¥148** | 仅作 **上架首发 90 天**（或固定截止日），会员页脚注 + 商店描述写清 **恢复 ¥168 日期** |
| **禁止** | 天天弹「最后 3 小时」、假原价 ¥298、对已购永久用户弹更低现价 |
| **语气** | 「早期支持者价」「感谢第一批愿意叙下去的人」——邀请式，非焦虑秒杀 |
| **主推** | 会员页首屏 CTA 仍为 **年付 ¥88**；永久角标「首发 ¥148」不盖过年包 |
| **老用户** | 已购永久不自动降价；若有 v0.1 前付费再个案处理 |

**永久价为何不用 ¥128 常备**：约 1.45 年年费，理性用户只买永久、年包失效，且日后买 ¥168 者易觉吃亏。

---

## 14. 技术实现分期

### Phase A（当前）

- [x] 今日回放 iOS / Web  
- [x] 场景包 UI（会员门槛已有，需巩固）  
- [ ] 权益次数：**代码层未统一 enforce**  
- [ ] `MemberPricingView` / Web 会员弹层：按 **§10.1.1** 替换权益五条  

### Phase B — 统计页生活切片 MVP

- [ ] 本地按周/月聚合指标服务  
- [ ] `SummaryPlaybackSheet`：章节数组 + 定时切幕  
- [ ] 统计页「生活切片卡片」+ 与 `StatsPeriod` 联动  
- [ ] 免费/会员次数：`UserDefaults`（或登录用户云端）— `weekKey` 周切片；`lifetimeMonthChapterRemaining` 月章（初值 3）；会员跳过校验  

### Phase C — 体验完善

- [ ] 宠物昵称注入 iOS 场景包与切片模板  
- [ ] 播完 Nudge → 会员页 / AI 复盘  
- [ ] OCR 真实识别 + 扣次逻辑  

### Phase D — 云端与增值

- [ ] `GET /v1/playback/summary?range=week|month`  
- [ ] 切片分享图、云端缓存  
- [ ] 年度长卷 + 与季/年 AI 联动  

---

## 15. 章节数据结构（开发参考）

```json
{
  "range": "week",
  "rangeLabel": "3月24日—3月30日",
  "durationSec": 40,
  "chapters": [
    {
      "id": "intro",
      "title": "本周开场",
      "metrics": { "count": 12, "total": 1280.5, "currency": "CNY" },
      "narration": {
        "warm": "团子陪你看了这一周，12 笔小记录，一共 ¥1280.50。",
        "plain": "本周共 12 笔，支出 ¥1280.50。"
      }
    },
    {
      "id": "rhythm",
      "title": "节奏",
      "metrics": { "busiestDay": "周三", "quietestDay": "周日" },
      "narration": { "warm": "...", "plain": "..." }
    }
  ],
  "cta": {
    "label": "生成周度 AI 建议",
    "action": "open_ai_insight",
    "feature": "weekly"
  }
}
```

---

## 16. 验收标准（TestFlight）

- [ ] 免费用户：本周切片第 2 次在同一自然周内提示用尽；**新自然周**恢复 1 次  
- [ ] 免费用户：筛选「本月」可播放，显示「剩余 x/3」；第 4 次起锁定，仅「了解会员」  
- [ ] 会员：本周/本月切片均无次数限制  
- [ ] 非会员看不到场景包区块（或点击引导会员）  
- [ ] OCR 第 4 次当日提示用尽；识别失败不扣次  
- [ ] 播完周/月切片可跳转 AI 复盘或会员页  
- [ ] 宠物关：切片旁白为 `plain` 模板；宠物开：为 `warm` 模板  

---

## 17. 实现状态对照（截至 2026-06-02）

| 能力 | 代码位置 | 产品定稿 | 次数限制 |
|------|----------|----------|----------|
| 今日回放 | `BillPlaybackSheet` | ✅ | ⏳ 待做 |
| 场景包 | `scenePacks` | ✅ 会员 | ✅ 已门槛 |
| 生活切片 | — | ✅ 本文 | ⏳ 待做 |
| OCR | `OCRService` 占位 | ✅ 次数规则 | ⏳ 待做 |
| 会员价展示 | `MemberPricingView` | ¥9/首月6/88/168（首发永久148） | — |

---

## 18. 相关文档

| 文档 | 说明 |
|------|------|
| `PRD_v0.1.md` | 页面清单、字段、Prompt |
| `PROJECT_ANALYSIS.md` | 架构与完成度 |
| `PROJECT_SETUP.md` | 部署与联调 |
| `TODO.md` | 排期勾选 |
| `API_v0.1.md` | 会员 tier、AI feature |
| `NativeDemoApp/IOS_REAL_INTEGRATION_CHECKLIST.md` | 上线参数 |

---

## 19. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-06-02 | 首版：回访三分法、统计页生活切片、免费/会员权益表、OCR/场景包/定价 |
| 2026-06-02 | 调整：免费仅「本周切片」每周 1 次；「本月生活章」会员专属、免费不开放 |
| 2026-06-02 | 调整：本周切片 **每自然周 1 次**；本月生活章免费用户 **终生 3 次**（用完才锁会员） |
| 2026-06-02 | 新增 **§10.1 会员页权益五条定稿**、免费脚注、商店表述与 `MemberPricingView` 映射；修正 §11 周切片用尽话术 |
| 2026-06-02 | 新增 **§5.4.1 Web「生活配方」概念**：对齐 `topCategoryStats`、周度分享卡环图与 iOS 月章「生活构成」 |
| 2026-06-02 | 新增 **§5.5.1 视觉分层**：主题渐变底 + 数据层 + Chrome；明确 v0.1 不用每章重背景图 |
| 2026-06-02 | 战略对齐北极星：**AI 非核心**；权益五条 AI 调至第 5 条；§12 与播完 CTA 以切片为先 |
| 2026-06-02 | 新增 **§5.8 切片感染力**：幕设计要点、旁白原则、warm/plain 模板库与验收清单 |
| 2026-06-02 | **§13 定价定稿**：月 ¥9、推介首月 ¥6、年 ¥88、永久 ¥168；§13.1 首发 ¥148 与反反感话术 |
