# P3 上线前安全与测试清单

> 阶段定位：P0/P1/P2 已完成基础合规、安全拦截与生产启动门禁。P3 不继续改核心业务，目标是把 TestFlight/staging 跑稳，并确认切 production 前没有高风险漏项。

## 1. 环境分层

### Staging / TestFlight

`backend/.env`：

```env
NODE_ENV=staging
PORT=8790
ALLOW_ORIGIN=*
DEV_ALLOW_SMS_CODE=123456
APPLE_APP_STORE_API_BASE_URL=https://api.storekit-sandbox.itunes.apple.com
```

`ai-proxy/.env`：

```env
NODE_ENV=staging
REQUIRE_JWT=0
```

要求：

- staging 可以继续用开发短信码。
- staging 使用 sandbox App Store 验单。
- staging 数据库必须和生产数据库分开。
- staging 的 `JWT_SECRET`、`AI_PROXY_TOKEN`、数据库密码不要和生产共用。

### Production

`backend/.env` 必须满足：

```env
NODE_ENV=production
JWT_SECRET=<强随机值>
ALLOW_ORIGIN=https://xuzhangapp.com
AI_PROXY_TOKEN=<强随机值>
DEV_ALLOW_SMS_CODE=
DATABASE_URL=<生产库>
APPLE_APP_STORE_API_BASE_URL=https://api.storekit.itunes.apple.com
```

`ai-proxy/.env` 必须满足：

```env
NODE_ENV=production
APP_PROXY_TOKEN=<与 backend 的 AI_PROXY_TOKEN 一致>
JWT_SECRET=<强随机值>
REQUIRE_JWT=1
AI_UPSTREAM_API_KEY=<生产模型 key>
```

生产门禁已在代码中生效：缺少关键配置会直接启动失败。

## 2. 密钥轮换

上线前必须轮换：

- 数据库密码。
- `JWT_SECRET`。
- `AI_PROXY_TOKEN` / `APP_PROXY_TOKEN`。
- 曾经贴出、截图、发给 AI 或进入聊天记录的测试密钥。

上线前建议确认：

- Apple `.p8` 私钥只保存在服务器安全目录，不进 Git。
- `.env` 不进 Git。
- 服务器日志不打印完整 `.env`。
- 测试环境和生产环境的数据库、密钥、模型 key 分开。

## 3. 登录策略

当前建议：

- 主登录：Apple ID 登录。
- 兜底：短信登录。
- 暂缓：本机号码一键登录。

原因：

- Apple ID 对个人开发者和 iOS 用户最稳，隐私感最好。
- 短信登录适合兜底、找回和国内用户习惯，但个人开发者会遇到签名/资质审核。
- 本机号码一键登录通常涉及运营商 SDK、聚合服务、商务资质和额外隐私合规，现阶段复杂度偏高。

短信服务建议：

- 优先试腾讯云短信。
- 备选阿里云短信。
- 模板只做验证码，不做营销。
- 后端保留当前短信限流。

## 4. Staging Smoke

每次 TestFlight 包发布前跑：

- `GET /health`：backend 正常。
- `GET /health`：ai-proxy 正常。
- 手机号验证码：发送、错误验证码、正确验证码、频率限制。
- Apple sandbox 内购：购买、恢复购买、会员有效期展示。
- OCR：导入微信/支付宝账单，确认分类和标题不回退。
- AI 复盘：今日小记、周记、月记能生成；失败时有本地 fallback。
- 云端账本：登录后同步、删除单条、删除云端账本。
- 注销账号：保留本机账本、清空本机账本两条路径都可用。
- 内容安全：昵称、备注、账单标题里输入手机号/链接/证件号会被拦截或脱敏。
- 分享图：保存前有隐私提示。

## 5. Production Smoke

切 `NODE_ENV=production` 前先在 staging 机器演练一次。

必须通过：

- 缺少关键环境变量时，backend 启动失败并提示缺项。
- 缺少关键环境变量时，ai-proxy 启动失败并提示缺项。
- 补齐配置后，两个服务能正常启动。
- `DEV_ALLOW_SMS_CODE` 为空时，不能再用开发验证码登录。
- `ALLOW_ORIGIN` 不再是 `*`。
- App Store 验单地址切到 production。
- 数据库指向生产库。

## 6. 日志与隐私

必须确认：

- 日志不记录完整手机号、身份证号、银行卡号、链接、邮箱。
- 日志不记录完整账单正文。
- AI 请求/响应安全拦截日志只保留脱敏 sample。
- analytics props 会过滤 token/key/signed/secret 字段。
- 账号注销后，服务端账号、云端账本、会话、会员绑定、内存埋点都清理。

## 7. 数据备份与恢复

上线前至少明确：

- PostgreSQL 每日备份策略。
- 最近 7 天备份可恢复。
- 恢复流程写在运维记录里。
- 删除账号不应被备份恢复重新带回线上账号。

## 8. 发布前人工检查

- 官网下载链接指向当前 TestFlight / App Store。
- 官网品牌解释统一：账单只是注脚，生活才是主角。
- 隐私政策和用户协议 URL 可访问。
- App 内协议入口指向线上 URL。
- 会员价格、权益、免费额度与 App Store Connect 一致。
- 截图展示的功能都已真实可用。

## 9. 不建议现在做

- 大范围政治敏感词库。
- 本机号码一键登录。
- 多短信供应商同时接入。
- 重写账号系统。
- 为了合规继续堆弹窗，影响主流程体验。

## 10. P3 完成标准

- staging 能连续跑完一次完整 TestFlight 回归。
- 生产门禁验证通过。
- 密钥轮换计划明确。
- 登录策略确认：Apple ID 主入口，短信兜底。
- 上线前必须人工确认的事项已写入清单。

