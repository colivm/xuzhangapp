你在 xuzhangapp 实现 Fix C1 — 切片引导不重复弹（小 PR · HomeViewModel）。

================================================================================
现象
================================================================================

「本周生活切片可播放」类引导：同条件 kill App 重启后再弹。
根因：emittedRouteGuidanceTypes 仅内存；consumeRouteGuidance 不持久化。

================================================================================
任务
================================================================================

1. 将已展示/已消费的 PlaybackRouteGuidance 类型持久化（UserDefaults 或 LocalStore 轻量键）

2. refreshRouteGuidanceIfNeeded：已持久化 consumed 的类型不再 assign activeRouteGuidance

3. consumeRouteGuidance：写入持久化

4. 不改触发条件（weekCount >= 3 等），只修「同用户同周期只引导一次」

================================================================================
禁止
================================================================================

不改 Stats 角标逻辑、不改切片播放、git commit

================================================================================
验收
================================================================================

[ ] 触发引导后点掉/消费，杀进程重开不再弹同一条
[ ] 新一周数据满足条件可再弹
[ ] 首记引导 C1 路径零回归

@NativeDemoApp/ViewModels/HomeViewModel.swift
