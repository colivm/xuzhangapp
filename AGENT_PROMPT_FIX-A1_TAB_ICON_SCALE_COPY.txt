你在 xuzhangapp 实现 Fix A1 — 底栏 Tab 图标真机比例对齐 Web（仅 ContentView.swift tabIcon 区）。

================================================================================
现象
================================================================================

真机上底栏图标：外圈偏大、图形偏小、边缘略糊。
根因：iOS 用 30pt 外圈 + 18pt 图形框，但 Path 坐标来自 Web SVG viewBox 0…24，未统一缩放。

Web 对照：web-preview/styles.css .tab-icon 24×24，SVG viewBox 0 0 24 24。

================================================================================
任务
================================================================================

1. tabIcon 外圈 Circle、glyph Group、外层 ZStack 统一改为 24×24 pt（与 Web 一致）

2. 不改 Path 坐标数字（已是 24 系）；不换成 PNG

3. 可选：描边 lineWidth 用 1.5 整数；底栏 .padding(.bottom) 可加 safeArea 适配（仅当真机仍扁）

================================================================================
禁止
================================================================================

不改 Tab 文案、pageTitle、各 tabXxxGlyph 路径形状、其他页面、git commit

================================================================================
验收
================================================================================

[ ] 真机五 Tab 图标与文字比例协调，选中绿底与图形大小匹配 Web 预览
[ ] 模拟器与 Web tabbar 视觉接近
[ ] 切换 Tab 零回归

@NativeDemoApp/ContentView.swift
@web-preview/styles.css（.tab-icon 对照）
