# 真机测试用例：2026-06-18 全量改动

日期：2026-06-18  
分支：`feature/外观色彩主题与会员方案`

## 一、今日修改摘要

### 1. 会员状态与记账页场景入口修复

- 修复会员权益状态刷新链路，重点处理恢复购买、切换账号、当天过期后需要进入「我的」页才刷新的问题。
- 会员状态刷新从 App 激活、设置页状态、购买回调等路径统一校验，减少「购买未完成但会员状态变化」这类错乱感。
- 清理记账页主 CTA 附近的场景包入口，避免免费场景包引导侵占「放进账本」视觉。
- 明确会员/非会员 UI 边界：非会员不应在会员状态下方看到不合适的场景包入口，记账页仍以记账为第一目标。

### 2. 免费用户 3 个场景包能力

- 新增 `FreeScenePackService`，支持免费用户自选 3 个可用场景包。
- 默认免费包为通勤、吃喝、居家，用户可在「换个角度」Sheet 内管理。
- 免费用户在确认档预览卡可使用「换一句 / 换个角度 / 自己写一句」，但只在 3 个免费包内出句。
- 首周可无限替换组合；首周后替换组合有 30 天冷却；拖拽排序不受冷却影响。
- 扩展包仍保持锁定，点击进入会员引导。
- 保持会员路径不变：会员仍可使用完整场景包列表、拖拽排序、折叠区会员场景包。
- 清洗了快速备注文案，避免「运动健康」等不生活化或非会员仅能用快速备注时显得不合适的文案。

### 3. 付费引导与会员文案优化

- 优化会员权益表达，加入更明确的功能价值说明。
- 新增命中锁定场景的轻提示策略，并带冷却期，避免频繁打扰。
- 付费引导从强打断改为更贴近场景的轻提示，重点放在用户已经遇到能力边界时。
- 会员页补充主题权益说明：25+ 色彩主题、永久会员限定主题、分享图同款主题。

### 4. 周分享保存确认弹框修复

- 修复周分享卡保存确认弹框与底部气泡瞬间重叠的问题。
- 调整保存确认层级和动画，减少弹框出现/消失时与底部 Toast、气泡的视觉冲突。

### 5. 痕迹页「线索」视图与 UI 层级优化

- 新增痕迹页「生活 / 线索」双视图。
- 「线索」视图改为一句数据结论 + 轻量证据，不做传统 KPI 报表。
- 优化「生活 / 线索」一级 Tab，与「本周 / 本月」二级筛选形成不同视觉层级。
- 调整线索 Hero、生活构成、一周节奏、变化线索、多看一层、细查入口的色彩、间距、玻璃感和层级。
- 去掉痕迹细查列表中不必要的趋势图，避免数据功能重复和首屏干扰。
- 修复痕迹页默认进入细查列表的问题，默认应停留在主视图。
- 修正文案「二更密一些」这类语义异常，避免生成结果显得生硬。

### 6. 生活深读 / 多看一层

- 新增 `LifeInsightService`，在痕迹页提供「多看一层」深读能力。
- 免费用户有有限次数体验，会员可持续使用。
- 深读卡片支持问题展开、追问 Chip、回答块和会员引导。
- UI 上弱化普通变化线索，突出「多看一层」作为更高级的解释能力。

### 7. OCR 复核与账单去重优化

- 优化 OCR 后的复核体验，针对支付宝等单笔详情截图减少拆出过多记录的风险。
- 增强重复金额、相邻文本、详情页/列表页线索判断，避免同一笔金额如 `299.89` 被识别成两笔。
- OCR 确认 Sheet 增加更明确的不确定项、重复疑似项和复核提示。
- 保持旧账单识别能力不退化，OCR 优化以「少误拆、少重复、可确认」为优先。

### 8. 复盘页 AI 指定操作

- 在复盘页新增 AI 指定操作入口。
- 支持用户输入自然语言指令，例如：
  - 「帮我看一下过去三天餐饮类的消费」
  - 「看一下这周交通」
  - 「找找最近有没有重复账单」
  - 「补记过去一周工作日通勤，早晚各一次」
- 查询类指令可生成简洁结论、列表和轻量图表。
- 补记类指令会生成待确认记录列表；有历史通勤金额时复用历史金额，没有金额时要求用户输入。
- 批量补记必须由用户确认后一次性保存，不直接静默写入账本。
- 保存入口走 `HomeViewModel.importAICommandDrafts`，不改动记账页已有分类、备注、情绪标签、预览卡文案逻辑。

### 9. 外观色彩主题体系

- 新增主题基础设施：`ThemeResolver`、`ThemeTokens`、`AppTheme`、`ThemeCatalog.json`。
- 完成 31 款主题的 light/dark token：免费 3 款、标准会员主题 25 款、永久会员限定 3 款。
- 新增设置持久化字段：
  - `colorThemeId`：当前 App 色彩主题。
  - `shareCardUsesAppTheme`：分享图是否跟随当前主题。
- 根视图接入主题：
  - App 启动时应用主题。
  - `AppColors` 从当前主题读取。
  - 系统浅色/深色变化时自动取对应 theme mode。
- 设置页外观 Sheet 升级：
  - 明暗模式：跟随系统 / 浅色 / 深色。
  - 免费主题 swatch。
  - 会员主题 Accordion。
  - 恢复默认主题。
  - 分享图使用当前主题开关。
- 主题会员边界：
  - 免费用户只能使用 3 款免费主题。
  - 月度/年度会员可用标准主题。
  - 永久会员可用全部主题和 3 款永久限定。
  - 会员过期或退出登录后，如当前主题不再有权限，回到默认主题。
- 视觉接入：
  - 5 个主 Tab 通过 `AppColors` 吃主题色。
  - 首页今日列表、痕迹页线索、细查列表、分类色条接入主题色。
  - 分类色统一走 `theme.categoryColors`。
  - 周分享图支持跟随 App 当前主题。

### 10. 真机编译问题修复

- 修复 `InsightWebView` 中非隔离上下文引用 `AppColors` 主线程隔离属性的问题。
- 修复 `StatsWebView` 中 `some View` getter 缺少 `return` 的编译问题。
- 修复 `traceLifeInsight` getter 缺少 `return` 的编译问题。

## 二、测试准备

### 测试账号

| 账号类型 | 用途 |
|---|---|
| 未登录 / 免费用户 | 测免费主题、免费 3 场景包、锁定提示、AI 功能边界 |
| 月度或年度会员 | 测标准会员主题、完整场景包、深读能力、分享图主题 |
| 永久会员 | 测永久限定主题、完整主题权益 |
| 过期会员账号 | 测主题回退、会员状态刷新、功能降级 |
| 新账号 / 老账号切换 | 测会员状态、云端设置、主题和本地数据边界 |

### 推荐测试主题

| 主题 | 目的 |
|---|---|
| 叙账默认 | 默认视觉回归 |
| 霓虹深渊 | 高差异主题，检查旧绿色残留 |
| 纸境·留白 | 浅色纸张主题，检查对比度 |
| 永享·档案金章 | 永久会员限定主题 |

### 测试账本数据

建议至少准备：

- 最近 7 天内 10 条以上记录。
- 包含餐饮、交通、购物、日用、健康、人情等分类。
- 至少 2 条交通记录金额为 `4.75` 或类似通勤金额。
- 至少 2 条金额相同、时间接近的记录，用于测试「找重复」。
- 至少 1 张支付宝详情页截图、1 张支付宝列表页截图，用于 OCR 回归。

## 三、真机测试用例

### A. 编译与启动

| 编号 | 用例 | 操作 | 预期 |
|---|---|---|---|
| A-01 | 真机编译 | Xcode 选择真机 Run | 无编译错误，无启动崩溃 |
| A-02 | 默认启动 | 全新安装后打开 App | 使用叙账默认主题，首页正常显示 |
| A-03 | 重启保留设置 | 切换主题、杀进程、重开 | 主题与明暗模式保持上次选择 |
| A-04 | 非法主题回退 | 手动制造无效 theme id 后启动 | 自动回到默认主题，不崩溃 |

### B. 会员状态与恢复购买

| 编号 | 用例 | 操作 | 预期 |
|---|---|---|---|
| B-01 | 恢复购买 | 会员页点击恢复购买 | 状态正确刷新，不出现「未完成但已变会员」的错乱 |
| B-02 | 当天过期刷新 | 使用当天过期账号，晚上打开 App | 不需要进入「我的」页也能刷新过期状态 |
| B-03 | App 激活刷新 | 后台切回前台 | 会员状态按本地票据/云端状态刷新 |
| B-04 | 切换账号 | A 账号退出，B 账号登录 | 会员状态、主题权限、云备份开关按 B 账号读取 |
| B-05 | 过期功能降级 | 会员主题/会员功能过期后打开 | 锁定能力不可继续用，主题回到默认或可用主题 |

### C. 记账页与免费 3 场景包

| 编号 | 用例 | 操作 | 预期 |
|---|---|---|---|
| C-01 | 主 CTA 位置 | 免费用户进入记账页输入金额 | 预览卡与「放进账本」之间没有新增场景包区块 |
| C-02 | 免费确认档操作 | 输入有效金额进入 confirm 档 | 显示「换一句 / 换个角度 / 自己写一句」 |
| C-03 | whisper 档边界 | 仅有低信息输入时查看 foot | 免费用户不显示换句/换角度，仅保留自己写一句 |
| C-04 | 免费换一句 | 免费用户点「换一句」 | 只在 3 个免费包内出句，不弹会员墙，不改用户已选分类 |
| C-05 | 免费换个角度 | 点「换个角度」 | 打开免费 3 包 Sheet，不进入会员全列表 |
| C-06 | 首周替换 | 首周内替换 3 包组合 | 无确认弹窗，可多次替换 |
| C-07 | 首周后替换 | 模拟首周后替换组合 | 出现确认；确认后进入 30 天冷却 |
| C-08 | 冷却中 | 冷却期内再点替换 | 按钮禁用并显示剩余天数 |
| C-09 | 拖拽排序 | 任意时期拖拽 3 包顺序 | 可拖拽，不触发确认或冷却 |
| C-10 | 扩展包锁定 | 免费用户点旅行/宠物/宝宝/健身等扩展包 | 进入会员页，不静默解锁 |
| C-11 | 会员回归 | 会员点换句/换角度 | 仍使用完整场景包列表，拖序和折叠区不变 |
| C-12 | 快速备注文案 | 非会员只使用快速备注 | 文案生活化，不出现「运动健康」等混杂生硬表达 |

### D. 付费引导与轻提示

| 编号 | 用例 | 操作 | 预期 |
|---|---|---|---|
| D-01 | 命中锁定场景 | 免费用户触发锁定场景能力 | 出现轻提示，不强打断当前记账 |
| D-02 | 冷却期 | 短时间重复触发同一锁定场景 | 不反复弹提示 |
| D-03 | 会员页文案 | 打开会员页 | 主题、AI、场景包等权益表达清楚，不夸张、不割裂 |
| D-04 | 非会员边界 | 非会员在我的页/记账页/复盘页查看 | 不出现看似已开通的会员入口状态 |

### E. 周分享保存弹框

| 编号 | 用例 | 操作 | 预期 |
|---|---|---|---|
| E-01 | 保存确认弹框 | 周分享卡点击保存 | 确认弹框出现时不与底部气泡重叠 |
| E-02 | 快速连续点击 | 连续点击保存/取消 | 不出现层级错乱、按钮失效或残留蒙层 |
| E-03 | 保存成功反馈 | 确认保存到相册 | 使用自定义反馈，不像系统默认突兀提示 |

### F. 痕迹页生活 / 线索

| 编号 | 用例 | 操作 | 预期 |
|---|---|---|---|
| F-01 | 默认入口 | 从底部 Tab 进入痕迹页 | 默认进入主视图，不自动打开细查列表 |
| F-02 | 一级 Tab | 切换「生活 / 线索」 | 两个 Tab 视觉清楚，中间分割线自然，不和二级筛选重复 |
| F-03 | 二级筛选 | 切换「本周 / 本月」 | 数据刷新正确，视觉上像筛选而不是主 Tab |
| F-04 | 线索 Hero | 查看线索首屏 | 第一眼只有一句主结论，金额/笔数不抢主标题 |
| F-05 | 生活构成 | 查看构成条和分类行 | 分类色低饱和，换主题后色盘同步变化 |
| F-06 | 一周节奏 | 查看有记录/无记录日期 | 空柱清晰可见，不像缺数据；今日标识不过度抢眼 |
| F-07 | 变化线索 | 下滑到变化线索 | 视觉轻于「多看一层」，不像重复读两遍 |
| F-08 | 细查入口 | 点「细查这一段」 | Sheet 正常打开；入口是 quiet link，不和主 CTA 竞争 |
| F-09 | 文案异常 | 多造几组本周数据 | 不出现「二更密一些」这类明显病句 |

### G. 生活深读 / 多看一层

| 编号 | 用例 | 操作 | 预期 |
|---|---|---|---|
| G-01 | 免费次数 | 免费用户打开多看一层 | 显示剩余次数或试用状态 |
| G-02 | 免费次数耗尽 | 用完免费次数后继续点 | 引导会员，不直接生成 |
| G-03 | 会员使用 | 会员点击多看一层 | 可持续展开深读，不受免费次数限制 |
| G-04 | 追问 Chip | 点击不同追问 | 选中态可辨，回答块正常刷新 |
| G-05 | 视觉层级 | 查看深读卡 | 全页只有一个主要实心 CTA，不被编号/Chip 抢层级 |

### H. OCR 复核与去重

| 编号 | 用例 | 操作 | 预期 |
|---|---|---|---|
| H-01 | 支付宝详情页 | 上传单笔详情截图 | 不应拆成多笔；同一金额不重复出现 |
| H-02 | 支付宝列表页 | 上传列表截图 | 能识别多笔账单，保持原有识别能力 |
| H-03 | 重复金额 | 截图里出现两处相同金额 `299.89` | 若是同一笔详情，不生成两条；若确实两笔，需有可复核依据 |
| H-04 | 不确定项 | OCR 结果置信不足 | Sheet 标出不确定项，让用户确认 |
| H-05 | 保存结果 | 用户确认后保存 | 分类、金额、备注正确写入，不生成空记录 |
| H-06 | 退化回归 | 使用历史可识别账单截图 | 不出现「之前能识别，现在识别不出来」 |

### I. 复盘页 AI 指定操作

| 编号 | 用例 | 操作 | 预期 |
|---|---|---|---|
| I-01 | 入口展示 | 打开复盘页 | AI 指定操作入口融入页面，不破坏原复盘结构 |
| I-02 | 餐饮查询 | 输入「帮我看过去三天餐饮类消费」 | 返回结论、列表和简洁图，数据范围正确 |
| I-03 | 交通查询 | 输入「看一下这周交通」 | 返回交通相关记录，不混入无关分类 |
| I-04 | 找重复 | 输入「找找最近有没有重复账单」 | 能列出疑似重复项和理由，不自动删除 |
| I-05 | 通勤补记有历史金额 | 输入「补记过去一周工作日通勤，早晚各一次」且历史有 4.75 | 生成工作日早晚记录，金额复用历史值 |
| I-06 | 通勤补记无历史金额 | 清空或换无历史账号后执行补记 | 要求输入/确认单程金额，不凭空保存 |
| I-07 | 批量确认保存 | 在补记草稿页点确认保存 | 一次性写入账本；保存前可检查条数、日期、金额 |
| I-08 | 取消保存 | 草稿生成后取消 | 账本不新增记录 |
| I-09 | 记账逻辑边界 | 使用 AI 功能后回到记账页手动记账 | 原分类、备注、情绪标签、预览卡逻辑不受影响 |

### J. 外观主题设置

| 编号 | 用例 | 操作 | 预期 |
|---|---|---|---|
| J-01 | 外观入口 | 我的页点击外观设置 | 打开外观 Sheet，含明暗、免费主题、会员主题、分享图开关 |
| J-02 | 免费切免费主题 | 免费用户选择纸境·留白 | 全 App 立即换色并保存 |
| J-03 | 免费点会员主题 | 免费用户选择霓虹深渊 | 不写入主题，进入会员页或出现会员引导 |
| J-04 | 月/年会员主题 | 月/年会员选择标准主题 | 全 App 立即换色并保存 |
| J-05 | 月/年点永久主题 | 月/年会员选择永久限定主题 | 不写入主题，提示永久会员专属 |
| J-06 | 永久会员主题 | 永久会员选择永享主题 | 全 App 立即换色并保存 |
| J-07 | 恢复默认 | 点击恢复默认主题 | 回到叙账默认，明暗模式按预期回到默认策略 |
| J-08 | 跟随系统 | 选择跟随系统后切换 iOS 深浅色 | App 使用对应 light/dark token |
| J-09 | 分享图主题开关 | 打开/关闭「分享图使用当前主题」 | 开启时分享图跟随主题，关闭时使用默认分享图风格 |
| J-10 | 过期回退 | 会员主题使用中模拟过期 | 自动回退到可用主题，不停留在锁定主题 |

### K. 5 个主 Tab 主题回归

| 编号 | 用例 | 操作 | 预期 |
|---|---|---|---|
| K-01 | 首页 | 切换霓虹深渊后看首页 | 无旧绿色残留，今日列表可读 |
| K-02 | 记账 | 切换霓虹深渊后看记账页 | CTA、预览卡、输入态正常，主 CTA 不位移 |
| K-03 | 痕迹 | 切换霓虹深渊后看痕迹页 | 生活/线索/细查颜色同步主题 |
| K-04 | 复盘 | 切换霓虹深渊后看复盘页 | AI 指定操作和原复盘模块视觉协调 |
| K-05 | 我的 | 切换霓虹深渊后看我的页 | 设置项、会员卡、外观入口颜色同步 |

### L. 核心回归

| 编号 | 用例 | 操作 | 预期 |
|---|---|---|---|
| L-01 | 手动记账 | 输入金额、改分类、改备注、保存 | 分类/备注/情绪标签以最后一次用户意图为准 |
| L-02 | 今日列表左滑删除 | 左滑今日记录点删除 | 删除按钮可点；右滑恢复和上下滚动不冲突 |
| L-03 | 今日列表右滑恢复 | 删除后右滑或恢复操作 | 恢复正常，不误触删除 |
| L-04 | 播放 | 痕迹/复盘播放 | 播放入口正常，播放服务不回归 |
| L-05 | 云备份账号切换 | A 退出、B 登录 | 本地数据保留；云备份开关按账号级设置读取 |
| L-06 | 会员 UI 边界 | 免费/会员/过期三种状态来回切 | 不出现免费可见会员专属、会员看不到入口等错乱 |

## 四、45 格主题矩阵

建议使用 3 个代表主题做完整走查：

- 叙账默认
- 霓虹深渊
- 纸境·留白

每个主题分别测：

| 页面 | 浅色 | 深色 | 跟随系统 |
|---|---|---|---|
| 首页 | 通过 | 通过 | 通过 |
| 记账 | 通过 | 通过 | 通过 |
| 痕迹 | 通过 | 通过 | 通过 |
| 复盘 | 通过 | 通过 | 通过 |
| 我的 | 通过 | 通过 | 通过 |

验收标准：

- 没有旧默认绿 `#7FB3A2` 在高差异主题下突兀残留。
- 所有正文在玻璃/纸张背景上可读。
- 按钮、选中态、分类色条随主题变化。
- 分享图主题开关开/关行为明确。

## 五、建议测试顺序

1. 先真机编译启动，确认没有编译错误和启动崩溃。
2. 测会员状态：恢复购买、过期刷新、切换账号。
3. 测记账页：免费 3 场景包、主 CTA 位置、原分类/备注/情绪标签逻辑。
4. 测 OCR：支付宝详情页、列表页、重复金额。
5. 测痕迹页：生活/线索、深读、细查入口。
6. 测复盘页：AI 指定操作查询和批量补记。
7. 测外观主题：免费/会员/永久边界、5 Tab、分享图。

## 六、重点观察项

- 会员状态不要出现「提示购买没完成，但状态已刷新」。
- 免费用户不要在记账页主 CTA 附近看到抢视觉的场景包推广。
- 免费 3 场景包可以用，但不能越权看到会员全包。
- OCR 不能为了去重导致列表账单识别退化。
- AI 批量补记必须确认后保存，不能静默写入。
- 主题切换必须全 App 生效，不能出现设置里选了但主界面不变。
- 记账页最后一次用户意图逻辑不能被新功能影响。
