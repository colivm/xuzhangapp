你在 xuzhangapp 实现 Task UI-P2.1 — 痕迹 Tab「切片先行」去工具感（小 PR · 仅 StatsWebView + ContentView pageTitle）。

================================================================================
边界
================================================================================

只做痕迹页信息架构重排 + 文案壳 + 列表样式对齐首页；不动 PlaybackService / 次数逻辑 / 会员。

可改：NativeDemoApp/Views/StatsWebView.swift、ContentView.swift（AppTab.pageTitle 痕迹一项）
禁止：InsightWebView、RecordView、文案引擎、web-preview、git commit

================================================================================
产品目标
================================================================================

痕迹 Tab 名是「痕迹」，首屏应是「听/读这一段生活」，不是「查账报表」。
对齐首页「今天留下的痕迹」气质；账单筛选与合计降级为附录。

================================================================================
任务
================================================================================

1. statsContent 顺序改为：
   summarySliceCard（生活切片/生活章 hero，置顶）
   → overviewPanel（「这一段」旁白 + 轻量合计，趋势一句化）
   → filtersPanel 改为可折叠「细查这一段」，默认 collapsed
   → recordListPanel 标题「这一段里的笔笔」，行样式对齐 HomeView.billListItem

2. filtersPanel 收起态：一行 quiet link「细查时间与分类」，展开才显示时间/分类控件

3. overviewPanel：保留合计数字但缩小；趋势区优先一句 billTrendInsight 式旁白，图表可保留但高度减半或 secondary

4. summarySliceCard：emoji 🎬 可保留；主按钮文案「播放」→「听听这一段」；副文案对齐首页回放卡语气

5. emptyRecordListText：「还没有账单」→「这一段还没有痕迹，先去记下一笔。」

6. ContentView AppTab.pageTitle .stats：「账单与切片」→「这一段痕迹」

7. filtersPanel 内 h2「账单」删除或改为折叠区内小字，避免首屏审计感

================================================================================
验收
================================================================================

[ ] 打开痕迹 Tab：第一眼是生活切片/章，不是筛选器
[ ] 默认看不到时间/分类下拉
[ ] 列表气质接近首页「今天留下的痕迹」
[ ] 播放切片 / 编辑账单 / 会员锁 零回归

@WALKTHROUGH_ISSUES_QUEUE.md
@NativeDemoApp/Views/StatsWebView.swift
@NativeDemoApp/Views/HomeView.swift
@NativeDemoApp/ContentView.swift
