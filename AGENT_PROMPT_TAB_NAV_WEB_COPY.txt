你在 xuzhangapp 实现 Task TAB-NAV-WEB — 五 Tab 命名 + 暖 glyph 图标（仅 web-preview/；同一 PR）。

================================================================================
范围（严格）
================================================================================

只做：web-preview/index.html、web-preview/styles.css、web-preview/app.js（仅 pageTitle 等导航文案映射）
禁止改：NativeDemoApp/、backend/、各页业务逻辑、记账/回放/会员引擎
禁止：git commit（除非用户明确要求）

================================================================================
目标
================================================================================

去掉 demo 期 Tab 名（看看花、小 AI 说、我的小窝）与冷/工具感 SVG。
对齐叙账气质：暖色 glass、记·叙·议；先叙后议（AI 不进 Tab 名）。
图标温暖好看——自定义 SVG glyph，禁止换成冷冰冰的通用 icon font。

================================================================================
定稿命名
================================================================================

底栏 Tab 文案（.tabbar 五个 <span> 末子）：
  home     → 今天
  record   → 记下
  stats    → 痕迹
  insight  → 复盘
  settings → 我的

顶栏 #pageTitle（app.js pageTitles 对象，与 Tab 名区分）：
  home     → 今日
  record   → 记下这一笔
  stats    → 账单与切片
  insight  → 生活复盘
  settings → 设置

禁止 Tab 底栏文案：小 AI、小窝、看看花、记账（作 Tab）

index.html 初始 #pageTitle 与 pageTitles.home 一致（今日）。

================================================================================
暖 glyph SVG 规范（viewBox 0 0 24 24 · 沿用 .tab-icon 结构）
================================================================================

保留现有 class 体系：tab-bg、tab-glyph、tab-glyph-soft、tab-cutout、tab-dot
色温由 CSS 控制（styles.css 已有暖色 mix），SVG 只改 path 造型

五枚隐喻（禁止旧 demo 造型）：
  今天   地平线弧 + 小太阳/柔光升起（不要房子）
  记下   折角纸条 + 轻笔意线条（不要大圆加号）
  痕迹   缓曲线 + 2~3 软圆点（不要心形/花瓣）
  复盘   微张双页/书页扇形（不要对话气泡）
  我的   圆肩人形轮廓（不要房子/齿轮）

线条：stroke-linecap round；留白充足；五枚视觉权重一致
可保留 1 种暖色 tab-dot 点缀（#ffb59f 或现有色），语法统一

================================================================================
app.js 改动范围（最小）
================================================================================

仅允许改：
  const pageTitles = { ... }  — 对齐上表顶栏文案
  若 switchTab / 初始化有硬编码旧 Tab 名，一并改

禁止改：记账 tier、playback、会员、场景包、耳语池等业务

================================================================================
styles.css 改动范围（可选微调）
================================================================================

仅 tabbar / .tab-icon 相关：选中态、暖色混色、字号
禁止大范围改全局主题

================================================================================
实施顺序
================================================================================

1. 改 app.js pageTitles 顶栏文案
2. 改 index.html tabbar 五个底栏 <span> 文案
3. 重绘五个 tab SVG path（隐喻见上）
4. index.html 初始 pageTitle → 今日
5. 本地打开预览验收

================================================================================
必须先 Read
================================================================================

@web-preview/index.html
@web-preview/styles.css
@web-preview/app.js
@LOGO_BRIEF.md
@AGENT_PROMPT_TAB_NAV_POLISH.md

================================================================================
验收
================================================================================

[ ] 底栏：今天|记下|痕迹|复盘|我的
[ ] 顶栏切换：今日|记下这一笔|账单与切片|生活复盘|设置
[ ] 五个 SVG 无房子重复、无气泡/三心
[ ] 选中/未选中态清晰；与 glass UI 同温
[ ] Tab 切换、stats 引导红点、各页功能不回归
[ ] 仅 web-preview/ 有 diff

================================================================================
交付
================================================================================

1. 改动文件列表（应 <=3）
2. 命名对照表（旧→新）
3. 五枚图标隐喻各一句
4. 验收勾选
