# 生活语义与回放边界回归文档 2026-06-24

## 覆盖范围

本轮改动集中在 iOS 端，目标是让账单更贴近用户真实意图，而不是只按一级分类套文案。重点覆盖：

- 记账页备注、分类、场景包、换个角度、换一句的联动边界
- 免费用户可用场景包、隐式场景包和生活印记奖励边界
- 情绪标签、生活印记、今日回放、周/月切片、分享图的语义一致性
- 今日回放和痕迹页的会员引导、次数提示和系统弹窗替换
- 首页左滑删除还原、AI 指令台跳转会员、相关记录排序
- 保存性能、天气上下文、晚下班通勤、饮品/咖啡等细分场景

## 编译与静态检查

- 已执行 `git diff --check`：通过。
- 已执行冲突标记检查 `<<<<<<<|=======|>>>>>>>`：未发现。
- 已执行系统 Alert 残留检查 `.alert(`/`Alert(`：未发现。
- 已执行乱码/异常关键词检查：未发现 `????`、`5496`、`5976`、`�`、`咖啡奶茶`、`靠它提神`。
- 当前环境为 Windows，未检测到 `xcodebuild` 和 `swift` 命令，无法在本机执行 iOS 真编译。需要在 macOS/Xcode 环境补跑 `xcodebuild`。

## 必回归用例

### 1. 手动备注和场景包联动

| 场景 | 操作 | 期望 |
| --- | --- | --- |
| 备注符合场景包 | 输入“路亚”，选择兴趣/装备类场景包，点“换个角度” | 备注不被改写，只切换角度、分类语义、情绪标签、生活印记方向 |
| 基于备注换一句 | 输入“路亚”，再点“换一句” | 文案贴着用户想法优化，例如“钓鱼装备补充”，不能跳到无关场景 |
| 备注和场景冲突 | 输入“吃饭”，再选择购物/兴趣场景包 | 可切换到最后选择的场景包基调，但分类、备注、情绪标签必须有理有据 |
| 快捷备注后改分类 | 从“日常添置”改分类，再选快捷备注 | 快捷备注不能把分类错误改成餐饮，后续场景包仍可生效 |
| 换一句无响应 | 连续点“换一句” | 每次有可见变化，且不跳到无关文案 |

### 2. 未选择场景包与免费用户边界

| 场景 | 操作 | 期望 |
| --- | --- | --- |
| 免费用户未选场景包 | 只输入“猫砂”，连续点“换一句” | 可给毛孩子相关文案，但隐式场景包只能来自免费可用包或已激活奖励包 |
| 免费用户输入游泳 | 没有运动健康场景包时输入“游泳” | 可按健康/运动语义生成，但不能偷偷使用锁定场景包权益 |
| 生活印记奖励包 | 用户触发毛孩子奖励包后输入宠物用品 | 允许使用已激活奖励包，不应误判为全量会员包 |
| 无场景包普通分类 | 输入普通餐饮/交通/日用备注并点换一句 | 按分类和用户备注生成，不强行套场景包 |

### 3. 饮品、咖啡与吃饭边界

| 场景 | 操作 | 期望 |
| --- | --- | --- |
| 水溶 C100 | 记录“水溶 c100” | 可归餐饮大类，但情绪标签不能是“好好吃饭”，应是饮品语义 |
| 瑞幸/咖啡 | 记录“瑞幸咖啡” | 生活印记为“咖啡饮品”，连续文案为“饮品补给” |
| 旧错误记录展示 | 已保存记录旧标签含“连续 3 天好好吃饭”但标题是饮料 | 展示层纠偏为“连续 3 天饮品补给” |

### 4. 晚下班通勤与天气

| 场景 | 操作 | 期望 |
| --- | --- | --- |
| 21 点后下班通勤 | 21:37 记录“下班”交通 | 情绪标签体现晚下班，例如“晚下班路上辛苦了” |
| 晚下班遇雨 | 21 点后通勤且已有雨天上下文 | 标签融合为“晚下班遇上雨，慢点到家”，今日回放自然安慰 |
| 改备注不倒灌天气 | 旧记录没有天气上下文，晚上下雨时编辑备注 | 不应因为编辑当下天气生成雨天效果 |
| 已有雨天上下文 | 记录原本保存了雨天上下文，编辑备注 | 雨天效果保留 |
| 首页/今日回放/痕迹 | 晚下班通勤进入今日记录 | 首页小记、今日回放、痕迹摘要都能自然融入，不出现奇怪语义 |

### 5. 今日回放与会员引导

| 场景 | 操作 | 期望 |
| --- | --- | --- |
| 今日回放免费次数 | 免费用户当天听今日回放 | 每日 3 次，提示清楚剩余/用完，不让第一页主 CTA 显得动不动收费 |
| 用完提示 | 今日回放次数用完 | 自定义弹窗，说明明天刷新，主 CTA 可跳会员详情 |
| 指令台会员引导 | AI 指令台批量补记无会员 | 先关闭当前 sheet，再跳会员详情，不能卡在 sheet 后面 |
| 痕迹/切片会员引导 | 周/月回放次数用完 | 自定义弹窗，不出现系统 Alert |

### 6. 痕迹页与生活印记

| 场景 | 操作 | 期望 |
| --- | --- | --- |
| 健身恢复图标 | 痕迹页出现“健身恢复” | 图标显示运动语义，例如 `🏋️` |
| 看病买药图标 | 痕迹页出现“看病买药” | 默认 `💊`，医院问诊类 `🏥`，护理康复类 `🩹` |
| 周分享/生活切片 | 有情绪标签和生活印记 | 文案能利用情绪点和生活印记，不只报分类金额 |
| 生活切片结束提示 | 生成切片结束 | “已放到首页”提示 3 秒自动消失 |

### 7. AI 指令台

| 场景 | 操作 | 期望 |
| --- | --- | --- |
| 查本周通勤 | 输入“查本周通勤” | 相关记录按时间倒序，最新在上；同一时间再按金额兜底 |
| 相关记录明细 | 查看 AI 查询结果 | 列表用于核对明细，不按金额抢排序 |
| 重新整理一次 | 点击重新整理 | 不改变排序规则，不丢筛选范围 |

### 8. 首页列表左滑删除

| 场景 | 操作 | 期望 |
| --- | --- | --- |
| 左滑显示删除 | 首页今日列表左滑 | 删除按钮出现，滑动跟手 |
| 右滑还原 | 删除按钮出现后右滑 | 删除按钮隐藏 |
| 点其他地方还原 | 删除按钮出现后点列表空白/其他卡片 | 删除按钮隐藏 |
| 滚动还原 | 删除按钮出现后上下滚动 | 删除按钮隐藏 |
| 性能体感 | 连续滑动多条 | 不应明显卡顿 |

### 9. 保存性能

| 场景 | 操作 | 期望 |
| --- | --- | --- |
| 手动保存 | 记账页保存一笔 | 立即返回首页，后台再做回放/引导/同步等后续工作 |
| 输入备注 | 连续输入文字 | 保留防抖，不应每输入一个字触发全量搜索 |
| 场景包/换一句 | 连续点击 | 不引入明显卡顿 |

## 残留风险

- Windows 环境无法执行 iOS 编译，需要 macOS/Xcode 做最终编译和真机回归。
- 全局仍能搜到一些老文案包含“这一笔”，主要在旧 `ContentView`、品牌文案池、宠物陪伴文案和旧编辑占位。它们不全属于本轮触达路径，建议单独做一次文案治理，避免扩大本次改动风险。
- `web-preview/app.js` 在工作区显示 modified，但 `git diff` 无实际内容差异，疑似行尾状态噪音，本次不纳入提交。

## 建议验收顺序

1. 先测记账页：备注/场景包/换一句/换个角度/免费场景包边界。
2. 再测首页：保存速度、左滑删除、今日回放次数、晚下班通勤、饮品标签。
3. 再测痕迹页：生活印记图标、周/月切片、会员引导、切片完成 toast。
4. 最后测 AI 指令台：查本周通勤排序、会员跳转、批量补记保存。
