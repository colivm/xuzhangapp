# 叙账 · 第三方短信平台申请模板

> 适用场景：手机号验证码登录（`POST /v1/auth/sms/send`）  
> 更新时间：2026-06-01  
> 可直接复制到第三方短信控制台提交审核

---

## 1. 项目基本信息（填表用）

| 字段 | 内容 |
|------|------|
| 产品名称 | 叙账 |
| 产品类型 | iOS App（AI 记账） |
| 使用场景 | 用户登录 / 身份验证 |
| 接口域名 | `https://api.xuzhangapp.com` |
| 主域名 | `xuzhangapp.com` |
| Bundle ID | `com.xuzhang.app` |
| 验证码位数 | 6 位数字 |
| 验证码有效期 | 5 分钟 |
| 发码冷却 | 60 秒/次 |
| 后端接口 | `POST /v1/auth/sms/send` |

---

## 2. 短信签名（SignName）

### 推荐签名

```text
叙账
```

### 签名申请说明（复制到平台「申请理由」）

```text
叙账是一款 iOS 记账 App，用户使用手机号+验证码登录云端账号，
用于同步账单数据与 AI 消费复盘。短信仅用于登录验证码，不做营销推广。
App Bundle ID：com.xuzhang.app
API 域名：https://api.xuzhangapp.com
```

### 签名来源选项建议

| 平台常见选项 | 建议选择 |
|-------------|---------|
| App 名称 | ✅ 优先选这个 |
| 网站 / 域名 | 备选（需备案域名 xuzhangapp.com） |
| 企业名称 | 有营业执照时可选 |

---

## 3. 验证码短信模板（主推）

### 模板 A（标准版，推荐提交）

**模板类型**：验证码  
**模板用途**：登录验证

**模板正文**：

```text
您的验证码为${code}，用于登录叙账，5分钟内有效。请勿泄露给他人。
```

| 项目 | 值 |
|------|-----|
| 变量名 | `code` |
| 变量类型 | 数字 / 字符串 |
| 变量长度 | 6 位 |
| 示例值 | `836291` |

**发送效果示例**：

```text
【叙账】您的验证码为836291，用于登录叙账，5分钟内有效。请勿泄露给他人。
```

---

### 模板 B（简短版，部分平台字数限制更严时用）

```text
您的登录验证码是${code}，5分钟内有效，请勿告知他人。
```

---

### 模板 C（含产品名版，审核要求必须出现 App 名时用）

```text
【叙账】您正在登录叙账，验证码${code}，5分钟有效。如非本人操作请忽略。
```

---

## 4. 各平台变量格式对照

提交模板时，按平台要求调整变量占位符：

| 平台 | 模板变量写法 | 发送参数示例 |
|------|-------------|-------------|
| 通用 / 阿里云 | `${code}` | `{ "code": "836291" }` |
| 腾讯云 | `{1}` | `["836291"]` |
| 云片 | `#code#` | `code=836291` |
| Spug 推送助手 | `${code}`（平台内置模板） | `{ "code": "836291", "targets": "手机号" }` |
| 网易云信 | `${code}` | `{ "code": "836291" }` |
| Submail | `@var(code)` | `{ "code": "836291" }` |

> 本项目计划使用 [Spug 推送助手](https://push.spug.cc/)，详见 `SPUG_SMS_GUIDE.md`。

---

## 5. 平台申请表单 · 场景说明（通用）

**业务场景**：

```text
用户打开叙账 App，在「设置 → 云端账号」输入手机号，
点击「获取验证码」，系统发送 6 位数字验证码；
用户输入验证码后完成登录，用于云端账单同步与会员状态识别。
```

**发送频率说明**：

```text
- 同一手机号 60 秒内仅可请求 1 次
- 验证码 5 分钟后失效
- 验证成功后立即作废
- 仅登录场景，无营销、通知类短信
```

**合规承诺**：

```text
1. 不向用户发送广告、营销类短信
2. 验证码仅用于身份验证
3. 用户手机号加密存储，不对外泄露
4. 提供退订/投诉渠道（App 内设置页可停止使用云端账号）
```

---

## 6. 审核材料清单

| 材料 | 说明 |
|------|------|
| App 名称 | 叙账 |
| App 截图 | 设置页「云端账号 / 手机号登录」界面 |
| 软件著作权 | 有则上传，提高通过率 |
| ICP 备案号 | 备案完成后填写 `xuzhangapp.com` 备案号 |
| 域名 | `xuzhangapp.com` / `api.xuzhangapp.com` |
| 企业营业执照 | 企业主体签名时需要 |

**建议截图内容**：设置页中手机号输入框 + 「发送验证码」按钮 + 验证码输入框。

---

## 7. 与现有接口的对应关系

iOS / 客户端**无需改动**，仍调用：

```http
POST https://api.xuzhangapp.com/v1/auth/sms/send
Content-Type: application/json

{"phone":"13800138000"}
```

验证接口：

```http
POST https://api.xuzhangapp.com/v1/auth/sms/verify
Content-Type: application/json

{"phone":"13800138000","code":"836291"}
```

---

## 8. 提交前自检

- [ ] 签名「叙账」与 App 名称一致
- [ ] 模板类型选 **验证码**（不是营销、通知）
- [ ] 模板含「验证码」字样和有效期说明
- [ ] 模板含「请勿泄露/请勿告知他人」类提示
- [ ] 生产环境关闭 `DEV_ALLOW_SMS_CODE=123456`

---

## 9. 相关文档

- `SPUG_SMS_GUIDE.md` — Spug 推送助手接入（推荐）
- `API_v0.1.md` — 短信接口说明（§3 认证）
- `backend/README.md` — 后端联调
